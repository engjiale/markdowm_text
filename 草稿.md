# 目标个数未知的二维距离–速度超分辨估计：全目标联合精修与BIC选阶

## 摘要

目标个数未知时，二维距离–速度超分辨估计需要同时确定目标个数及其连续参数。在牛顿化正交匹配追踪（NOMP）生成的候选模型中，有限次逐目标循环精修可能留下未充分拟合的目标回波，使新增分量通过吸收这些残差降低模型评分，增加贝叶斯信息准则（BIC）高估目标个数的风险。为此，本文提出结合全目标联合精修与BIC选阶的估计方法：在每轮加入新目标后，利用变量投影消去复幅度，采用Levenberg–Marquardt方法联合更新全部已检测目标的距离和速度；保存各阶候选模型，重新估计复幅度，并按统一的残差平方和与参数计数规则进行BIC比较。

在准静态、精确可分离二维模型和复高斯白噪声条件下，对真实目标个数为0、1、2和3的情形各生成500个场景；非空场景采用等幅目标，单目标信噪比为20 dB。以目标个数正确且所有目标的距离、速度误差均满足门限为端到端成功判据，四类场景等权汇总后，所提方法相对于本文实现的二维NOMP基线，将成功率由57.90%提高至98.50%。同观测模块对照表明，主要增益来自全目标联合精修模块。三目标间隔分带实验中，从0.35 Rayleigh起的已测试分带均满足成功率95%置信区间下限不低于95%的判据，支持将0.35 Rayleigh作为当前实验条件下的保守经验工作边界。

**关键词：** 目标个数估计；二维距离–速度估计；超分辨；牛顿化正交匹配追踪；变量投影；Levenberg–Marquardt方法；贝叶斯信息准则

## 1 引言


在雷达多目标观测中，区分相邻目标并准确估计其距离和径向速度，是目标检测与参数估计中的重要任务[1–4]。传统的距离–多普勒处理方法通过在离散网格上形成图谱并搜索峰值实现估计，具有计算效率高、实现简单等优点[1]。然而，真实距离和速度不一定落在预设网格点上，由此产生的离网误差会使回波能量分散到相邻单元，降低参数估计精度和相邻目标的分辨能力[5–7]。因此，在可接受的计算代价下实现连续距离–速度参数估计，并在目标个数未知时准确确定目标个数，是本文关注的研究问题。下文所称“连续参数”，指取值不必落在预设网格点上的距离或速度参数。

针对离网误差，现有方法主要包括原子范数等连续域凸优化方法[6–8]、离网稀疏贝叶斯学习（sparse Bayesian learning，SBL）与变分推断方法[9–10]、以牛顿化正交匹配追踪（Newtonized orthogonal matching pursuit，NOMP）为代表、通过逐次检测新分量并精修其连续参数的迭代方法[11]，以及RELAX类循环非线性优化方法[12]。其中，NOMP先在过采样离散网格上检测新分量，再利用Newton迭代修正其连续参数，并通过循环精修已有分量和最小二乘幅度回代形成反馈机制[11]。随后，NOMP被扩展至多快拍线谱估计、二维波达方向估计与压缩波束形成、频率捷变雷达以及稀疏正交频分复用（orthogonal frequency division multiplexing，OFDM）感知等场景[13–17]。在此基础上，近期研究进一步考察了二维多快拍NOMP中循环精修步骤的作用[18]，并将NOMP用于相控阵雷达的距离–速度–方位–俯仰角联合估计以及水下弱目标的连续时延–多普勒估计[19–20]。这些研究表明，“离散网格检测—连续参数域精修”框架能够适配不同观测结构和参数维度，也为本文在二维距离–速度场景中生成目标个数不同的候选模型提供了方法基础。


当目标个数 \(K\) 未知时，还需要根据观测数据确定模型阶数。原始NOMP研究了恒虚警率（constant false alarm rate，CFAR）和贝叶斯信息准则（Bayesian information criterion，BIC）两种可替代的停止规则，分别依据残差谱峰检验和加入新分量前后的增量模型比较决定是否继续检测[11]。NOMP-CFAR则先生成候选分量集合，再通过逐次CFAR检验删减分量，并按需补充新分量[21]。变分线谱估计（variational line spectral estimation，VALSE）采用稀疏先验和变分推断，联合估计模型阶数、连续频率与复幅度[10]。本文采用BIC比较目标个数不同的候选模型，通过权衡拟合误差与模型复杂度确定目标个数[22–23]。但候选模型的残差平方和（residual sum of squares，RSS）也受到参数精修程度的影响，因此需要考察精修不足是否会改变BIC排序，进而造成目标个数估计错误。


本文采用的二维NOMP基线在加入新目标后，对全部已检测目标逐个循环精修，每次固定其余目标，仅更新当前目标的距离和速度。目标回波相互干扰时，有限次循环更新可能留下参数优化误差；随后进行的最小二乘幅度回代只调整复幅度，不能直接修正距离和速度。因此，即使候选模型的目标个数正确，仍可能有部分目标回波未被充分拟合。后续新增分量若吸收这部分残差，便可能使BIC偏向更高阶模型。由此，目标个数估计的准确性不仅取决于是否生成正确阶数的候选模型，也取决于各候选模型的连续参数是否得到有效精修。

与逐目标循环精修不同，QNOMP采用同时更新全部已检测分量的联合优化方式[24]。该方法主要面向多输入多输出信道估计与外推，在每轮检测后采用Broyden–Fletcher–Goldfarb–Shanno（BFGS）算法联合更新全部已检测路径的连续参数，并通过线性最小二乘（least squares，LS）消去复增益。在确定模型阶数方面，QNOMP采用CFAR残差门限决定是否继续检测路径，而本文保存目标个数不同的候选模型，再通过统一的BIC比较确定目标个数。

上述研究已将NOMP扩展到二维或多维参数估计，并引入全分量联合优化。不过，在本文考察的代表性工作[14–21,24]中，关于参数精修不足对BIC选阶的影响，基于相同观测或相同候选初值的对照分析仍较为有限。本文据此通过模块对照实验，考察全目标联合精修能否减少参数优化不足引起的目标个数估计错误，并分析各精修环节的作用。

为此，本文在准静态、精确可分离的二维距离–速度模型下，将全目标联合精修嵌入NOMP候选模型生成过程：每轮加入新目标后，利用变量投影（variable projection）消去复幅度，采用Levenberg–Marquardt方法联合更新全部已检测目标的距离和速度，记为VarPro-LM[25–28]。随后保存各阶候选模型，在最终比较前通过未正则化最小二乘重新估计复幅度，并按统一的RSS与参数计数规则计算BIC，确定目标个数[22–23]。

围绕上述研究问题，本文的主要贡献包括以下三个方面：

1. **参数优化误差影响BIC选阶的机制分析。** 分析候选模型的参数优化误差如何通过RSS影响BIC，说明精修不足时，留下的残差为何可能使BIC偏向包含更多目标的模型。
2. **结合全目标联合精修的BIC选阶方法。** 将联合VarPro-LM精修嵌入NOMP的候选模型生成过程，并对空模型以及保存的各非空候选模型采用统一的BIC计算规则，完成目标个数估计。
3. **受控实验与经验工作边界评估。** 通过同观测模块对照实验、相同候选初值下的优化器比较和最小间隔分带实验，分别检验各精修模块的作用、联合优化器的影响以及当前实验条件下的经验工作边界。

仿真结果表明，全目标联合VarPro-LM精修模块是当前实验中端到端性能改善的主要来源。

## 2 信号模型与问题定义

本节在脉冲多普勒雷达建模[1]的基础上，借鉴稀疏OFDM通信感知一体化系统中的二维时延–多普勒NOMP可分离原子结构[17]，以距离–慢时间域的复数回波矩阵为观测数据，建立准静态条件下的二维距离–速度多目标观测模型。与基于稀疏OFDM时频资源构造的二维复指数原子不同，本文模型的距离维为线性调频（linear frequency modulation，LFM）脉压点扩散函数（point spread function，PSF）在连续距离参数处的响应；速度维则采用复指数序列，描述目标径向运动引起的回波相位随脉冲序列的变化。在忽略相干处理间隔内距离走动与加速度的条件下，可以将两部分响应写成可分离的形式。

### 2.1 准静态二维距离–速度观测场景

对于单站脉冲多普勒雷达在一个相干处理间隔（coherent processing interval，CPI）内获取的回波数据[1]，选取局部距离区间作为感兴趣区域（region of interest，ROI），其中包含 \(N_R\) 个距离采样点。每个CPI包含 \(N_p\) 个相干脉冲，因此观测可写为距离–慢时间复数矩阵

\[
Y_{\mathrm{sep}}\in\mathbb C^{N_R\times N_p},
\]

其中，矩阵行对应距离采样，列对应慢时间脉冲。记 \(M=N_RN_p\) 为该矩阵包含的复观测样本数。在单个CPI内，假设目标处于准静态状态，具体而言，忽略CPI内的距离走动与加速度，并将目标的参考距离、径向速度和复幅度视为常量。径向速度采用“接近雷达为正”的符号约定。记距离–慢时间观测中的噪声矩阵为 \(W\in\mathbb C^{N_R\times N_p}\)，并将其建模为零均值圆对称复高斯白噪声。令 \(w=\operatorname{vec}(W)\)，则

\[
w\sim\mathcal{CN}(0,\sigma^2I_M).
\]
其中，不同采样位置的噪声相互独立，每个复噪声样本的实部和虚部均服从方差为 \(\sigma^2/2\) 的零均值高斯分布，因此 \(\sigma^2\) 表示每个复样本的总噪声功率。

令 \(c\) 为光速，\(B\) 为发射信号等效带宽，\(\lambda\) 为载波波长，\(T_r\) 为脉冲重复间隔。为统一衡量目标间隔与估计误差，本文采用由雷达参数确定的距离和速度Rayleigh尺度作为参考：

\[
\Delta R_{\mathrm{res}}=\frac{c}{2B},
\qquad
\Delta v_{\mathrm{res}}=\frac{\lambda}{2N_pT_r}.
\]

这两个参考尺度用于目标间隔与定位误差的归一化，与过采样搜索网格的步长不同。离散慢时间采样使速度原子具有周期

\[
P_v=\frac{\lambda}{2T_r},
\]

因此，速度参数只需在长度为 \(P_v\) 的一个主区间内表示。为避免两个周期端点对应同一个慢时间原子，无模糊速度主区间取为 \([-P_v/2,P_v/2)\)。

### 2.2 可分离二维连续原子

在第2.1节的准静态假设下，单目标响应可分解为距离响应与慢时间多普勒响应[1]。对于ROI内任意实值距离 \(R\)，记 \(a_R(R)\in\mathbb C^{N_R}\) 为距离 \(R\) 处的归一化距离原子。距离维PSF描述位于距离 \(R\) 处的单位幅度点目标的脉压响应。

令 \(x[n]\) 为在 \(N\) 点快时间区间内表示的离散LFM发射序列，脉冲持续时间以外取零。其中，\(N\) 同时为构造距离PSF所用的DFT点数，\(X[\kappa]\) 为该序列的中心化 \(N\) 点离散傅里叶变换。记 \(W_R[\kappa]\) 为距离维频域窗，\(F_s\) 为快时间采样率，\(T_s=1/F_s\) 为相应的采样间隔。中心化频率索引集合定义为
\[
\mathcal I_N = \{-\lfloor N/2\rfloor,\ldots,\lceil N/2\rceil-1\}.
\]
距离 \(R\) 对应的双程时延，以采样间隔为单位表示为
\[
d(R)=\frac{2R}{cT_s}.
\]
\(d(R)\) 不必为整数，因此该模型可以表示位于距离采样点之间的目标。记 \(S_{\mathrm{ROI}}\) 为从完整距离响应中选取ROI样点的算子，\(\mathcal F_{N,c}^{-1}\) 为按照 \(\mathcal I_N\) 的中心化频率次序定义的 \(N\) 点逆离散傅里叶变换。则ROI内相应的未归一化距离响应为
\[
\widetilde a_R(R) =S_{\mathrm{ROI}}\mathcal F_{N,c}^{-1}\!\left( \left[ |X[\kappa]|^2W_R[\kappa] \exp\!\left(-\mathrm j\frac{2\pi\kappa}{N}d(R)\right) \right]_{\kappa\in\mathcal I_N} \right).
\]
将其按二范数归一化，得到
\[
a_R(R) = \frac{\widetilde a_R(R)} {\|\widetilde a_R(R)\|_2}.
\]
上述距离原子表示LFM回波经频域匹配滤波、加窗和ROI截取后的脉压响应。连续时延 \(d(R)\) 通过频域相位进入模型，使距离参数 \(R\) 可在ROI内连续优化。

慢时间维采用矩形窗。对于速度 \(v\)，归一化多普勒原子定义为
\[
a_D(v) =\frac{1}{\sqrt{N_p}} \left[ \exp\!\left( \mathrm j\frac{4\pi v}{\lambda}mT_r \right) \right]_{m=0}^{N_p-1} \in\mathbb C^{N_p}.
\]
在忽略CPI内距离走动的条件下，单目标的距离–慢时间响应可写为距离原子与多普勒原子的外积，即
\[
\Psi(R,v) = a_R(R)a_D(v)^{\mathsf T} \in\mathbb C^{N_R\times N_p}.
\]
由于与慢时间索引 \(m\) 对应的矩阵列应保留相位因子 \(a_D(v)[m]\)，式中采用普通转置而非共轭转置。

设场景中包含 \(K\) 个目标。第 \(k\) 个目标的距离、速度和复幅度分别记为 \(R_k\)、\(v_k\) 和 \(\beta_k\)。目标散射系数以及由参考距离引起、在CPI内保持不变的载波相位均并入复幅度 \(\beta_k\)。多目标观测模型为
\[
Y_{\mathrm{sep}} = \sum_{k=1}^{K} \beta_k a_R(R_k)a_D(v_k)^{\mathsf T} +W.
\]
仿真观测的生成与参数估计均采用上述可分离二维原子模型，观测噪声服从第2.1节定义的复高斯白噪声分布。

### 2.3 向量化模型与参数集合

对 \(Y_{\mathrm{sep}}\) 按列向量化，令

\[
y=\operatorname{vec}(Y_{\mathrm{sep}})
\in\mathbb C^M.
\]

记 \(\operatorname{mat}_{N_R,N_p}:\mathbb C^M\rightarrow\mathbb C^{N_R\times N_p}\) 为按列向量化的逆算子，满足 \(\operatorname{mat}_{N_R,N_p}(\operatorname{vec}(X))=X\)，其中 \(X\in\mathbb C^{N_R\times N_p}\)。

利用恒等式 \(\operatorname{vec}(ab^{\mathsf T})=b\otimes a\)，单目标二维向量原子可写为

\[
\phi(R,v)
=\operatorname{vec}\!\left(\Psi(R,v)\right)
=a_D(v)\otimes a_R(R)
\in\mathbb C^M.
\]

将全部目标的距离和速度参数交错排列：

\[
\boldsymbol\theta_K
=\left[R_1,v_1,R_2,v_2,\ldots,R_K,v_K\right]^{\mathsf T}
\in\mathbb R^{2K},
\]

并令

\[
\boldsymbol\beta_K
=\left[\beta_1,\beta_2,\ldots,\beta_K\right]^{\mathsf T}
\in\mathbb C^K.
\]

由 \(K\) 个单目标向量原子组成的原子矩阵定义为

\[
A_K(\boldsymbol\theta_K)
=\begin{bmatrix}
\phi(R_1,v_1)&\phi(R_2,v_2)&\cdots&\phi(R_K,v_K)
\end{bmatrix}
\in\mathbb C^{M\times K}.
\]

于是多目标模型可统一写为

\[
y=A_K(\boldsymbol\theta_K)\boldsymbol\beta_K+w.
\]

其中，\(\boldsymbol\theta_K\) 为需要连续优化的非线性参数；给定 \(\boldsymbol\theta_K\) 后，复幅度 \(\boldsymbol\beta_K\) 可通过线性最小二乘求解。目标排列顺序不影响观测模型，因此比较估计参数与真实参数时，需先进行目标匹配。

### 2.4 目标个数未知时的联合估计问题

设真实目标个数 \(K_{\mathrm{true}}\) 未知，并给定最大候选阶数 \(K_{\max}\)。记实际生成的最大候选阶数为 \(K_{\mathrm{cand}}\)，其不超过预设上限 \(K_{\max}\)，则可供比较的阶数集合为

\[
\mathcal K_{\mathrm{available}}
=\{0,1,\ldots,K_{\mathrm{cand}}\},
\qquad K_{\mathrm{cand}}\leq K_{\max},
\]

算法需从 \(\mathcal K_{\mathrm{available}}\) 中确定最终阶数，并估计相应的距离、速度和复幅度。候选模型生成未提前终止时，\(K_{\mathrm{cand}}=K_{\max}\)。阶数为零的空模型定义为

\[
A_0\in\mathbb C^{M\times0},
\qquad
\widehat{\boldsymbol\beta}_0=\varnothing,
\qquad
\operatorname{RSS}_0=\|y\|_2^2.
\]

对于任意非空阶数 \(K\)，以该阶模型在允许参数域内的RSS下确界作为理想拟合参照：
\[
\operatorname{RSS}_K^{\star} = \inf_{\substack{ \boldsymbol\theta_K\in\Theta_K\\ \boldsymbol\beta_K\in\mathbb C^K}} \left\| y-A_K(\boldsymbol\theta_K)\boldsymbol\beta_K \right\|_2^2.
\]
其中，\(\Theta_K\) 为满足距离与速度边界约束的连续参数集合。若下确界能够取到，相应参数即为全局最优解。实际计算采用有限次局部优化，下文以 \(\widehat{\boldsymbol\theta}_K\) 和 \(\widehat{\boldsymbol\beta}_K\) 表示实际获得的估计结果。

在允许新增目标复幅度为零的嵌套模型中，增加模型阶数不会使全局最优RSS增大。受候选生成及有限次连续参数优化的影响，实际候选的RSS可能偏离相应阶数的全局最优值，并可能在阶数增加时出现上升。由于RSS本身不包含模型复杂度惩罚，不能单独作为未知阶数条件下的选阶准则。

### 2.5 不同阶数候选模型的RSS与BIC选阶准则

为统一不同阶数候选模型的RSS计算口径，在各阶候选模型完成连续参数精修后，固定 \(\widehat{\boldsymbol\theta}_K\)，并选取一个未正则化复幅度最小二乘解：

\[
\widehat{\boldsymbol\beta}_K^{\mathrm{BIC}}
\in \arg\min_{\boldsymbol\beta_K}
\left\|
y-A_K(\widehat{\boldsymbol\theta}_K)
\boldsymbol\beta_K
\right\|_2^2,
\]

相应的RSS定义为

\[
\operatorname{RSS}_K
=\left\|
y-A_K(\widehat{\boldsymbol\theta}_K)
\widehat{\boldsymbol\beta}_K^{\mathrm{BIC}}
\right\|_2^2.
\]

在零均值圆对称复高斯白噪声假设下，将第 \(K\) 阶候选参数代入后，对数似然为

\[
\log L_K
=-M\log(\pi\sigma^2)
-\frac{\operatorname{RSS}_K}{\sigma^2},
\]

在给定候选参数的条件下，噪声方差的极大似然估计为 \(\widehat\sigma_K^2=\operatorname{RSS}_K/M\)。每个目标包含距离和速度两个实参数，以及复幅度的实部和虚部两个实参数，因此阶数相关的实参数数目为

\[
p_K=4K.
\]

变量投影虽在求解过程中消去了复幅度，但复幅度仍属于统计模型，其实部和虚部对应的两个实自由度应计入 \(p_K\)。若进一步将各阶候选模型共有的未知噪声方差计为参数，BIC仅增加与 \(K\) 无关的常数项，所以不改变选阶结果。为避免 \(\operatorname{RSS}_K\) 为零或因数值误差过小而导致对数项不稳定，定义带相对下限的RSS为

\[
\operatorname{RSS}_{K,\mathrm{floor}}
=\max\!\left(
\operatorname{RSS}_K,
\varepsilon_{\mathrm{BIC}}\|y\|_2^2
\right),
\]

其中，\(\varepsilon_{\mathrm{BIC}}>0\) 为控制RSS相对数值下限的常数。去除各阶候选模型共有的常数项后，本文采用的复高斯BIC为[22–23]

\[
\operatorname{BIC}(K)
=2M\log\!\left(
\frac{\operatorname{RSS}_{K,\mathrm{floor}}}{M}
\right)
+4K\log M.
\]

最终估计阶数为

\[
\widehat K
=\arg\min_{K\in\mathcal K_{\mathrm{available}}}
\operatorname{BIC}(K),
\]

并输出所选候选对应的 \(\widehat{\boldsymbol\theta}_{\widehat K}\) 与 \(\widehat{\boldsymbol\beta}_{\widehat K}^{\mathrm{BIC}}\)。

经典BIC的渐近一致性依赖模型局部正则性和参数可辨识性；目标幅度趋近于零或原子趋于重合时，这些条件可能失效。因此，本文采用BIC比较候选模型，并通过蒙特卡洛（Monte Carlo）实验评价其有限样本选阶性能。

BIC的数据拟合项由各阶候选模型的RSS决定，因此连续参数的优化程度会影响选阶结果。未正则化复幅度回代统一了RSS的计算口径，但不能修正距离和速度的估计误差，也不能保证各阶候选模型达到全局最优。第3章据此，介绍结合全目标联合精修的候选模型生成与BIC选阶方法。

## 3 全目标联合精修与BIC选阶方法

### 3.1 方法概述

本文将未知阶数估计划分为候选模型生成和跨阶模型选择两个阶段。从空模型出发，每轮通过残差搜索、新目标局部精修和全目标联合精修生成一个更高阶候选模型[11]，形成候选序列
\[
\mathcal M_0,\mathcal M_1,\ldots,\mathcal M_{K_{\mathrm{cand}}}.
\]
其中，\(\mathcal M_m\) 表示包含 \(m\) 个目标的候选模型，\(K_{\mathrm{cand}}\le K_{\max}\)。每一阶候选模型以上一阶的估计结果和新目标初值为起点，联合精修时同时调整已有目标与新目标的距离和速度。候选生成完成后，按第2.5节统一计算未正则化RSS与BIC，并选择BIC最小的模型。

本文采用两种候选生成模式：全阶BIC模式生成零至 \(K_{\max}\) 阶的全部候选模型；CFAR+BIC模式允许根据当前残差峰值提前停止，随后仅在实际生成的候选模型中选阶。两种模式采用相同的全目标联合精修和RSS计算口径，区别在于候选生成的停止规则。CFAR判定公式见第3.2节，具体配置与校准依据见第4.5.1节。

### 3.2 二维残差搜索与候选模型生成

设第 \(m-1\) 轮联合精修后的活动原子矩阵为 \(A_{m-1}\)，并用 \(\lambda_{\mathrm{ridge}}\) 表示用于数值稳定的有效岭系数，其具体定义见第3.3节。在最终活动位置上，利用原始观测 \(y\)，通过带岭正则化的最小二乘法重新估计内部复幅度：
\[
\widehat{\boldsymbol\beta}_{m-1} = \arg\min_{\boldsymbol\beta\in\mathbb C^{m-1}} \left( \|y-A_{m-1}\boldsymbol\beta\|_2^2 +\lambda_{\mathrm{ridge}}\|\boldsymbol\beta\|_2^2 \right).
\]
该复幅度仅用于候选生成阶段的残差递推，不同于新目标局部精修得到的单目标幅度，也不同于BIC计算前重新求解的未正则化复幅度。当 \(m=1\) 时，尚无已检测目标。此时 \(A_0\) 和 \(\widehat{\boldsymbol\beta}_0\) 分别为空原子矩阵和空幅度向量，其乘积为 \(\mathbf 0_M\)，因而初始残差满足 \(e_0=y\)。第 \(m\) 轮粗搜索使用的残差为
\[
\begin{aligned}
e_{m-1}
&=y-A_{m-1}\widehat{\boldsymbol\beta}_{m-1},\\
E_{m-1}
&=\operatorname{mat}_{N_R,N_p}(e_{m-1})\\
&=Y_{\mathrm{sep}}
-\sum_{\ell=1}^{m-1}
\widehat\beta_\ell
\Psi(\widehat R_\ell,\widehat v_\ell).
\end{aligned}
\]
残差 \(E_{m-1}\) 中可能包含尚未检测的目标、噪声及已有目标的拟合误差。

设距离和速度搜索网格字典分别为

\[
A_R=
\begin{bmatrix}
a_R(R_1^g)&\cdots&a_R(R_{G_R}^g)
\end{bmatrix},
\]

\[
A_D=
\begin{bmatrix}
a_D(v_1^g)&\cdots&a_D(v_{G_v}^g)
\end{bmatrix}.
\]

利用二维原子的可分离结构，全部网格点与当前残差的相关值可一次计算为

\[
C_m=A_R^{\mathsf H}E_{m-1}\overline{A_D}
\in\mathbb C^{G_R\times G_v}.
\]

对于网格点 \((R_i^g,v_j^g)\)，归一化匹配分数为

\[
S_{ij}^{(m)}
=\frac{|[C_m]_{ij}|^2}
{\|a_R(R_i^g)\|_2^2\|a_D(v_j^g)\|_2^2}.
\]

由于两类字典的列向量均具有单位二范数，匹配分数简化为 \(S_{ij}^{(m)}=|[C_m]_{ij}|^2\)。该分数表示向当前残差加入对应单原子，并通过未正则化最小二乘估计其复幅度后可获得的最大RSS下降。取分数最大的网格点作为连续参数精修初值。

为避免重复选择已检测目标附近的残余峰，算法首先排除历次粗搜索已选中的网格点，再排除当前距离–速度估计附近的网格点。对于每个网格点，分别将其与已有目标估计之间的距离差和速度差除以相应的搜索网格步长，并以两项归一化差值构成的二维欧氏距离作为排重依据；其中，速度差采用无模糊速度周期内的最短差值。当该归一化距离不大于0.5时，排除相应网格点。记排重后的有效网格集合为 \(\mathcal G_{m-1}^{\mathrm{valid}}\)，则本轮粗候选满足

\[
(\widehat i_m,\widehat j_m)
=\arg\max_{(i,j)\in\mathcal G_{m-1}^{\mathrm{valid}}}
S_{ij}^{(m)},
\]

\[
(\widehat R_{m,\mathrm{grid}},
\widehat v_{m,\mathrm{grid}})
=(R_{\widehat i_m}^g,v_{\widehat j_m}^g).
\]

\(\mathcal G_{m-1}^{\mathrm{valid}}\) 中的网格位置用于初始化新目标，\(\mathcal M_m\) 则表示包含 \(m\) 个目标的候选模型。粗搜索仅确定局部精修初值，残差在活动集精修与幅度更新后再更新。

当启用CFAR+BIC模式时，在接受每轮粗搜索候选前，对当前残差的最大匹配分数进行CFAR检验。整个候选生成过程最多进行 \(J=K_{\max}\) 轮检验，每轮至多加入一个新目标。设名义总虚警概率为 \(p_{\mathrm{fa}}\)，等效独立搜索单元数为 \(N_{\mathrm{cell}}\)，按独立检验近似，将其分配为每轮虚警概率和单元尾概率：
\[
p_{\mathrm{fa,step}} =1-(1-p_{\mathrm{fa}})^{1/J}, \qquad q=1-(1-p_{\mathrm{fa,step}})^{1/N_{\mathrm{cell}}}.
\]
在纯复高斯白噪声和固定单位范数原子条件下，匹配分数除以噪声功率服从单位均值指数分布。据此，利用当前残差的噪声功率估计 \(\widehat{\sigma}^{2}\) 构造阈值
\[
\tau=-\widehat{\sigma}^{2}\log q.
\]
若
\[
\max_{(i,j)\in\mathcal G_{m-1}^{\mathrm{valid}}} S_{ij}^{(m)}>\tau,
\]
则继续执行候选精修与加入步骤；否则停止候选扩展。由于搜索原子及各轮检验之间存在相关性，上述概率分配用于近似阈值设计。噪声估计方法、具体参数及校准依据见第4.5.1节。

本轮粗候选经局部精修后，得到距离和速度的初值 \((\widehat R_m^{\mathrm{loc}},\widehat v_m^{\mathrm{loc}})\)。将该初值与上一轮的联合精修结果组合，构成第 \(m\) 阶活动集初值。当 \(m=1\) 时，局部精修结果可直接作为单目标联合结果；当 \(m\geq2\) 时，以原始观测 \(y\) 为拟合数据，对全部 \(m\) 个目标执行第3.3—3.5节所述的联合VarPro-LM。完成全部内部复幅度的重新估计后，再按第3.6节更新残差并保存该阶候选模型。

### 3.3 变量投影与复幅度消元

单目标局部精修与全目标联合精修采用同一VarPro-LM求解器[25–28]。令 \(z\) 表示待拟合数据。局部精修时取 \(z=e_{m-1}\) 和 \(K=1\)；全目标联合精修时取 \(z=y\) 和 \(K=m\)。给定连续参数
\[
\boldsymbol\theta =[R_1,v_1,\ldots,R_K,v_K]^{\mathsf T},
\]
候选生成阶段采用带岭正则化的可分离非线性最小二乘目标：
\[
\min_{\boldsymbol\theta,\boldsymbol\beta} \left\{ \|z-A(\boldsymbol\theta)\boldsymbol\beta\|_2^2 +\lambda_{\mathrm{ridge}} \|\boldsymbol\beta\|_2^2 \right\}.
\]
令 \(\rho_{\mathrm{ridge}}\geq0\) 为无量纲相对岭系数，并定义有效岭系数为
\[
\lambda_{\mathrm{ridge}} =\rho_{\mathrm{ridge}} \max\!\left( \frac{\|A(\boldsymbol\theta)\|_F^2}{K}, 1 \right).
\]
由于二维原子均为单位范数，\(\|A(\boldsymbol\theta)\|_F^2/K=1\)，故 \(\lambda_{\mathrm{ridge}}=\rho_{\mathrm{ridge}}\)；取值见表4-3。

固定 \(\boldsymbol\theta\) 后，复幅度是线性参数。为避免显式形成正规方程的逆，本文通过增广最小二乘计算其条件最优解：

\[
\widehat{\boldsymbol\beta}(\boldsymbol\theta)
=\arg\min_{\boldsymbol\beta}
\left\|
\begin{bmatrix}
A(\boldsymbol\theta)\\
\sqrt{\lambda_{\mathrm{ridge}}}I
\end{bmatrix}
\boldsymbol\beta-
\begin{bmatrix}
z\\0
\end{bmatrix}
\right\|_2^2.
\]

将该条件最优解回代原目标函数并消去复幅度变量，得到仅以距离和速度为非线性变量的带岭约化目标函数（下文简称约化目标函数）

\[
F(\boldsymbol\theta)
=\|r(\boldsymbol\theta)\|_2^2
+\lambda_{\mathrm{ridge}}
\|\widehat{\boldsymbol\beta}(\boldsymbol\theta)\|_2^2,
\]

其中

\[
r(\boldsymbol\theta)
=z-A(\boldsymbol\theta)
\widehat{\boldsymbol\beta}(\boldsymbol\theta).
\]

对每个LM试探位置，均重新构造连续原子并求解条件最优复幅度，使非线性优化仅涉及 \(2K\) 个距离和速度参数。复幅度仍属于统计模型，因此BIC参数计数按第2.5节取 \(p_K=4K\)。

岭正则用于提高候选生成阶段内部复幅度回代与连续优化的数值稳定性，尤其适用于原子近相关的情形。候选位置确定后，按照第2.5节的未正则化口径重新估计 \(\widehat{\boldsymbol\beta}^{\mathrm{BIC}}_K\) 并计算RSS。因此，带岭约化目标函数 \(F\)、其中的数据残差平方和 \(\|r\|_2^2\) 与BIC使用的未正则化RSS应分别记录，不能混用；岭惩罚不进入BIC的数据拟合项。不过，岭正则化仍可能通过影响候选位置的优化结果，间接改变最终RSS及BIC选阶。本文采用固定相对岭系数 \(\rho_{\mathrm{ridge}}=10^{-8}\)。

### 3.4 Kaufman型投影近似雅可比矩阵

第3.3节通过变量投影消去复幅度，得到仅以距离和速度为非线性变量的约化目标函数。为构造LM更新方向，还需描述相应残差关于全部距离和速度参数的一阶变化。Golub–Pereyra公式给出了变量投影后约化残差雅可比矩阵的精确形式[25–26]；本文采用Kaufman型投影近似[28]，保留其中的主要投影项，并省略与当前残差成比例的修正项。对第 \(k\) 个目标，二维原子关于距离和速度的导数分别为

\[
\phi_{k,R}
=a_D(v_k)\otimes
\frac{\partial a_R(R_k)}{\partial R_k},
\qquad
\phi_{k,v}
=\frac{\partial a_D(v_k)}{\partial v_k}
\otimes a_R(R_k).
\]

根据第2.1节采用的速度符号约定，慢时间原子的解析导数为

\[
\frac{\partial a_D(v)[m]}{\partial v}
=\mathrm j\frac{4\pi}{\lambda}mT_r\,a_D(v)[m],
\qquad m=0,\ldots,N_p-1.
\]

距离原子由LFM脉压PSF在连续距离参数处的响应数值生成，其距离导数采用边界感知的两点有限差分。在ROI内部使用中心差分；靠近边界时，根据合法端点构造非对称差分。有限差分直接作用于归一化后的原子，因此包含归一化因子随距离变化产生的影响。

为简化记号，以下省略各变量对 \(\boldsymbol\theta\) 的显式依赖，并记

\[
A=A(\boldsymbol\theta),\qquad
\widehat{\boldsymbol\beta}
=\widehat{\boldsymbol\beta}(\boldsymbol\theta),\qquad
r=r(\boldsymbol\theta).
\]

在当前复幅度处，固定幅度时的模型导数矩阵为

\[
D_{\theta}
=\begin{bmatrix}
\widehat\beta_1\phi_{1,R}&
\widehat\beta_1\phi_{1,v}&
\cdots&
\widehat\beta_K\phi_{K,R}&
\widehat\beta_K\phi_{K,v}
\end{bmatrix}
\in\mathbb C^{M\times 2K}.
\]

变量投影会在每组距离和速度参数取值处重新估计全部复幅度。因此，\(D_\theta\) 中能够由当前活动原子的线性组合解释的部分，不应继续用于驱动距离和速度更新。为去除该部分，求解投影系数

\[
C_{\lambda}
=\arg\min_{C\in\mathbb C^{K\times 2K}}
\left\{
\|D_\theta-AC\|_F^2
+\lambda_{\mathrm{ridge}}\|C\|_F^2
\right\}.
\]

进一步定义增广原子矩阵、增广残差和增广导数矩阵：

\[
\widetilde A=
\begin{bmatrix}
A\\
\sqrt{\lambda_{\mathrm{ridge}}}I_K
\end{bmatrix}
\in\mathbb C^{(M+K)\times K},
\]

\[
\widetilde r=
\begin{bmatrix}
r\\
-\sqrt{\lambda_{\mathrm{ridge}}}
\widehat{\boldsymbol\beta}
\end{bmatrix}
\in\mathbb C^{M+K},
\qquad
\widetilde D_\theta=
\begin{bmatrix}
D_\theta\\
0_{K\times 2K}
\end{bmatrix}
\in\mathbb C^{(M+K)\times 2K}.
\]

由此得到本文采用的Kaufman型投影近似雅可比矩阵：

\[
\widetilde J
=\widetilde D_\theta-\widetilde A C_\lambda
=
\begin{bmatrix}
D_\theta-AC_\lambda\\
-\sqrt{\lambda_{\mathrm{ridge}}}C_\lambda
\end{bmatrix}.
\]

\(\widetilde J\) 按模型变化方向定义，故增广残差关于 \(\boldsymbol\theta\) 的雅可比近似为 \(-\widetilde J\)，与第3.5节残差线性化中的负号一致。其中，\(\widetilde A C_\lambda\) 表示能够通过重新分配复幅度吸收的一阶模型变化；\(\widetilde J\) 则保留需要通过移动距离或速度参数解释的方向。当 \(K>1\) 时，全部目标对应的 \(2K\) 个导数列参与同一投影，活动原子之间的相关性通过 \(A^{\mathsf H}A\) 和 \(C_\lambda\) 共同进入参数更新方向。

上述近似雅可比矩阵用于构造LM更新方向，试探步则依据重新计算的约化目标函数值进行接受或拒绝。具体更新与停止规则见第3.5节。

### 3.5 无量纲LM联合更新

在经典LM阻尼框架[27]下，对距离与速度参数进行无量纲化，以处理二者的单位与数值尺度差异。定义

\[
S=\operatorname{diag}(s_R,s_v,\ldots,s_R,s_v),
\qquad
q=S^{-1}\boldsymbol\theta,
\]

其中 \(s_R\) 和 \(s_v\) 分别取距离与速度搜索网格的尺度。令 \(\delta\in\mathbb R^{2K}\) 表示本次准备尝试的无量纲参数增量，称为试探步。其对应的物理量增量为 \(\Delta\boldsymbol\theta=S\delta\)，则

\[
J_q=\widetilde J S.
\]

在当前点附近，增广残差满足局部近似

\[
\widetilde r(\boldsymbol\theta+S\delta)
\approx\widetilde r(\boldsymbol\theta)-J_q\delta.
\]

由于距离和速度均为实参数，定义Gauss–Newton近似矩阵及LM方程的右端项为

\[
H=\operatorname{Re}(J_q^{\mathsf H}J_q),
\qquad
g=\operatorname{Re}(J_q^{\mathsf H}\widetilde r).
\]

按本文的残差符号约定，\(g\) 对应约化目标函数关于无量纲参数 \(q\) 的负梯度的一半；实际计算采用有限差分近似距离导数，因此该关系受导数近似误差影响。为避免Gauss–Newton矩阵的对角元素过小，定义
\[
\tau_H=10^{-12}\max\!\left\{\operatorname{Re}[\operatorname{tr}(H)],\epsilon_{\mathrm{mach}}\right\},
\qquad
d_i=\max\{H_{ii},\tau_H\},
\]
并令 \(d=(d_1,\ldots,d_{2K})^{\mathsf T}\)、\(D_H=\operatorname{diag}(d)\)。相应的基于投影雅可比的尺度化梯度指标定义为
\[
\eta_g=\left\|g\oslash\sqrt{d+\epsilon_{\mathrm{mach}}\mathbf 1}\right\|_\infty,
\]
其中，\(\oslash\) 和平方根均表示逐元素运算。为使病态活动集采用更保守的试探步，将活动原子矩阵 \(A\) 按列归一化，并将所得矩阵记为 \(A_{\mathrm n}\)。定义
\[
G=A_{\mathrm n}^{\mathsf H}A_{\mathrm n}, \qquad \kappa_G= \frac{\lambda_{\max}(G)} {\max\{\lambda_{\min}(G),\epsilon_{\mathrm{mach}}\}}.
\]
其中，\(\epsilon_{\mathrm{mach}}\) 为机器精度。在本文采用的单位范数原子下，有 \(A_{\mathrm n}=A\)。令 \(\mu\) 为根据LM步的接受或拒绝状态更新的基础阻尼，\(\kappa_{\mathrm{ref}}\) 为条件数参考值，\(c_{\max}\) 为条件数放大上限，\(\mu_{\mathrm{cap}}\) 为阻尼上限。定义
\[
c_\kappa =\min\!\left( c_{\max}, \max\!\left( 1,\frac{\kappa_G}{\kappa_{\mathrm{ref}}} \right) \right), \qquad \mu_{\mathrm{eff}} =\min(\mu_{\mathrm{cap}},\mu c_\kappa).
\]
LM步由
\[
(H+\mu_{\mathrm{eff}}D_H)\delta=g
\]
确定。阻尼相关参数见表4-3。当活动原子的相关性增强时，\(\kappa_G\) 和 \(c_\kappa\) 随之增大，从而缩小局部试探步；该规则只改变数值阻尼，不改变约化目标函数或BIC定义。

求得 \(\delta\) 后，将其映射回物理单位，并分别限制单次距离和速度更新幅度，再将更新后的距离参数裁剪到ROI内。速度搜索范围覆盖完整无模糊区间时，对更新后的速度参数采用周期回绕；限定局部速度区间时，则将更新后的速度参数裁剪到该区间内。

记限步和边界处理后的参数为 \(\boldsymbol\theta_{\mathrm{trial}}\)，按第3.3节重新求解条件最优复幅度，并计算 \(F(\boldsymbol\theta_{\mathrm{trial}})\)。若其相对当前值的下降量超过规定阈值，则接受更新并减小基础阻尼；否则增大基础阻尼，在当前参数处重新计算试探步，直到接受更新或达到本轮试探次数上限。

当梯度指标 \(\eta_g\) 小于表4-3给出的阈值，或已接受的更新中各目标的距离和速度改变量均小于各自容差时，求解器判定数值收敛并停止迭代。若本轮允许的阻尼试探均未被接受，但最后一次经边界处理后的距离和速度改变量仍满足上述容差要求，求解器同样将当前状态记为数值收敛并停止迭代。

若本轮试探均未被接受，且不满足上述小步长条件，则终止迭代，并将停止原因记为试探步拒绝。当梯度项或Gauss–Newton矩阵出现非有限值，或达到最大迭代次数时，也终止迭代并记录相应原因。

### 3.6 残差递推、BIC选择与算法汇总

记联合精修后的活动原子矩阵为
\[
A_m=A(\widehat{\boldsymbol\theta}_m),
\]
并利用原始观测重新估计全部内部复幅度：
\[
\widehat{\boldsymbol\beta}_m =\arg\min_{\boldsymbol\beta} \left( \|y-A_m\boldsymbol\beta\|_2^2 +\lambda_{\mathrm{ridge}}\|\boldsymbol\beta\|_2^2 \right).
\]
用重新估计的内部复幅度构造下一轮检测残差：
\[
e_m=y-A_m\widehat{\boldsymbol\beta}_m, \qquad E_m=\operatorname{mat}_{N_R,N_p}(e_m).
\]
第 \(m\) 阶候选模型记为
\[
\mathcal M_m= \left( m,\widehat{\boldsymbol\theta}_m, \widehat{\boldsymbol\beta}_m \right).
\]
同时记录终止原因、约化目标函数值、Gram矩阵条件数、重复目标告警和求解状态等诊断量。全阶BIC模式继续扩展至 \(K_{\max}\)；CFAR+BIC模式则可根据当前残差峰值提前停止。两种模式仅在可用候选的最高阶数上可能不同，各阶BIC均按第2.5节计算。

对于因达到最大迭代次数而停止的候选，可选择执行一次追加LM。首先，比较追加精修前后采用内部带岭幅度计算的数据RSS；若通过下降检查，再比较两者经未正则化复幅度回代后得到的RSS。两项检查均通过时，才替换原候选。若求解异常、结果非有限或任一级RSS检查未通过，则保留原候选。

上述迭代上限门控根据候选的求解状态触发，并依据观测数据计算的RSS判断是否接受追加精修结果。该门控不使用真实目标个数及其真实距离、速度或复幅度，仅在全部候选模型生成完成后执行，追加精修结果不参与候选模型的生成。门控开关由第4.3.1节的A–D算法变体设置确定，追加LM的迭代预算和RSS保护容差均在实验前预先给定，具体数值见表4-3。

最后，在空模型及全部可用非空候选模型中选择BIC最小者，并输出其阶数、距离与速度参数，以及按第2.5节重新估计的未正则化复幅度。

本文方法可概括为以下步骤：

1. 初始化空模型 \(\mathcal M_0\)，令 \(e_0=y\)。
2. 对 \(m=1,\ldots,K_{\max}\) 依次执行，直至达到上限或CFAR判定停止候选扩展：
   1. 在 \(E_{m-1}\) 上计算二维相关矩阵并选取排重后的最大分数网格点；
   2. 若启用CFAR且最大分数未超过阈值，停止候选扩展；
   3. 以粗网格点为初值，用单目标VarPro-LM拟合 \(e_{m-1}\)；
   4. 将局部结果加入活动集，在原始观测 \(y\) 上采用联合VarPro-LM执行全目标联合精修；
   5. 重新估计全部复幅度、更新 \(e_m\)，并保存第 \(m\) 阶候选模型 \(\mathcal M_m\)。
3. 对满足迭代上限条件的候选执行可选追加LM，仅在内部数据RSS和未正则化RSS检查均通过时替换原候选。
4. 对 \(\mathcal M_0,\ldots,\mathcal M_{K_{\mathrm{cand}}}\) 统一重算未正则化RSS与BIC。
5. 输出BIC最小候选的阶数、距离、速度和复幅度。

上述矩阵化计算避免了显式构造尺寸为 \(M\times G_RG_v\) 的完整Kronecker字典。在本文采用可分离LFM-PSF原子的求解程序中，一次包含 \(K\) 个目标的联合VarPro-LM迭代需要构造距离原子及其有限差分导数，并完成复幅度最小二乘、投影雅可比矩阵计算和 \(2K\) 维LM系统求解。其主要计算代价可写为
\[
\mathcal O\!\left( KN\log N+MK^2+K^3 \right),
\]
其中 \(KN\log N\) 对应距离原子及其有限差分所需的 \(N\) 点IFFT，\(MK^2+K^3\) 对应复幅度消元、导数投影和LM线性系统求解。

令 \(T_m^{\mathrm{loc}}\) 和 \(T_m^{\mathrm{joint}}\) 分别表示第 \(m\) 阶候选的单目标局部精修迭代次数和全目标联合精修迭代次数。将有界的阻尼试探次数吸收到常数因子中，生成 \(K_{\mathrm{cand}}\) 个非空候选模型的主导计算代价为
\[
\mathcal O\!\left[ K_{\mathrm{cand}} \left( G_RN_RN_p+G_RN_pG_v \right) +\sum_{m=1}^{K_{\mathrm{cand}}} T_m^{\mathrm{loc}} \left( N\log N+M \right) +\sum_{m=1}^{K_{\mathrm{cand}}} T_m^{\mathrm{joint}} \left( mN\log N+Mm^2+m^3 \right) \right].
\]
生成零至 \(K_{\max}\) 阶的全部候选模型需要对多个阶数重复执行联合精修，其计算量通常高于采用提前停止的候选生成流程。第5.2节在相同候选初值和运行环境下，报告目标函数与导数评估次数、停止状态及运行时间。

## 4 实验设置与评价方法

本章依次说明雷达与观测参数、随机目标几何与样本设置、对照实验设计、评价指标与统计规则，以及CFAR配置和数值实现参数。三套预设实验分别为A–D同观测精修消融、相同候选初值下的优化器比较和最小间隔分带实验，下文简称A–D消融、优化器比较和分带实验。A–D消融与优化器比较共享大部分观测，具体样本关系见第4.3.2节，因此不将二者视为独立重复验证，也不跨方案直接比较成功率。

### 4.1 雷达参数与观测生成

仿真采用X波段单站脉冲多普勒雷达参数，并按照第2章的精确可分离原子，直接在距离–慢时间层合成 \(Y_{\mathrm{sep}}\) 并叠加复高斯白噪声，不生成完整快时间原始回波。载频、带宽、距离窗和脉冲参数仍通过距离PSF、多普勒原子、无模糊区间及Rayleigh标尺进入观测模型。具体配置见表4-1。

| 参数 | 取值 |
| --- | --- |
| 雷达与波形 | 单站脉冲多普勒；\(f_0=10\ \mathrm{GHz}\)，\(\lambda=0.03\ \mathrm m\)；LFM脉宽 \(T_p=10\ \mu\mathrm s\)，带宽 \(B=10\ \mathrm{MHz}\) |
| 快时间与距离PSF | \(F_s=10\ \mathrm{MHz}\)，每脉冲1000点；该快时间序列用于构造采用频域Hamming窗的距离PSF |
| 慢时间配置 | \(T_r=100\ \mu\mathrm s\)，\(N_p=64\)；估计原子采用矩形慢时间窗 |
| 名义Rayleigh尺度 | \(\Delta R_{\mathrm{res}}=15\ \mathrm m\)，\(\Delta v_{\mathrm{res}}=2.34375\ \mathrm{m/s}\) |
| 速度周期 | 无模糊主区间 \([-75,75)\ \mathrm{m/s}\)，周期 \(P_v=150\ \mathrm{m/s}\) |
| 距离ROI | 模型中心为 \(3000\ \mathrm m\)，请求半宽为 \(180\ \mathrm m\)；实际粗距离轴从 \(2835\ \mathrm m\) 延伸至 \(3165\ \mathrm m\)，间隔为 \(15\ \mathrm m\)，共 \(N_R=23\) 点 |
| 观测维数 | \(Y_{\mathrm{sep}}\in\mathbb C^{23\times64}\)，向量化后 \(M=1472\) 个复样本 |
| 距离搜索网格 | 4倍过采样，共92点，步长约 \(3.62637\ \mathrm m\) |
| 速度搜索网格 | 8倍过采样；实验搜索范围限制为 \([-30,30)\ \mathrm{m/s}\)，实际网格覆盖 \([-29.8828125,29.8828125]\ \mathrm{m/s}\)，间隔为 \(0.29296875\ \mathrm{m/s}\)，共205点 |
| 目标个数与候选上限 | 综合消融中，真实目标个数 \(K_{\mathrm{true}}\) 分别取0、1、2和3；五分带实验固定 \(K_{\mathrm{true}}=3\)；估计器均取 \(K_{\max}=5\) |
| 信噪比 | \(20\ \mathrm{dB}\)，按单个目标的平均样本功率与每个复样本的噪声方差之比定义 |

表4-1中的快时间序列仅用于构造发射波形及距离PSF，估计器输入为 \(23\times64\) 的距离–慢时间矩阵。ROI按开区间选取粗距离单元，因此请求的 \(3000\pm180\ \mathrm m\) 范围实际对应 \(3000\pm165\ \mathrm m\)。周期速度差使用物理无模糊周期，而非局部搜索区间的宽度；几何归一化和误差判定采用名义Rayleigh尺度，而非过采样网格步长。

对于 \(K>0\)，各目标取等幅随机相位复系数

\[
\beta_k=\exp(\mathrm j\varphi_k),
\qquad
\varphi_k\sim\mathcal U(0,2\pi),
\]

且不同目标的相位独立。由于各目标的复幅度模长均为1，单个目标的平均样本功率为

\[
P_{\phi}=\frac{1}{M}\|\phi(R,v)\|_2^2,
\]

本文以该功率与每个复样本的噪声方差之比定义参考信噪比：
\[
\mathrm{SNR}_{\mathrm{sample}} =10\log_{10}\!\left(\frac{P_\phi}{\sigma^2}\right) =20\ \mathrm{dB}.
\]
因此，生成噪声时采用
\[
\sigma^2=P_\phi10^{-20/10}.
\]
其中，\(\sigma^2\) 与第2.1节定义的每个复噪声样本的方差一致。上述20 dB按单个目标的平均样本功率定义，不表示多目标叠加后的整幅场景信噪比；后者会随目标几何和随机相位变化。对于 \(K=0\) 的空场景，仍按同一参考原子功率设置噪声方差，以保持空场景与有目标场景的噪声尺度一致。

### 4.2 随机目标几何与样本设置

A–D消融、优化器比较和分带实验的非空场景均采用随机二维目标几何，不预设固定的目标排列。记距离和速度搜索网格步长分别为 \(\delta_R\) 和 \(\delta_v\)，以对应Rayleigh尺度为单位的最大半跨度分别取 \(\eta_R=0.90\) 和 \(\eta_v=0.55\)。为给目标分布及局部精修预留边界余量，在安全矩形内均匀抽取场景中心 \((R_c,v_c)\)。距离和速度方向的边界余量分别为
\[
m_R=\eta_R\Delta R_{\mathrm{res}}+2\delta_R, \qquad m_v=\eta_v\Delta v_{\mathrm{res}}+2\delta_v,
\]
场景中心满足
\[
(R_c,v_c)\sim\mathcal U\!\left( [R_{\min}+m_R,R_{\max}-m_R] \times[v_{\min}+m_v,v_{\max}-m_v] \right).
\]
其中，\([R_{\min},R_{\max}]\) 和 \([v_{\min},v_{\max}]\) 为表4-1所列的距离和速度搜索边界。\(3000\ \mathrm m\) 仅用于确定距离ROI，每个非空场景的中心均在上述安全矩形内重新抽取。

单目标直接置于随机中心。对于多目标场景，先独立生成 \(\mathbf u_k\sim\mathcal U([-1,1]^2)\)，再分别沿距离和速度方向将其边界框中心平移至原点：
\[
\widetilde u_{k,q} =u_{k,q} -\frac{\min_l u_{l,q}+\max_l u_{l,q}}{2}, \qquad q\in\{R,v\}.
\]
各目标的距离和速度由下式确定：
\[
R_k=R_c+\eta_R\Delta R_{\mathrm{res}}\widetilde u_{k,R}, \qquad v_k=v_c+\eta_v\Delta v_{\mathrm{res}}\widetilde u_{k,v}.
\]
中心平移后不将样本缩放至固定跨度，也不设置单轴最小跨度。因此，目标簇的方向、跨度和成对间隔仍具有随机性，\(\eta_R\) 与 \(\eta_v\) 仅限定两个方向允许的最大半跨度。

为描述目标之间的接近程度，定义目标对的归一化二维间隔为
\[
d_{ij} =\sqrt{ \left(\frac{R_i-R_j}{\Delta R_{\mathrm{res}}}\right)^2 + \left(\frac{d_v(v_i,v_j)}{\Delta v_{\mathrm{res}}}\right)^2 },
\]
其中，\(d_v(\cdot,\cdot)\) 为第4.4.1节定义的周期速度差。多目标场景的最小二维间隔为
\[
d_{\min}=\min_{i<j}d_{ij}.
\]
生成器通过拒绝抽样约束 \(d_{\min}\)。A–D消融和优化器比较仅接受 \(d_{\min}\ge0.10\) 的多目标场景；分带实验则接受 \(d_{\min}\) 落入指定区间的三目标场景。这里的0.10是二维归一化最小间隔下限，不同于第4.4.1节用于判定定位成功的0.10 Rayleigh单轴误差门限。

\(d_{\min}\) 仅描述最接近目标对的间隔，不能完全表征场景难度。即使其取值相同，目标排列、其他成对间隔、随机相位、原子相干性和Gram矩阵条件数仍可能不同。

### 4.3 对照实验设计

本文设置三组实验，分别考察精修模块的作用、相同候选初值下的联合优化器差异，以及三目标场景的经验工作边界。此外，利用已保存的候选模型开展事后机制分析，并通过新样本复验和迭代预算诊断补充检验结果。

#### 4.3.1 A–D同观测精修消融

为了分离不同精修模块的作用，本文设置表4-2所示的四个算法变体（分别记为A、B、C、D）。四组只改变新目标的局部精修方式、活动目标集的精修方式和迭代上限门控，其余设置——包括距离–速度原子、粗搜索网格、历史支撑排除、\(K_{\max}\)、复幅度回代、BIC定义、CFAR配置、目标匹配和成功门限——均保持一致。

| 变体 | 新目标局部精修 | 活动目标集精修方式 | 迭代上限门控 | 对比含义 |
| --- | --- | --- | --- | --- |
| A | 直接二维Newton | 逐目标循环Newton并联合LS回代 | 关闭 | 本文实现的NOMP式二维受控基线 |
| B | 单目标VarPro-LM | 逐目标循环Newton并联合LS回代 | 关闭 | 相对A只替换局部精修 |
| C | 单目标VarPro-LM | 联合VarPro-LM并联合LS回代 | 关闭 | 相对B将多目标精修替换为全目标联合精修 |
| D | 单目标VarPro-LM | 联合VarPro-LM并联合LS回代 | 开启 | 相对C检查门控的附加作用 |

A组将原始NOMP的残差网格检测、局部与循环Newton精修及LS幅度更新流程[11]适配到二维距离–速度模型。局部和循环精修均对当前单个目标的距离与速度执行二维Newton更新，其余配置与其他变体一致。因此，A组是本文构建的NOMP式二维受控基线，而非原作者代码或完整系统的复现。

其中，A\(\rightarrow\)B、B\(\rightarrow\)C和C\(\rightarrow\)D分别用于考察单目标VarPro-LM、采用联合VarPro-LM的全目标联合精修和迭代上限门控的增量作用。A\(\rightarrow\)D仅反映完整方法相对该NOMP式二维受控基线的链路级变化，不能将该差异全部归因于某一个模块。

真实目标个数分别取0、1、2和3，每种情形生成500份独立观测，共2000份，供A–D四个变体共同使用。每个变体分别运行全阶BIC和CFAR+BIC，得到4000个“场景–选阶模式”结果。各模式内按真实目标个数和场景编号配对，四个变体使用相同的真实参数、噪声及复观测，模块差异采用同观测配对统计。

四个变体均采用相同的CFAR阈值配置。全阶BIC模式比较零至五阶全部候选模型，CFAR+BIC模式则允许候选生成提前停止；CFAR判定公式见第3.2节，具体阈值参数及校准依据见第4.5.1节。

#### 4.3.2 相同候选初值下的优化器比较

为考察联合优化后端的影响，在各阶候选模型初值相同的条件下，比较适配二维距离–速度原子的QNOMP式联合BFGS与联合VarPro-LM。QNOMP式联合BFGS适配版将QNOMP同时优化全部已检测路径的BFGS思路[24]适配至本文模型，同时保持连续原子、候选生成和BIC选阶规则不变。因此，该方法仅作为优化器层面的受控对照，不等同于QNOMP官方代码或完整系统复现。

候选模型的共同初值统一采用与B组一致的参考流程逐阶生成。在第 \(m\) 阶，参考流程先从残差中检测新目标，并采用单目标VarPro-LM精修其距离和速度。随后，将新目标参数与上一阶精修后的已有目标参数合并，并在执行当前阶循环Newton精修之前，记录该活动集的距离和速度，作为两种联合优化器的共同初值：
\[
\boldsymbol\theta_m^{\mathrm{init}}
= [R_1^{\mathrm{init}},v_1^{\mathrm{init}},\ldots,
R_m^{\mathrm{init}},v_m^{\mathrm{init}}]^{\mathsf T},
\]

参考流程随后按B组规则完成该阶精修、复幅度回代和残差更新，并继续生成下一阶候选。BFGS与VarPro-LM分别读取各阶已记录的共同初值，独立执行全目标联合精修；两者的优化结果均不反馈至参考流程。两者使用同一观测 \(y\)、连续原子、参数边界、网格尺度和单步上限，采用相同的复幅度条件最小二乘消元方式，并优化相同的带岭约化目标函数，其中相对岭系数为 \(10^{-8}\)。精修结束后，两种方法均按照第2.5节的未正则化口径重新估计复幅度，并以相应RSS计算BIC，避免将内部数值正则带入模型选择。

在QNOMP式联合BFGS适配版中，距离和速度参数按各自搜索网格步长无量纲化，Hessian矩阵的逆近似由单位阵初始化，并采用距离块与速度块相互独立的块对角结构。梯度按照变量投影的包络关系计算：在当前距离和速度参数下回代条件最优复幅度，再利用残差、复幅度和原子导数形成各参数方向的梯度。搜索方向由Hessian矩阵的逆近似与梯度确定，步长采用Armijo回溯；仅在满足曲率条件时更新Hessian矩阵的逆近似，并限制单次距离和速度更新幅度。导数设置和线搜索参数见表4-3。

联合VarPro-LM采用第3.4和3.5节所述的投影近似雅可比矩阵与阻尼更新。两种方法共享观测、各阶候选模型初值、目标函数、参数尺度和边界，其差别限定于联合更新机制，包括导数近似、搜索或阻尼策略以及预设计算预算。

该比较对真实目标个数为0、1、2和3的情形各设置500个场景。按表4-3的种子规则，每种情形与A–D消融共享498个观测，合计1992个，占比99.6%，因此两套实验不构成独立样本复验。优化器比较内部仍以2000个场景进行逐场景配对。

实验取 \(K_{\max}=5\)，采用20 dB单目标固定SNR、第4.2节规定的随机目标几何和第4.4.1节定义的定位门限。零至五阶候选模型均参与全阶BIC选阶，不启用CFAR停止或迭代上限门控；两种优化器的计算预算和停止容差见表4-3。第5.2节同时报告准确性指标、目标函数与导数评估次数、同一MATLAB进程内的运行时间、停止原因和求解失败率。

预设的主要分析对象为500个三目标场景，主要评价量为联合VarPro-LM相对QNOMP式联合BFGS适配版的配对端到端成功率差。分析对象、评价量、样本量和失败处理规则均在实验运行前确定。两种优化器按场景配对，置信区间与检验方法按照第4.4.2节执行。达到最大迭代次数的候选保留当前结果并标记为未收敛；若发生求解异常，则将相应方法在该场景中的端到端结果记为失败。

#### 4.3.3 最小间隔分带实验

本实验采用相同的随机目标几何生成方法，仅生成三目标场景，并通过拒绝抽样使最小二维间隔 \(d_{\min}\) 落入指定区间。估计器不使用真实目标个数信息，仍在实际生成的零至五阶候选模型中选阶。

预设的五个最小间隔带依次为 \([0.20,0.25)\)、\([0.25,0.30)\)、\([0.30,0.35)\)、\([0.35,0.40)\) 和 \([0.40,+\infty)\)，单位均为Rayleigh，带编号依次为1—5。各间隔带使用互不重叠的随机数种子序列生成场景，基础种子和带间偏移见表4-3。

每个间隔带包含500个三目标场景，共2500个场景。全阶BIC和CFAR+BIC处理相同观测，且均启用迭代上限门控；前者用于主要边界判定，后者作为同观测次要对照。由于本实验与A–D消融采用不同观测和几何接受区间，两套实验的成功率不作配对比较，也不用于单一模块归因。

#### 4.3.4 事后机制分析与补充验证

为分析候选精修状态对BIC选阶的影响，利用第4.3.2节优化器比较中保存的各阶候选模型记录，开展事后分析。对于每个场景的零至五阶候选模型，“精修前”曲线以共同初始活动集为基础，按照第2.5节的未正则化口径重新估计复幅度并计算RSS与BIC；“精修后”曲线则采用同一活动集经联合VarPro-LM更新后的距离和速度参数，并按相同口径计算RSS与BIC。两条曲线共享观测、候选阶数和各阶初值，区别仅在于BIC评分前是否对每个非空候选模型执行全目标联合精修。该审计只评价选阶变化，不涉及端到端定位，因而仅作为事后机制分析，不作为预设主要比较或系统级基线。

为展示候选精修对RSS与BIC的影响，本文分别从优化器比较和A–D消融的三目标场景中选择代表场景。两幅图均采用事后确定性筛选与排序规则，仅用于机制展示，不增加独立样本量，也不作为总体性能的统计证据。

图5-3从优化器比较中筛选满足以下条件的场景：BFGS选择四阶，VarPro-LM选择三阶且端到端成功；两种方法均无求解器异常，零至五阶候选记录完整，全部非空候选均达到各自求解器的数值收敛条件。零阶空模型直接计算RSS与BIC，不涉及迭代求解。筛选后，以最小二维间隔 \(d_{\min}\) 和BFGS三阶BIC相对于其零至五阶最小BIC的差值作为两项排序指标。

图5-4从A–D消融中筛选满足以下条件的场景：A、B均选择五阶，C、D均选择三阶且端到端成功；四组均已生成真实三阶候选，且无求解器异常。该筛选不要求全部候选达到数值收敛。筛选后，以最小二维间隔 \(d_{\min}\) 和真实目标原子的最大相干性 \(\mu_{\max}\) 作为两项排序指标。

对每幅图，分别在其筛选出的场景集合中选择代表场景。对集合中的每个场景，计算各项指标值与该指标集合中位数之差的绝对值，再除以该指标的中位绝对偏差，分母下限取机器精度。将每个场景的两项归一化偏差相加，选取总和最小的场景；若并列，则选择随机种子较小者。

为检验联合优化器比较结果的重复性，沿用第4.3.2节的实验设置，采用随机种子907232601—907233100另行生成500个三目标场景。两种优化器处理相同观测，并从各阶共同初值出发，采用相同的评价指标与统计方法。复验结果与原实验分别报告。

此外，从原优化器实验的三目标场景中按编号选取前100个场景，随机种子为3171594—3171693，开展迭代预算诊断，选取过程不依据成败结果。在其余设置不变的条件下，将BFGS和LM的迭代上限分别从120和60提高至原值的2倍和4倍，各档预算均从相同公共初值重新求解零至五阶候选模型。该检查复用原实验场景，属于探索性诊断，不作为独立样本复验。

### 4.4 评价指标与统计规则

#### 4.4.1 置换不变匹配与端到端成功

估计目标与真实目标之间没有预先固定的一一对应关系，且速度参数以 \(P_v\) 为周期。为同时处理目标标签置换和速度周期性，首先定义周期速度差

\[
d_v(\widehat v,v)
=\operatorname{mod}\!\left(
\widehat v-v+\frac{P_v}{2},P_v
\right)-\frac{P_v}{2}.
\]

该差值位于半开区间 \([-P_v/2,P_v/2)\)。本文实验的速度模糊周期为 \(P_v=150\ \mathrm{m/s}\)；该周期由物理无模糊速度区间决定，不等于实验搜索区间 \([-30,30)\ \mathrm{m/s}\) 的宽度。

当 \(\widehat K=K_{\mathrm{true}}=K>0\) 时，在 \(K\) 个目标的全部排列组成的集合 \(\mathfrak S_K\) 中，选择使归一化总平方误差最小的匹配：

\[
\pi^\star
=\arg\min_{\pi\in\mathfrak S_K}
\sum_{k=1}^{K}
\left[
\left(
\frac{\widehat R_{\pi(k)}-R_k}
{\Delta R_{\mathrm{res}}}
\right)^2
+
\left(
\frac{d_v(\widehat v_{\pi(k)},v_k)}
{\Delta v_{\mathrm{res}}}
\right)^2
\right].
\]

匹配后的最大归一化距离和速度误差分别定义为

\[
e_R=\max_k
\frac{|\widehat R_{\pi^\star(k)}-R_k|}
{\Delta R_{\mathrm{res}}},
\qquad
e_v=\max_k
\frac{|d_v(\widehat v_{\pi^\star(k)},v_k)|}
{\Delta v_{\mathrm{res}}}.
\]

定义距离和速度两个方向上的最大归一化误差为

\[
e_\infty=\max\{e_R,e_v\}.
\]

上述归一化总平方误差仅用于确定匹配排列 \(\pi^\star\)；定位成功要求 \(e_R\) 和 \(e_v\) 均不超过0.10，该门限分别作用于两个方向的归一化误差。

选阶正确、定位成功和端到端成功的指示量分别定义为

\[
I_{\mathrm{ord}}
=\mathbf1\{\widehat K=K_{\mathrm{true}}\},
\qquad
I_{\mathrm{loc}}
=\mathbf1\{e_\infty\le0.10\}
=\mathbf1\{e_R\le0.10,\ e_v\le0.10\},
\qquad
I_{\mathrm{E2E}}=I_{\mathrm{ord}}I_{\mathrm{loc}}.
\]

由表4-1的 \(\Delta R_{\mathrm{res}}=15\ \mathrm m\) 和 \(\Delta v_{\mathrm{res}}=2.34375\ \mathrm{m/s}\)，0.10 Rayleigh门限分别对应 \(1.5\ \mathrm m\) 和 \(0.234375\ \mathrm{m/s}\)。由于 \(e_R\) 和 \(e_v\) 已对目标取最大值，该条件要求每个匹配目标在两个坐标方向上都满足相应门限。

当 \(\widehat K\ne K_{\mathrm{true}}\) 时，评价中令 \(I_{\mathrm{loc}}=0\) 且 \(e_R=e_v=+\infty\)，该场景直接计为端到端失败，不再根据部分目标的匹配结果判断定位成功。

当 \(\widehat K=K_{\mathrm{true}}=0\) 时，令 \(I_{\mathrm{loc}}=1\) 且 \(e_R=e_v=0\)，因此空场景的端到端成功等价于 \(\widehat K=0\)。此处的零误差仅为评价记账约定，不表示进行了距离–速度参数定位。

对于非空场景，除报告 \(P(I_{\mathrm{loc}}=1\mid\widehat K=K_{\mathrm{true}})\) 外，还报告正确阶数条件下 \(e_R\) 与 \(e_v\) 的中位数，以区分模型选择失败和距离–速度定位失败。

#### 4.4.2 选阶、诊断与配对指标

按真实目标个数分别报告选阶正确率和端到端成功率，并给出 \(\widehat K\) 混淆矩阵、欠估率、过估率和平均估计阶数。欠估和过估分别指 \(\widehat K<K_{\mathrm{true}}\) 与 \(\widehat K>K_{\mathrm{true}}\)。此外，报告真实阶数候选生成率，即候选模型序列中包含 \(K=K_{\mathrm{true}}\) 对应模型的场景比例。

若求解器异常终止、返回非有限输出或无法形成有效估计，则将相应方法在该场景中的端到端结果记为失败，并单独统计求解失败率。Gram矩阵条件数、最大原子相干性、重复目标告警、停止原因，以及迭代上限门控的尝试和接受次数仅用于机制分析，不参与成功判定。

在候选精修状态的机制审计中，定义真实阶数的BIC优势为

\[
\Delta_{\mathrm{true}}
=
\min_{\substack{K\in\mathcal K_{\mathrm{available}}\\
K\ne K_{\mathrm{true}}}}
\operatorname{BIC}(K)
-\operatorname{BIC}(K_{\mathrm{true}}).
\]

当 \(\Delta_{\mathrm{true}}>0\) 时，真实阶数候选模型的BIC低于其余模型；其值越大，真实阶数候选模型相对于其他阶数的BIC优势越明显。该指标仅在真实阶数候选已生成且存在其他可比较阶数时定义。

对于在同一组观测上运行的两个变体 \(X\) 和 \(Y\)，设共有 \(n\) 个配对场景。对任意 \(a,b\in\{0,1\}\)，定义

\[
n_{ab}
:=
\sum_{i=1}^{n}
\mathbf1\left\{
I_{\mathrm{E2E},i}^{X}=a,\,
I_{\mathrm{E2E},i}^{Y}=b
\right\}.
\]

其中，\(n_{00}\) 和 \(n_{11}\) 分别表示两个变体均失败和均成功的场景数，\(n_{01}\) 表示由 \(X\) 失败转为 \(Y\) 成功的改善数，\(n_{10}\) 表示由 \(X\) 成功转为 \(Y\) 失败的退化数。配对端到端成功率差值定义为

\[
\widehat\Delta_{Y-X}
=
\frac{n_{01}-n_{10}}{n},
\qquad
n=n_{00}+n_{01}+n_{10}+n_{11}.
\]

配对成功率差值的95%置信区间采用文献[29]中的Newcombe方法10计算。该方法利用四种配对结果的计数构造得分置信区间。双侧精确McNemar检验则只使用两类不一致配对，即仅 \(X\) 成功或仅 \(Y\) 成功的场景[30]：
\[
p_{\mathrm{McN}}
=\min\!\left\{
1,
2\sum_{j=0}^{\min(n_{01},n_{10})}
\binom{n_{01}+n_{10}}{j}2^{-(n_{01}+n_{10})}
\right\}.
\]

本文以成功率百分点差及其95%置信区间作为主要比较量，并同时报告改善数和退化数；\(p\)值作为辅助证据。A–D消融用于评价精修模块的增量作用，优化器比较以第4.3.2节预设的三目标配对端到端成功率差为主要评价量；CFAR+BIC结果用于补充比较，候选精修审计和代表场景用于机制解释。所报告的区间和\(p\)值均针对单项比较，未作跨比较的多重性调整，不据此声称所有比较同时获得整体统计保证。

四种真实目标个数采用固定等额抽样，总体成功率及配对差均表示四类场景的等权平均。合并计数得到的总体置信区间作为近似汇总；各真实目标个数下的区间与检验分别报告。

#### 4.4.3 比例区间与经验边界

对 \(n\) 个独立场景中的 \(x\) 次成功，记 \(\widehat p=x/n\)，并采用双侧Wilson 95%置信区间描述单一方法成功率的抽样不确定性[31]。令 \(z=z_{0.975}\approx1.96\)，则

\[
[L,U]
=
\frac{
\widehat p+\frac{z^2}{2n}
\pm z\sqrt{
\frac{\widehat p(1-\widehat p)}{n}
+\frac{z^2}{4n^2}}
}{1+\frac{z^2}{n}}.
\]

在五分带实验中，单个间隔带以Wilson 95%置信区间下限 \(L\ge0.95\) 为通过条件；当 \(n=500\) 时，至少需要485次成功。对每个预设间隔带，检查该带及所有目标间隔更大的带是否均通过。将满足这一条件的各间隔带下界取最小值，定义为保守经验工作边界。该边界适用于第4.3.3节规定的实验设置。各带区间分别计算，未作同时覆盖调整，因此不赋予该边界整体95%的覆盖保证。

### 4.5 CFAR配置与数值实现参数

#### 4.5.1 CFAR配置与校准依据

CFAR+BIC模式采用第3.2节的候选扩展检验，取 \(J=K_{\max}=5\)、\(p_{\mathrm{fa}}=10^{-3}\) 和 \(N_{\mathrm{cell}}=2500\)。第 \(m\) 轮检验使用当前残差 \(e_{m-1}\) 估计噪声功率：
\[
\widehat{\sigma}^{2} =\frac{\operatorname{median}_{1\le n\le M} \left\{|[e_{m-1}]_n|^2\right\}}{\log 2}.
\]
由于过采样搜索原子之间存在相关性，\(N_{\mathrm{cell}}\) 采用纯噪声预实验确定的等效独立搜索单元数，而非二维粗搜索网格的总点数 \(G_RG_v\)。

等效搜索单元数的取值依据为纯噪声预实验：距离搜索区域半宽取90 m和180 m，距离与速度过采样因子分别取2、4、8，共形成18组设置。每组采用3个随机种子重复，每次使用200个校准场景和另外200个独立验证场景。在与本文相同的180 m半宽、距离过采样因子4和速度过采样因子8设置下，三次估计的等效单元数介于1802.46与2418.56之间，因此取略高于该范围上限的2500。该预实验采用的候选上限为4，总虚警概率设置为0.1和0.05，与本文 \(K_{\max}=5\)、\(p_{\mathrm{fa}}=10^{-3}\) 的设置不同。因此，该预实验用于确定等效单元数的经验取值，正式配置下的实际虚警率仍需另行验证。

本文的CFAR+BIC模式以CFAR限制候选模型的生成范围，再由BIC确定最终阶数，作为全阶BIC之外的补充方案。该流程不等同于NOMP-CFAR[21]，当前实验未对两者进行性能比较。

#### 4.5.2 数值参数与复现信息

实验在MATLAB R2025b中运行，并使用Signal Processing Toolbox和Statistics and Machine Learning Toolbox。表4-3汇总第5章实验采用的主要数值设置；表中未重复列出的目标函数、边界处理和停止条件见第3章及第4.3节。

| 模块 | 主要预设值 |
| --- | --- |
| 候选与选阶 | \(K_{\max}=5\)；采用全阶BIC或CFAR+BIC；BIC计算采用未正则化复幅度回代，并将RSS的数值下限设为 \(10^{-12}\lVert y\rVert_2^2\) |
| A–D与五分带基础预算 | 新目标局部精修和全目标联合精修的最大迭代数均为10 |
| VarPro-LM正则化与阻尼 | 相对岭系数 \(\rho_{\mathrm{ridge}}=10^{-8}\)，LM阻尼初值为 \(10^{-3}\)；试探步被拒绝后阻尼乘10，被接受后乘0.3，阻尼上限为 \(10^{12}\)；\(\kappa_{\mathrm{ref}}=50\)，条件数放大上限为 \(10^4\)，每轮最多进行12次阻尼试探 |
| VarPro-LM停止与限步 | 投影梯度容差 \(10^{-8}\)，相对目标下降容差 \(10^{-12}\)，距离和速度步长容差分别为 \(10^{-4}\ \mathrm m\) 与 \(10^{-5}\ \mathrm{m/s}\)；单次接受更新不超过相应搜索网格一步 |
| 连续原子导数 | 距离导数采用边界感知的两点有限差分，名义差分步长为距离搜索网格步长的 \(1/8\)；ROI内部采用中心差分，靠近边界时采用合法端点构造的非对称差分；可分离速度原子采用解析导数 |
| QNOMP式联合BFGS适配版 | 距离/速度分块BFGS；Armijo初始步长1、充分下降系数 \(10^{-4}\)、回缩因子0.5、每轮至多20次回溯；曲率容差 \(10^{-10}\) |
| 直接二维Newton | 导数步长为0.125个网格尺度，每轮至多8次回溯，无量纲步长容差为 \(10^{-6}\)；要求局部Hessian矩阵通过数值正定性检查，未通过时保留当前参数并停止本次精修调用，记录相应停止状态，不切换为一阶更新 |
| 重复候选处理 | 距离差与速度差分别按对应搜索网格步长归一化，并计算二维欧氏距离，速度差采用周期最短差值；粗搜索阶段排除归一化距离不大于0.5的网格点，精修后对归一化距离不大于0.25的目标对记录重复告警 |
| CFAR | \(p_{\mathrm{fa}}=10^{-3}\)，\(N_{\mathrm{cell}}=2500\)，采用中位数噪声估计 |
| 迭代上限门控 | 仅对因达到最大迭代数而停止的候选追加LM精修，追加迭代数不超过10；RSS相对改善容差为 \(10^{-12}\)，基线一致性容差为 \(10^{-10}\) |
| 随机数生成（A–D与优化器比较） | 使用MATLAB的twister生成器；基础种子 \(s_0\) 分别为3141592和3141594。真实目标个数为 \(K_{\mathrm{true}}\) 时，第 \(t\) 个场景的种子为 \(s=s_0+10000K_{\mathrm{true}}+t-1\) |
| 随机数生成（五分带实验） | 使用MATLAB的twister生成器；基础种子为3141593，第 \(b\) 个间隔带中第 \(t\) 个场景的种子为 \(s=3141593+100000(b-1)+t-1\) |
| 随机几何拒绝抽样 | A–D消融和优化器比较中，每个场景最多尝试5000次；五分带实验中，每个场景最多尝试20000次 |
| 优化器比较 | 真实目标个数 \(K_{\mathrm{true}}\) 分别取0、1、2和3，每种设置包含500个场景；BFGS和VarPro-LM的最大迭代数分别为120和60；仅采用全阶BIC，不启用CFAR或迭代上限门控 |

所有汇总结果均由逐场景记录复算。记录内容包括配对核验信息、真实参数、估计结果、匹配误差、求解状态、目标函数与导数评估次数以及运行时间。优化器比较还保存各阶公共初值及精修前后的RSS和BIC，用于核对配对关系和开展机制分析。

优化器比较记录了MATLAB版本、系统信息及串行执行标志，第5.2节据此报告同一MATLAB进程内两种求解程序的相对计算代价。

## 5 实验结果

本章依次报告A–D消融、优化器比较、事后机制审计、完整方法的失败分布以及三目标间隔分带实验的结果。除特别说明外，区间估计和配对检验均按照第4.4节执行。三套预设实验均未出现求解器异常。

### 5.1 精修模块消融结果

表5-1给出全阶BIC模式下四个变体的选阶正确率和端到端成功率。表中每个单元格依次报告选阶正确率和端到端成功率，单位为百分比。空场景和单目标场景中四组表现一致，差异主要集中在双目标和三目标场景。A组和B组的等权总体端到端成功率均为57.90%；在B组基础上加入联合VarPro-LM全目标精修后，C组提高至98.35%；进一步开启迭代上限门控后，D组为98.50%。

| 变体 | \(K=0\) | \(K=1\) | \(K=2\) | \(K=3\) | 等权总体 |
| --- | :--: | :--: | :--: | :--: | :--: |
| A | 99.60 / 99.60 | 99.80 / 99.80 | 32.40 / 31.40 | 2.40 / 0.80 | 58.55 / 57.90 |
| B | 99.60 / 99.60 | 99.80 / 99.80 | 32.40 / 31.40 | 2.40 / 0.80 | 58.55 / 57.90 |
| C | 99.60 / 99.60 | 99.80 / 99.80 | 99.80 / 99.20 | 96.00 / 94.80 | 98.80 / 98.35 |
| D | 99.60 / 99.60 | 99.80 / 99.80 | 99.80 / 99.20 | 97.00 / 95.40 | 99.05 / 98.50 |

A组三目标场景的端到端成功率仅为0.80%，主要失败类型为高阶过估计：500个场景中有487个高估目标个数，仅12个选阶正确，其中4个端到端成功。

表5-2所示的CFAR+BIC结果与全阶BIC保持相同的模块排序。A/B组的等权总体端到端成功率为60.85%，较全阶BIC提高2.95个百分点，但仍分别低于C/D组37.70和37.85个百分点。在当前实验条件下，加入CFAR停止缩小了部分性能差距，但未消除缺少全目标联合精修时的端到端损失。

| 变体 | \(K=0\) | \(K=1\) | \(K=2\) | \(K=3\) | 等权总体 |
| --- | :--: | :--: | :--: | :--: | :--: |
| A | 100.00 / 100.00 | 100.00 / 100.00 | 42.40 / 41.00 | 10.60 / 2.40 | 63.25 / 60.85 |
| B | 100.00 / 100.00 | 100.00 / 100.00 | 42.40 / 41.00 | 10.80 / 2.40 | 63.30 / 60.85 |
| C | 100.00 / 100.00 | 100.00 / 100.00 | 99.60 / 99.40 | 96.20 / 94.80 | 98.95 / 98.55 |
| D | 100.00 / 100.00 | 100.00 / 100.00 | 99.60 / 99.40 | 96.80 / 95.40 | 99.10 / 98.70 |

同观测配对结果进一步分解了各模块的增量作用。表5-3中的“改善/退化”分别表示后一个变体成功而前一个失败，以及后一个变体失败而前一个成功的场景数。

| 对比 | 全阶BIC端到端差异 | CFAR+BIC端到端差异 | 配对解释 |
| --- | --- | --- | --- |
| A\(\rightarrow\)B | 0.00个百分点，95%区间 \([-0.10,0.10]\) | 0.00个百分点，95%区间 \([-0.11,0.11]\) | 两种模式均为0次改善、0次退化 |
| B\(\rightarrow\)C | +40.45个百分点，95%区间 \([38.30,42.61]\) | +37.70个百分点，95%区间 \([35.58,39.84]\) | 分别有809和754个场景得到改善，均无退化；精确McNemar \(p<10^{-220}\) |
| C\(\rightarrow\)D | +0.15个百分点，95%区间 \([-0.17,0.50]\) | +0.15个百分点，95%区间 \([-0.17,0.50]\) | 两种模式均为5次改善、2次退化，精确McNemar \(p=0.453\) |
| A\(\rightarrow\)D | +40.60个百分点，95%区间 \([38.45,42.76]\) | +37.85个百分点，95%区间 \([35.73,39.99]\) | 分别有812和757个场景得到改善，均无退化 |

![图5-1 A–D同观测精修消融的总体结果](images/range_doppler_abcd_frozen_ablation_comparison_zh.png)

图5-1集中展示表5-1和表5-2的等权总体结果。四个面板依次对应全阶BIC和CFAR+BIC下的选阶正确率与端到端成功率；每根柱子等权聚合真实目标个数为0、1、2和3的四组结果，每组500个场景，共2000个场景。柱子上的数字为点估计，误差棒为单方法Wilson 95%置信区间。该图用于比较模块排序和效应量级；由于A–D共享观测，模块增量仍应以表5-3的场景级配对差为准，不能根据两组边际置信区间是否重叠进行配对推断。

在全阶BIC和CFAR+BIC两种模式下，A→B均未改变2000个配对场景的端到端判定，成功率差的点估计均为0，且95%置信区间均包含0。相比之下，B→C的端到端成功率分别提高40.45和37.70个百分点，对应809次和754次改善，且均无退化，表明本文采用的全目标联合VarPro-LM精修模块是本组消融中性能提升的主要来源。由于这一替换同时改变了参数更新方式和非线性优化器，当前实验尚不能分离两者各自的贡献，因此上述增益不能单独归因于联合更新方式。

C→D在两种模式下的端到端成功率均仅提高0.15个百分点，95%置信区间包含0，双侧精确McNemar检验 \(p=0.453\)，当前样本未显示确定的独立增益。因此，本文将迭代上限门控视为可选的数值保护措施，而非主要性能来源。

### 5.2 相同候选初值下的联合优化器比较

按照第4.3.2节的设置，本节比较QNOMP式联合BFGS适配版（BFGS组）与联合VarPro-LM（LM组）。两组从各阶共同候选初值出发，采用各自预设的更新策略与迭代预算，因此结果反映联合优化后端的整体差异。

表5-4给出了主要结果。空场景和单目标场景中，两组的端到端判定完全一致；差异从多目标场景开始出现。真实双目标时，LM组的端到端成功率为99.40%，相较BFGS组的87.40%提高12.00个百分点，95%置信区间为 \([9.31,15.16]\) 个百分点；60个场景由失败转为成功，没有出现反向变化。真实三目标时，端到端成功率由56.20%提高至96.60%，成功率差值（LM−BFGS）为40.40个百分点，95%置信区间为 \([36.01,44.74]\) 个百分点；其中203个场景仅LM组成功，1个场景仅BFGS组成功。四种真实目标个数等权汇总后，端到端成功率提高13.10个百分点，95%置信区间为 \([11.67,14.65]\) 个百分点。

| \(K_{\mathrm{true}}\) | BFGS：选阶 / 端到端（%） | VarPro-LM：选阶 / 端到端（%） | LM−BFGS端到端差（百分点，95%区间） | LM改善 / LM退化 |
| --- | :--: | :--: | :--: | :--: |
| 0 | 99.60 / 99.60 | 99.60 / 99.60 | 0.00 \([-0.85,0.85]\) | 0 / 0 |
| 1 | 99.80 / 99.80 | 99.80 / 99.80 | 0.00 \([-0.85,0.85]\) | 0 / 0 |
| 2 | 88.40 / 87.40 | 100.00 / 99.40 | +12.00 \([9.31,15.16]\) | 60 / 0 |
| 3 | 60.60 / 56.20 | 98.20 / 96.60 | +40.40 \([36.01,44.74]\) | 203 / 1 |
| 等权总体 | 87.10 / 85.75 | 99.40 / 98.85 | +13.10 \([11.67,14.65]\) | 263 / 1 |

在预设的主要比较对象——三目标场景中，双侧精确McNemar检验为 \(p=1.59\times10^{-59}\)。作为补充分析，双目标场景与四种真实目标个数等权总体的端到端成功率差，双侧精确McNemar检验分别为 \(p=1.73\times10^{-18}\) 和 \(p=1.79\times10^{-77}\)。等权总体的选阶正确率提高12.30个百分点，95%置信区间为 \([10.85,13.75]\) 个百分点，对应247次改善和1次退化。

阶数混淆结果进一步说明了差异的来源。BFGS组的错误主要表现为高阶过估计：在双目标场景中，有58次将真实二阶估计为三阶或四阶；在三目标场景中，有191次估计为四阶或五阶。相比之下，LM组在500个双目标场景中全部选对阶数；在三目标场景中，491次选对，仅有6次欠估为二阶、3次过估为四阶。三目标场景中，选阶正确率和端到端成功率分别提高37.60个百分点和40.40个百分点。两者增幅接近，说明总体性能提升主要来自高阶过估计的减少。此外，在全部500个三目标场景中，目标个数估计正确、但距离或速度误差超出门限的场景，由BFGS组的22个减少至LM组的8个。

在真实双目标和三目标场景中，BFGS组最终所选候选的数值收敛率分别为83.6%和57.4%；LM组在四种真实目标个数设置下，最终所选候选的数值收敛率均为100%。这里的收敛率按照两种优化器各自采用的数值停止判据统计，不表示所有候选均达到统一的一阶最优性标准。在全部非零阶候选中，BFGS组和LM组分别有1203和378个候选达到迭代上限；此外，LM组还有372个候选因试探步被拒绝而终止且未标记为收敛。上述LM组未标记收敛的候选均未被最终选中。因此，未收敛状态在BFGS组中更频繁地出现在最终所选候选上。

计算代价方面，在同一MATLAB进程中，将四种真实目标个数设置等权汇总后，BFGS组和LM组每个场景的平均联合优化时间分别为0.475 s和0.252 s。相较于BFGS组，LM组的目标函数评估次数和导数评估次数分别减少35.2%和51.8%。分阶结果表明，BFGS组在空场景和单目标场景中用时较少，而LM组在双目标和三目标场景中同时获得了更高的成功率和更低的平均用时。

![图5-2 相同候选初值下QNOMP式联合BFGS适配版与联合VarPro-LM的比较](images/range_doppler_qnomp_style_bfgs_vs_varpro_holdout_comparison_zh.png)

图5-2从四个方面汇总上述结果：图（a）给出端到端成功率，图（b）给出成功率的差值（LM−BFGS），图（c）给出所选候选的收敛率，图（d）给出同一MATLAB进程中的平均联合优化时间。每种真实目标个数包含500个配对场景，横轴中的“总体”表示四种真实目标个数的等权汇总。

按第4.3.4节的规则，优化器比较中共有12个三目标场景满足筛选条件，最终选中随机种子3171740对应的场景，其最小二维间隔为 \(d_{\min}=0.369\) Rayleigh。该场景已包含在表5-4的500个三目标场景中。图5-3展示两种优化器从相同候选初值出发得到的逐阶RSS与BIC，用于解释具体场景中的选阶差异。

![图5-3 同一代表场景下各阶候选模型的RSS与BIC曲线](images/range_doppler_qnomp_style_representative_rss_bic_path_zh.png)

图5-3展示了该场景从零阶到五阶各候选模型的结果。图（a）以 \(K=0\) 的RSS为基准，用 \(10\log_{10}(\mathrm{RSS}_K/\mathrm{RSS}_0)\) 表示相对RSS；灰色虚线表示两组共享的初始候选，蓝色和橙色曲线分别表示BFGS组和LM组精修后的结果。图（b）将每一阶BIC减去该方法在零至五阶中的最小BIC，即 \(\Delta\mathrm{BIC}_m(K)=\mathrm{BIC}_m(K)-\min_{0\leq j\leq5}\mathrm{BIC}_m(j)\)，并以对数坐标绘制 \(1+\Delta\mathrm{BIC}_m(K)\)；星形表示最终选择，竖虚线表示真实阶数 \(K=3\)。

该场景中，两种方法的10个非空候选均满足各自求解器的数值收敛判据，未出现求解异常或迭代上限终止；两个零阶候选直接计算RSS和BIC。各自满足停止判据并不意味着两种方法达到相同的优化精度。在真实三阶处，BFGS组和LM组的精修后RSS分别为 \(1.3509\times10^{-2}\) 和 \(1.0326\times10^{-2}\)，后者降低23.56%。BFGS组三阶候选的BIC仍比其四阶最小值高750.58，因此最终选择四阶；LM组则在三阶达到路径最小BIC，其四阶和五阶BIC分别高23.96和50.03，因此选择真实三阶。该个例展示了一种可能的RSS—BIC传递路径，总体差异仍应以表5-4的配对结果为准。

在新增的500个三目标场景中，BFGS组与LM组的端到端成功率分别为56.00%和95.20%，配对差为39.20个百分点，95%置信区间为 \([34.79,43.54]\) 个百分点。该结果与原三目标实验的差异方向和量级一致，支持优化器性能差异在相同实验条件下的重复性。

在原实验100个三目标场景的预算诊断中，将迭代上限提高至原值的2倍和4倍后，两种方法的逐场景端到端成败判定均未改变。在这一诊断范围内，增加迭代预算未带来端到端成功率改善。复验与预算诊断的设置见第4.3.4节。

综上所述，在相同候选初值、预设计算预算和全阶BIC条件下，LM组相对BFGS组的优势主要出现在双目标和三目标场景。该结果反映本文两种联合优化后端的差异，比较范围见第6.2节。

### 5.3 候选精修对RSS与BIC选阶的影响

全阶BIC模式下，A–D四组的真实阶数候选生成率均为100%，因此A/B在多目标场景中的低成功率不能归因于“没有生成真实阶数候选模型”。各组最终选择的目标个数分布存在明显差异。以未采用全目标联合精修的B组为例，真实 \(K=2\) 时平均估计阶数为3.044：500个场景中仅162个选择 \(\widehat K=2\)，其余分别有165、162和11个选择三、四和五阶。切换到C组后，平均估计阶数降至2.002，其中499/500选择真实二阶。

在三目标场景中，这一差异更为明显。B组的平均估计阶数为4.766，仅12/500个场景选择真实三阶；其余场景中，487个发生高阶过估计，其中90个选择四阶、397个选择五阶，另有1个低估为二阶。C组的平均估计阶数降至3.008，其中480/500个场景选择真实三阶；其余20个场景中，9个选择二阶、9个选择四阶、2个选择五阶。因此，三目标场景中的高阶过估计由B组的487次降至C组的11次。

B\(\rightarrow\)C的直接改动是将活动目标集的精修方式由逐目标循环Newton替换为联合VarPro-LM。由于精修结果会继续影响后续候选模型的生成，该比较反映的是全目标联合精修在完整候选生成流程中的模块级作用，而不等同于一次共享初值的优化器比较。高阶过估计的减少与以下解释一致：联合精修改善了真实阶数候选模型的拟合，从而减少了通过增加额外原子来补偿残差的情况。

表5-5报告第4.3.4节所述的事后机制审计结果。“改善”和“退化”分别表示精修后由选阶错误转为正确，以及由正确转为错误。该分析只考察候选精修状态与BIC选阶的关系，不评价端到端定位。

| \(K_{\mathrm{true}}\) | 精修前选阶正确率 | 精修后选阶正确率 | 精修前/后过估率 | 改善/退化 |
| :--- | :--: | :--: | :--: | :--: |
| 0 | 99.60% | 99.60% | 0.40% / 0.40% | 0 / 0 |
| 1 | 99.80% | 99.80% | 0.20% / 0.20% | 0 / 0 |
| 2 | 0.00% | 100.00% | 100.00% / 0.00% | 500 / 0 |
| 3 | 0.40% | 98.20% | 99.60% / 0.60% | 490 / 1 |
| 等权汇总 | 49.95% | 99.40% | 50.05% / 0.30% | 990 / 1 |

空场景和单目标场景的选阶结果在精修前后均未发生变化，差异集中在多目标场景。双目标和三目标的选阶正确率分别由0.00%和0.40%提高至100.00%和98.20%。按四种真实目标个数等权汇总，选阶正确率提高49.45个百分点，95%置信区间为 \([47.25,51.65]\) 个百分点。选阶正确性的配对比较中，共有990次改善和1次退化，双侧精确McNemar检验为 \(p=9.48\times10^{-296}\)。总体过估率由50.05%降至0.30%。

按照第4.4.2节定义的真实阶数BIC优势 \(\Delta_{\mathrm{true}}\)，双目标和三目标场景的中位数分别由 \(-5705.29\) 和 \(-3467.76\) 变为21.05和23.66，相应真实阶数候选模型的中位RSS分别降低85.77%和70.79%。上述变化表明，在这组固定初始候选模型中，联合精修改善了多目标真实阶数候选模型的拟合，并使BIC最小点由高阶模型移向真实阶数。

该审计复用优化器比较的记录，且“精修前”公共活动集不等同于NOMP基线完成循环精修后的输出。因此，其结果仅用于解释候选精修状态与BIC选阶之间的关系。

按第4.3.4节的规则，A–D消融中共有385个三目标场景满足筛选条件，最终选中随机种子3172044对应的场景，其最小二维间隔为 \(d_{\min}=0.444\) Rayleigh，真实原子的最大相干性为 \(\mu_{\max}=0.840\)。图5-4展示该场景下四个变体的逐阶RSS与BIC，用于说明不同精修模块对候选模型拟合和最终选阶的影响。

![图5-4 A–D同观测代表场景下各阶候选模型的RSS与BIC曲线](images/range_doppler_abcd_representative_rss_bic_path_zh.png)

图5-4展示A–D四组在同一观测上各阶候选模型的RSS与BIC变化。图（a）仍以 \(10\log_{10}(\mathrm{RSS}_K/\mathrm{RSS}_0)\) 表示相对RSS；图（b）使用 \(\Delta\mathrm{BIC}_m(K)=\mathrm{BIC}_m(K)-\min_j\mathrm{BIC}_m(j)\)，并以对数坐标绘制 \(1+\Delta\mathrm{BIC}_m(K)\)。星形表示各组最终选择，竖虚线为真实阶数。与图5-3的优化器比较不同，A–D只共享观测、粗搜索设置、计算预算和BIC定义；各分支按自身精修结果继续生成后续候选模型。因此，图5-4解释的是完整精修模块对候选生成过程的影响，而不是一次相同初值优化调用的差异。

该场景中，A与B的逐阶曲线几乎重合，B在真实三阶处的RSS甚至比A高0.17%，二者均在五阶时取得自身最小BIC，说明只替换新目标的局部精修没有改变此次过估计。由B切换到C后，三阶RSS从 \(1.2912\times10^{-2}\) 降至 \(9.8267\times10^{-3}\)，降低23.90%；四阶和五阶RSS则仅分别降低4.65%和1.23%。因此联合精修的收益集中在原本拟合不足的真实三阶候选：A/B的三阶BIC分别比各自五阶最小值高735.54和740.29，而C在三阶达到路径最小BIC，其四阶和五阶BIC反而分别高13.57和27.16，最终选阶由5回到3。

在预设的10次最大迭代预算下，该场景中A/B的二至五阶候选模型均因达到最大迭代数而停止，C/D的五个非空候选模型则按步长或稳定性条件收敛；这反映两类精修后端在相同名义预算下的收敛差异，而非求解异常。D组没有接受额外的迭代上限门控精修，因此C与D的曲线完全重合，也与C\(\rightarrow\)D总体增益很小的结果一致。

综合真实阶数候选生成率、B/C选阶分布和事后审计结果，多目标失败并非主要源于真实阶数候选模型缺失，而主要表现为该模型精修后的RSS仍不足以在BIC比较中胜出。代表场景仅用于展示这一现象在各阶候选模型中的具体表现，模块效应仍以表5-3的配对结果为准，进一步讨论见第6.1节。

### 5.4 完整方法的分阶性能与剩余失败

表5-6给出完整方法D在全阶BIC模式下的分阶结果。空场景、单目标和双目标的端到端成功率分别为99.6%、99.8%和99.2%；三目标场景降至95.4%，Wilson 95%置信区间为 \([93.19\%,96.92\%]\)。因此，当前设置下的剩余失败主要集中在三目标场景。

| \(K_{\mathrm{true}}\) | 选阶正确率 | 端到端成功率 | 端到端Wilson 95%置信区间 | 正确阶数条件定位率 | 正确阶数条件下中位 \(e_R\) | 正确阶数条件下中位 \(e_v\) |
| ---: | :--: | :--: | :-: | :--: | :--: | :--: |
| 0 | 99.60% | 99.60% | [98.55%, 99.89%] | 100.00% | 0 | 0 |
| 1 | 99.80% | 99.80% | [98.88%, 99.96%] | 100.00% | 0.00134 | 0.00068 |
| 2 | 99.80% | 99.20% | [97.96%, 99.69%] | 99.40% | 0.00318 | 0.00188 |
| 3 | 97.00% | 95.40% | [93.19%, 96.92%] | 98.35% | 0.00736 | 0.00507 |

表中“正确阶数条件定位率”表示：在选阶正确的场景中，距离和速度估计同时满足第4.4.1节定位门限的场景所占比例。空场景的条件定位率与零误差为第4.4.1节规定的记账方式。在500个三目标场景中，485个场景选阶正确，477个场景端到端成功。23次端到端失败包括15次选阶错误和8次正确阶数条件下的定位失败。因此，选阶错误构成三目标剩余失败的主要部分，但距离–速度定位仍贡献了部分失败。

![图5-5 完整方法D在不同真实目标个数下的全阶BIC结果](images/range_doppler_full_varpro_bic_by_k_performance_zh.png)

图5-5汇总上述分阶结果。图（a）给出选阶正确率和端到端成功率及其Wilson 95%置信区间，每种真实目标个数包含500个场景；图（b）给出选阶正确条件下最大距离误差和最大速度误差的中位数，均以对应Rayleigh尺度归一化。空场景误差只具有记账意义，因此图（b）仅绘制真实目标个数为1、2和3的三种情形。随着真实目标个数增加，条件中位误差逐步增大，但三目标时仍分别只有0.00736和0.00507 Rayleigh；相较之下，端到端成功率的下降更多来自选阶错误。

表5-7比较A组与完整D组的BIC阶数混淆计数。两组在空场景和单目标场景中几乎相同；在双目标和三目标场景中，A组错误主要向高阶模型偏移，而D组的估计集中回真实阶数附近。

| 变体 | \(K_{\mathrm{true}}\) | \(\widehat K=0\) | \(\widehat K=1\) | \(\widehat K=2\) | \(\widehat K=3\) | \(\widehat K=4\) | \(\widehat K=5\) |
| --- | :--: | :--: | :--: | :--: | :--: | :--: | :--: |
| A | 0 | 498 | 2 | 0 | 0 | 0 | 0 |
| A | 1 | 0 | 499 | 1 | 0 | 0 | 0 |
| A | 2 | 0 | 0 | 162 | 164 | 162 | 12 |
| A | 3 | 0 | 0 | 1 | 12 | 88 | 399 |
| D | 0 | 498 | 2 | 0 | 0 | 0 | 0 |
| D | 1 | 0 | 499 | 1 | 0 | 0 | 0 |
| D | 2 | 0 | 0 | 499 | 1 | 0 | 0 |
| D | 3 | 0 | 0 | 9 | 485 | 5 | 1 |

![图5-6 NOMP式二维受控基线A组与完整D组的BIC目标个数估计混淆矩阵](images/range_doppler_ad_bic_order_confusion_zh.png)

图5-6将表5-7可视化。两幅矩阵使用相同的0–100%行比例色标，每个单元格同时标注场景数和该真实目标个数内的比例，每行均含500个场景。A组在真实双目标时仅32.4%选择二阶，其余主要分散到三、四阶；在真实三目标时79.8%选择五阶、17.6%选择四阶。D组则分别有99.8%和97.0%的场景集中在真实二、三阶，表明完整链路大幅减少了多目标条件下向高阶模型偏移的错误。

同一批观测中，双目标与三目标场景的平均真实原子最大相干性分别为0.580和0.809，平均最小二维间隔分别为0.785和0.450 Rayleigh。三目标场景具有更高的原子相关性和更紧密的目标几何，但两组比较尚未分离各几何因素的影响。下一节在独立三目标样本上按 \(d_{\min}\) 分带评价。

### 5.5 最小间隔分带与经验工作边界

表5-8给出固定真实目标个数为3、但估计器仍需自行确定目标个数时的全阶BIC结果。在五个预设间隔带中，端到端成功率点估计随间隔带增大而依次上升。前三个间隔带的点估计分别为93.2%、94.8%和96.2%，但Wilson 95%置信区间下限均未达到预设的95%标准。从 \([0.35,0.40)\) 间隔带开始，下限达到95.36%；更高间隔带的下限为98.55%。

| 最小二维间隔带 | 成功数 | 端到端成功率 | Wilson 95%置信区间 | 下限是否达到95% |
| --- | :--: | :--: | :-: | :---: |
| \([0.20,0.25)\) | 466/500 | 93.2% | [90.65%, 95.09%] | 否 |
| \([0.25,0.30)\) | 474/500 | 94.8% | [92.49%, 96.43%] | 否 |
| \([0.30,0.35)\) | 481/500 | 96.2% | [94.14%, 97.55%] | 否 |
| \([0.35,0.40)\) | 486/500 | 97.2% | [95.36%, 98.32%] | 是 |
| \([0.40,+\infty)\) | 498/500 | 99.6% | [98.55%, 99.89%] | 是 |

![图5-7 三目标随机几何五分带的端到端成功率与经验边界](images/range_doppler_k3_gap_boundary_paper_zh.png)

图5-7比较全阶BIC和CFAR+BIC在五个间隔带上的端到端成功率。两种模式均在 \([0.35,0.40)\) 及更高间隔带满足第4.4.3节规定的通过条件，因此得到相同的0.35 Rayleigh保守经验工作边界。CFAR+BIC在五个间隔带中的成功数依次为461、470、484、485和500，与全阶BIC呈现相同的“前三带未通过、后两带通过”结构。

因此，0.35 Rayleigh是本次三目标实验在预设分带网格上得到的保守经验工作边界，其适用条件与局限见第6.3节。

## 6 讨论与局限性

本章结合第5章结果，讨论候选优化误差对BIC选阶的影响、本文方法与相关工作的关系，以及结论的适用范围。

### 6.1 候选优化误差与跨阶RSS/BIC可比性

BIC包含复杂度惩罚，其数据拟合项由各阶候选模型精修后的RSS决定[22–23]。为说明优化误差的传递作用，可将阶数为 \(K\) 的实际候选模型的RSS概念性地写成

\[
\operatorname{RSS}_K
=\operatorname{RSS}_K^{\star}+\delta_K,
\qquad \delta_K\geq0,
\]

其中，\(\operatorname{RSS}_K^\star\) 为第2.4节定义的 \(K\) 阶模型RSS下确界，作为理想拟合参照；\(\delta_K\) 表示实际候选RSS与该下确界的差值，反映初始化、有限迭代预算及局部求解带来的优化误差。在两阶候选均未触发RSS数值下限时，对于满足 \(K+q\leq K_{\mathrm{cand}}\) 的整数 \(q\geq1\)，高阶候选与 \(K\) 阶候选之间的BIC差可写为

\[
\operatorname{BIC}(K+q)-\operatorname{BIC}(K)
=2M\log\!\left(
\frac{\operatorname{RSS}_{K+q}^{\star}+\delta_{K+q}}
{\operatorname{RSS}_K^{\star}+\delta_K}
\right)
+4q\log M.
\]

由此可得，高阶候选的BIC低于 \(K\) 阶候选的充要条件为

\[
\operatorname{BIC}(K+q)<\operatorname{BIC}(K)
\quad\Longleftrightarrow\quad
\frac{\operatorname{RSS}_{K+q}}
{\operatorname{RSS}_K}
<M^{-2q/M}.
\]

在本文 \(M=1472\) 的观测设置下，增加一个目标对应的复杂度惩罚为 \(4\log M\approx29.18\)。此时，只要 \(K+1\) 阶候选相对于 \(K\) 阶候选的RSS下降超过约0.986%，其BIC便更低。因此，真实阶数候选因优化不足留下的残差即使占比不大，也可能使新增原子带来的拟合改善抵偿复杂度惩罚。上述百分比由既有BIC公式计算得到，仅描述两个候选之间的排序条件；最终阶数仍由全部可用候选的BIC共同确定。

上述关系说明，BIC随模型阶数的变化不仅反映阶数差异，也会包含各阶优化误差的相对大小。若真实阶数候选模型的 \(\delta_K\) 偏大，而更高阶候选模型通过新增原子获得更小的相对优化误差，则对数项会向有利于高阶模型的方向移动，并可能抵消 \(4q\log M\) 的复杂度惩罚。对每个非空候选模型采用同一类全目标联合精修并不能保证 \(\delta_K=0\)，也不能保证各阶具有相同的优化误差。其设计目的在于尽量降低由精修方式或精修程度不一致引入的RSS偏差，从而提高跨阶比较的一致性。

多目标场景可能放大上述问题。距离–速度原子之间的相关性使距离和速度参数与复幅度相互耦合；新增目标后，条件最优复幅度和已有目标的最优距离和速度参数都可能随之改变。原始NOMP的逐目标循环Newton精修能够形成检测后的反馈[11]，但仍按目标依次更新。若有限循环后真实阶数候选模型保留结构化残差，后续匹配步骤便可能用额外原子吸收本可由已有目标协同调整解释的能量。联合VarPro-LM同时更新当前候选模型中全部目标的距离和速度，并在每个试探位置重新求解条件最优复幅度，使幅度估计随距离和速度参数的变化而更新[25–28]。

第5.4节的结果显示，相较于双目标场景，三目标场景中真实目标原子的最大相干性均值更高，最小二维间隔均值更低，端到端成功率也更低。这与相邻目标回波相互干扰可能增加估计难度的解释相符，但由于两组场景未单独控制目标间隔或原子相干性，不能据此确定任一因素对性能下降的因果作用。

第5.1—5.3节的三类证据从不同层面支持上述解释：A–D消融表明，多目标失败并非主要源于真实阶数候选模型缺失，性能变化主要出现在引入全目标联合精修之后；优化器比较在固定各阶初值和BIC口径后，将差异限定在联合优化后端；事后审计则显示，候选精修状态能够改变真实阶数候选模型在BIC曲线中的相对位置。综合来看，全目标联合精修在当前实验条件下改善了候选模型的拟合及跨阶BIC比较，并减少了高阶过估计。

迭代上限门控只为达到最大迭代次数的候选提供追加精修机会。追加精修可能通过改变各阶候选的BIC评分影响目标个数选择，也可能在所选目标个数不变时改变距离和速度估计，进而影响端到端判定。RSS下降并不保证参数误差同步减小。结合第5.1节的配对结果，当前样本未显示该门控具有确定的独立增益，因此本文将其定位为可选的数值保护，而非主要性能来源。

### 6.2 本文方法与现有路线的关系及比较边界

原始NOMP已包含CFAR与BIC定阶，并通过局部和循环Newton形成检测—反馈闭环[11]；二维或多维NOMP也已用于波达方向估计、声学波束形成、频率捷变雷达、稀疏OFDM距离–速度估计、相控阵雷达多参数估计和水下弱目标时延–多普勒估计[14–20]。QNOMP则采用BFGS联合优化全部已检测路径，并以线性最小二乘消去复增益[24]。因此，BIC、二维NOMP和全路径联合优化均不是本文首次提出的单项技术。

本文关注的是：在准静态、目标个数未知的二维距离–速度估计任务中，将联合VarPro-LM嵌入NOMP的逐轮候选生成过程，每轮精修当前全部目标后保存相应候选模型，再以统一的未正则化RSS/BIC完成跨阶选择，并通过受控实验检验候选精修程度与高阶过估计之间的关系。

表6-1概括各近邻路线与本文数值证据的关系。

| 方法或路线 | 参数精修与定阶方式 | 本文中的角色与解释边界 |
| --- | --- | --- |
| 原始NOMP [11] | 局部/循环Newton与LS幅度更新；CFAR或增量式BIC停止 | A组为本文基于相同二维距离–速度原子模型实现的NOMP式二维受控基线，用于完整链路对照；不是原作者官方代码 |
| QNOMP [24] | 每轮对全部已检测路径进行联合BFGS与LS增益消元；CFAR确定路径数，最终再联合优化 | 本文只适配其联合BFGS思想，并在相同候选初值下作优化器比较；不能据此声称优于QNOMP官方完整系统 |
| NOMP-CFAR [21] | NOMP初始化候选分量集合；根据CFAR检验逐次删减分量并重新精修，再对残差进行CFAR检测，按需加入新分量，迭代确定目标个数 | 作为目标个数未知时的检测型替代路线讨论；本文CFAR+BIC不是其复现，当前未进行两者的直接数值比较 |
| RELAX [12] | 给定阶数下的循环非线性最小二乘/最大似然精修，目标个数未知时需外接定阶规则 | 作为历史和方法学近邻讨论，不作当前直接数值基线 |
| VALSE与离网稀疏贝叶斯学习（SBL）[9–10] | 依赖概率模型、先验和变分/贝叶斯推断联合处理连续参数与阶数 | 用于说明目标个数未知时的贝叶斯路线；其目标函数和定阶机制与本文不同 |
| 原子范数 [6–8] | 连续域凸松弛及相应理论条件 | 用于说明连续超分辨的凸优化路线；当前二维实验条件下未作系统级适配 |
| 本文 | 对各非空候选模型采用联合VarPro-LM，并通过全阶BIC统一选阶 | 研究候选优化一致性、跨阶RSS可比性及其对高阶过估计的影响 |

本文数值证据涵盖两个比较层次：A–D消融评价精修模块在完整候选生成流程中的作用，相同候选初值实验评价联合优化后端的差异。表6-1中的其他路线未纳入同条件数值比较，现有结果支持所设条件下的模块与优化器结论，尚不足以判断不同完整系统之间的性能排序。

### 6.3 适用范围与局限性

当前观测限定为单个CPI内目标准静态、距离与速度响应精确可分离，且噪声服从复高斯白噪声。该模型未描述CPI内距离走动、宽带时延—多普勒耦合、完整物理LFM回波原子、相关噪声、杂波或扩展目标，因此本文结论不能直接外推到这些模型失配或真实雷达观测。

实验覆盖有限：当前蒙特卡洛实验仅考察固定雷达参数、20 dB单目标固定SNR、等幅随机相位、真实目标个数不超过3且最大候选阶数为5的情形。第5.5节得到的0.35 Rayleigh边界仅针对三目标随机几何，并依赖SNR、目标动态范围、场景分布、成功门限和求解器配置。该数值是有限条件下的经验边界，不是解析Rayleigh分辨极限，也不能回答弱目标遮蔽、更低SNR、更多目标或其他雷达参数下的性能变化。

联合VarPro-LM是依赖初值的局部非线性优化方法，数值停止条件不提供全局最优保证；原子高度相关或初值不利时，仍可能出现选阶错误或定位失败。实验采用固定相对岭系数 \(\rho_{\mathrm{ridge}}=10^{-8}\)，其对参数估计及BIC选阶结果的敏感性尚待评估。此外，经典BIC的渐近解释依赖局部正则性与参数可辨识性[22–23]，当前有限样本实验不足以确立非正则条件下的选阶一致性。

A–D消融与优化器比较共享大部分观测，其结果不构成相互独立的重复验证。第5.2节新增的500个三目标场景提供了相同实验条件下的复验证据；精修前后审计及代表场景则复用既有记录，主要用于机制分析。

计算与复现方面，生成全部阶数的候选模型需要重复执行联合精修，因而增加了计算负担。第5.2节的运行时间仅反映两种求解程序在同一MATLAB进程中的相对计算代价；早期A–D实验又未完整保存运行硬件和源码版本信息。因此，本文不主张普遍的计算效率优势，相关代价应与目标个数未知条件下的选阶收益共同评价。

本文结论的适用范围由上述模型假设、实验配置及比较条件共同限定。

## 7 结论与展望

### 7.1 主要结论

本文研究准静态、精确可分离二维距离–速度模型下目标个数未知时的距离和速度参数估计问题。为减小候选精修不足对BIC选阶的影响，在逐轮生成候选模型时采用联合VarPro-LM更新全部已检测目标的距离和速度，并保存各阶候选模型。候选生成结束后，通过未正则化最小二乘重新估计复幅度，计算各阶RSS与BIC，并选择BIC最小的模型。

同观测A–D消融表明，完整方法D相对NOMP式二维受控基线A组的等权总体端到端成功率由57.90%提高至98.50%，主要增益来自本文采用的全目标联合VarPro-LM精修模块，其参数更新方式与非线性优化器各自的贡献尚未分离。单目标局部精修替换和迭代上限门控的附加作用较小。逐候选事后审计进一步显示，候选精修状态能够改变BIC最小点并减少高阶过估计。

在相同候选初值和BIC口径下，联合VarPro-LM在三目标场景中的端到端成功率为96.60%，QNOMP式联合BFGS适配版为56.20%，配对差为40.40个百分点。该结果只支持当前受控设置下的优化器级比较。

在当前雷达参数、20 dB单目标固定SNR、等幅随机相位、真实目标个数不超过3且最高候选阶数为5的条件下，0.35 Rayleigh是三目标分带实验中通过预设可靠性判据的最小分带下界，而非解析分辨极限。总体而言，在本文实验范围内，对各阶候选模型实施一致的全目标联合精修，是改善目标个数估计和端到端估计可靠性的关键环节。

### 7.2 后续工作

后续可在现有可分离二维模型下，检验不同信号与场景参数对估计性能的影响。可在预设分析方案下考察SNR、目标幅度动态范围、真实目标个数、\(K_{\max}\)、雷达参数和几何分布的敏感性，并通过独立重复实验检验模块效应与0.35 Rayleigh保守经验工作边界的稳定性。同时，应完整记录处理器、内存、MATLAB版本、并行配置以及目标函数和导数评估次数，以获得可复现的计算代价比较。

现有自由复幅度模型可用于考察不等幅目标，后续实验应明确目标幅度动态范围、参考SNR及噪声功率设置。

对于完整物理LFM回波、CPI内距离走动、时延—多普勒耦合、着色噪声和杂波，应分别检查连续原子与噪声似然的适用性，按需修正，并核对参数自由度和成功判据。在此基础上，再通过仿真失配场景及真实雷达数据检验候选一致精修对跨阶可比性的作用是否仍然成立。

若研究进一步扩展到系统级方法比较，则应在同一二维观测、相同参数范围和明确计算预算下适配NOMP-CFAR，并为RELAX、VALSE/SBL和原子范数方法分别预设先验、正则化与停止规则。理论方面还可分析初始化误差、原子相干性和Gram矩阵条件数对局部收敛域的影响，以及非正则近重合条件下BIC选阶的适用性。

## 参考文献

[1] RICHARDS M A. Fundamentals of Radar Signal Processing[M]. 3rd ed. New York: McGraw Hill, 2022.

[2] HERMAN M A, STROHMER T. High-Resolution Radar via Compressed Sensing[J]. IEEE Transactions on Signal Processing, 2009, 57(6): 2275-2284. DOI: 10.1109/TSP.2009.2014277.

[3] LIU Z, QUAN Y, WU Y, et al. Super-Resolution Range and Velocity Estimations for SFA-OFDM Radar[J]. Remote Sensing, 2022, 14(2): 278. DOI: 10.3390/rs14020278.

[4] WEN D, YI H, ZHANG W, et al. 2D-Unitary ESPRIT Based Multi-Target Joint Range and Velocity Estimation Algorithm for FMCW Radar[J]. Applied Sciences, 2023, 13(18): 10448. DOI: 10.3390/app131810448.

[5] CHI Y, SCHARF L L, PEZESHKI A, et al. Sensitivity to Basis Mismatch in Compressed Sensing[J]. IEEE Transactions on Signal Processing, 2011, 59(5): 2182-2195. DOI: 10.1109/TSP.2011.2112650.

[6] TANG G, BHASKAR B N, SHAH P, et al. Compressed Sensing Off the Grid[J]. IEEE Transactions on Information Theory, 2013, 59(11): 7465-7490. DOI: 10.1109/TIT.2013.2277451.

[7] CHI Y, FERREIRA DA COSTA M. Harnessing Sparsity Over the Continuum: Atomic Norm Minimization for Superresolution[J]. IEEE Signal Processing Magazine, 2020, 37(2): 39-57. DOI: 10.1109/MSP.2019.2962209.

[8] CANDÈS E J, FERNANDEZ-GRANDA C. Towards a Mathematical Theory of Super-Resolution[J]. Communications on Pure and Applied Mathematics, 2014, 67(6): 906-956. DOI: 10.1002/cpa.21455.

[9] YANG Z, XIE L, ZHANG C. Off-Grid Direction of Arrival Estimation Using Sparse Bayesian Inference[J]. IEEE Transactions on Signal Processing, 2013, 61(1): 38-43. DOI: 10.1109/TSP.2012.2222378.

[10] BADIU M A, HANSEN T L, FLEURY B H. Variational Bayesian Inference of Line Spectra[J]. IEEE Transactions on Signal Processing, 2017, 65(9): 2247-2261. DOI: 10.1109/TSP.2017.2655489.

[11] MAMANDIPOOR B, RAMASAMY D, MADHOW U. Newtonized Orthogonal Matching Pursuit: Frequency Estimation Over the Continuum[J]. IEEE Transactions on Signal Processing, 2016, 64(19): 5066-5081. DOI: 10.1109/TSP.2016.2580523.

[12] LI J, STOICA P. Efficient Mixed-Spectrum Estimation with Applications to Target Feature Extraction[J]. IEEE Transactions on Signal Processing, 1996, 44(2): 281-295. DOI: 10.1109/78.485924.

[13] ZHU J, HAN L, BLUM R S, et al. Multi-Snapshot Newtonized Orthogonal Matching Pursuit for Line Spectrum Estimation with Multiple Measurement Vectors[J]. Signal Processing, 2019, 165: 175-185. DOI: 10.1016/j.sigpro.2019.07.012.

[14] HAN L, LIU X, ZHANG N, et al. Two-Dimensional Multi-Snapshot Newtonized Orthogonal Matching Pursuit for DOA Estimation[J]. Digital Signal Processing, 2022, 121: 103313. DOI: 10.1016/j.dsp.2021.103313.

[15] YANG Y, CHU Z, YANG Y, et al. Two-Dimensional Newtonized Orthogonal Matching Pursuit Compressive Beamforming[J]. The Journal of the Acoustical Society of America, 2020, 148(3): 1337-1348. DOI: 10.1121/10.0001919.

[16] ZHU J, GUO H, ZHANG N, et al. Newtonalized Orthogonal Matching Pursuit for the Linear Frequency-Modulated Pulse Frequency Agile Radar[J]. IET Radar, Sonar & Navigation, 2021, 15(12): 1670-1686. DOI: 10.1049/rsn2.12156.

[17] SHAH S N H, SEMPER S, KHAN A U, et al. Newtonized Orthogonal Matching Pursuit for High-Resolution Target Detection in Sparse OFDM ISAC Systems[J]. IEEE Transactions on Vehicular Technology, 2025, 74(10): 16137-16151. DOI: 10.1109/TVT.2025.3573165.

[18] HAN L, SHENTU K, WANG Z, et al. The Benefits of Cyclic Refinement Steps of Two-Dimensional Multi-Snapshot Newtonized Orthogonal Matching Pursuit Algorithm[C]//2024 IEEE 7th Information Technology, Networking, Electronic and Automation Control Conference (ITNEC). Chongqing: IEEE, 2024: 601-605. DOI: 10.1109/ITNEC60942.2024.10733104.

[19] ZHANG H, GUO R, WANG Z, et al. A Multi-Dimensional Newtonized Orthogonal Matching Pursuit Algorithm for Phased Array Radar[J]. Journal of Signal Processing, 2025, 41(7): 1165-1177. DOI: 10.12466/xhcl.2025.07.003.

[20] ZHOU L, ZHOU X, ZHU J, et al. Newtonized Orthogonal Matching Pursuit Based High-Resolution Parameter Estimation for Underwater Weak Targets[J]. Digital Ocean & Underwater Warfare, 2026, 9(1): 89-98. DOI: 10.19838/j.issn.2096-5753.2026.01.009.

[21] XU M, ZHU J, FANG J, et al. CFAR Based NOMP for Line Spectral Estimation and Detection[J]. IEEE Transactions on Aerospace and Electronic Systems, 2023, 59(5): 6971-6990. DOI: 10.1109/TAES.2023.3282610.

[22] SCHWARZ G. Estimating the Dimension of a Model[J]. The Annals of Statistics, 1978, 6(2): 461-464. DOI: 10.1214/aos/1176344136.

[23] STOICA P, SELEN Y. Model-Order Selection: A Review of Information Criterion Rules[J]. IEEE Signal Processing Magazine, 2004, 21(4): 36-47. DOI: 10.1109/MSP.2004.1311138.

[24] ZENG Y, HAN M, LI X, et al. QNOMP: A Quasi-Newton-Based Joint Multipath Optimization Method for Super-Resolution Channel Estimation and Extrapolation[J]. IEEE Access, 2025, 13: 212652-212671. DOI: 10.1109/ACCESS.2025.3645128.

[25] GOLUB G H, PEREYRA V. The Differentiation of Pseudo-Inverses and Nonlinear Least Squares Problems Whose Variables Separate[J]. SIAM Journal on Numerical Analysis, 1973, 10(2): 413-432. DOI: 10.1137/0710036.

[26] GOLUB G H, PEREYRA V. Separable Nonlinear Least Squares: The Variable Projection Method and Its Applications[J]. Inverse Problems, 2003, 19(2): R1-R26. DOI: 10.1088/0266-5611/19/2/201.

[27] MARQUARDT D W. An Algorithm for Least-Squares Estimation of Nonlinear Parameters[J]. Journal of the Society for Industrial and Applied Mathematics, 1963, 11(2): 431-441. DOI: 10.1137/0111030.

[28] KAUFMAN L. A Variable Projection Method for Solving Separable Nonlinear Least Squares Problems[J]. BIT Numerical Mathematics, 1975, 15(1): 49-57. DOI: 10.1007/BF01932995.

[29] NEWCOMBE R G. Improved Confidence Intervals for the Difference Between Binomial Proportions Based on Paired Data[J]. Statistics in Medicine, 1998, 17(22): 2635-2650. DOI: 10.1002/(SICI)1097-0258(19981130)17:22\<2635::AID-SIM954\>3.0.CO;2-C.

[30] MCNEMAR Q. Note on the Sampling Error of the Difference Between Correlated Proportions or Percentages[J]. Psychometrika, 1947, 12(2): 153-157. DOI: 10.1007/BF02295996.

[31] WILSON E B. Probable Inference, the Law of Succession, and Statistical Inference[J]. Journal of the American Statistical Association, 1927, 22(158): 209-212. DOI: 10.1080/01621459.1927.10502953.

