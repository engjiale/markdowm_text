# 无结构随机二维几何下的二维 NOMP：固定 $K=3$ 机制审计与 unknown-$K$ 最终边界

> 文档编号：19.5
>
> **主线定位：** 本文由《19.2D_NOMP未知K联合精修》的 5.6.17.5 节独立拆出，属于正式 separable 主线的随机几何、候选选择、门控和边界证据审计，不是旁支。
>
> 本文现已按独立文档重组为第 1–7 章。实验溯源以脚本名、结果文件名和固定种子为准；内容仍上接第 19 篇的前一阶段，并保留候选选择、解簇、追加 LM、门控接入和最终五带确认的完整证据链。

---

## 1. 文档定位、主线关系与冻结结论

本文与主线的关系不是“公式正文—无关附录”，而是“统一公式—详细证据审计”。四篇文档的职责为：

| 文档 | 主要职责 |
| --- | --- |
| [《19.2D_NOMP未知K联合精修》](./19.2D_NOMP未知K联合精修.md) | 保留从固定阶数到 unknown-$K$、随机几何和门控的算法演进 |
| **本文（19.5）** | 保存随机几何生成、失败重放、候选簇、追加 LM、门控接入和最终五带边界的完整实验审计 |
| [《20.2D_NOMP联合精修》](./20.2D_NOMP联合精修.md) | 给出当前 separable 主线的统一观测模型、公式、代码映射与正式结论 |
| [《20.5.2D_NOMP physical-LFM 扩展》](./20.5.2D_NOMP_physical-LFM扩展.md) | 单独记录 physical-LFM 观测、匹配原子与模型失配实验 |

第 2–6 章按研发时间线保留先导实验、失败重放、最低 RSS、解聚类、四折 CV、追加 LM 和门控确认过程。其中“下一步”“尚未运行”等措辞只表示当时状态，不代表当前仍有待完成任务；当前冻结结论以第 7 章为准。

本篇全部正式边界证据属于精确 separable 合成观测：

$$
Y_{\mathrm{sep}}
=\sum_{k=1}^{K}\beta_k a_R(R_k)a_D(v_k)^{\mathsf T}+W.
$$

这里“固定 $K=3$”是指五带实验的真实场景固定为 $K_{\mathrm{true}}=3$；unknown-$K$ 求解器并不知道该真值，只接收 $K_{\max}=5$。完整前缀 BIC 比较 $K=0,1,\ldots,5$，CFAR+BIC 则只比较在线停止前实际生成的 $K=0,1,\ldots,K_{\mathrm{candidate,max}}$。因此该实验仍然评价 unknown-$K$ 端到端选阶与定位，而不是把 $K=3$ 直接提供给求解器。

它不使用 `scene.y2d_roi` 作为正式估计输入；physical-LFM 和真实数据也尚未在同一冻结协议下确认该边界。

最终五带实验固定真实 $K_{\mathrm{true}}=3$、候选 $K_{\max}=5$、SNR $=20\ \mathrm{dB}$、`SNRMode='fixed_per_target'`、`BICRidge=0`、安全 ROI 均匀中心、iteration-cap gate 开启，以及距离和速度均不超过 $0.10$ Rayleigh 的定位容差。五个间隔带各包含 500 个独立场景；每个场景配对运行完整前缀 BIC 与 CFAR+BIC，共 2500 个场景、5000 行求解结果。外层求解失败率和门控内部求解失败数均为 0。

两种模式得到相同的冻结网格保守边界：

$$
\boxed{
d_{\min,2D}^{\mathrm{conservative}}
=0.35\ \text{Rayleigh}
}.
$$

对主结果完整前缀 BIC，$[0.30,0.35)$ 带成功 $481/500=96.2\%$，但 Wilson 95% 下限只有 $94.142\%$；$[0.35,0.40)$ 带成功 $486/500=97.2\%$，Wilson 下限为 $95.355\%$，且更高间隔带连续通过。因此 $0.35$ 是当前网格、生成分布和统计判据下的保守经验工作边界，不是无条件理论相变点，也不是单轴距离或速度阈值。

本章结论的冻结证据链为：

```text
experiments/range_doppler/run_2d_nomp_unknown_k_k3_random_geometry_gap_boundary_mc500.m
→ demos/demo_2d_unknown_k_k3_random_geometry_gap_boundary.m
→ data/range_doppler_unknown_k_k3_random_geometry_gap_boundary_mc500_*.csv
→ tests/test_range_doppler_unknown_k_k3_random_geometry_gap_boundary_demo.m
```

2026-07-21 的当前复核同时运行 core、unknown-$K$/BIC、iteration-cap gate、随机几何和五带边界五组测试，共 28/28 项通过。

---

## 2. 随机几何模型与先导实验

本章首先在真实阶数和求解阶数均固定为 $K=3$ 的条件下研究随机几何，只回答“给定三个目标时，顺序 NOMP、Top-$L$ 多起点和真值初始化能否完成定位”。它不包含 BIC/CFAR 选阶，因而属于机制实验；unknown-$K$ 接入和端到端边界见第 6–7 章。

在该机制实验建立之前，项目中已有三类相近代码，但各自缺少一项必要能力：

| 既有入口 | 已有能力 | 不能直接回答的问题 |
| --- | --- | --- |
| `eval_2d_nomp_k3_random_spacing_joint.m` | 固定 $K=3$ 的二维联合估计 | 三目标始终沿二维对角线等间隔排列，不是无结构随机几何 |
| `demo_2d_unknown_k_random_k_random_geometry.m` | 无结构二维随机坐标生成 | 同时混入 unknown-$K$ 选阶，不能隔离固定阶数定位损失 |
| `demo_2d_k3_hard_multistart_recovery.m` | 顺序 NOMP、Top-$L$、候选池覆盖和真值初始化对照 | 只复现一个固定 hard 拓扑，Top-$L$ 当时还是文件内私有函数 |

因此，该阶段新增并冻结了以下三个实现入口：

```text
src/algorithms/top_l_beam_range_doppler_refinement.m
demos/demo_2d_k3_random_geometry_generalization.m
experiments/range_doppler/run_2d_k3_random_geometry_generalization_pilot.m
```

三者的职责分别为：

- `top_l_beam_range_doppler_refinement.m`：将 Top-$L$ 残差峰搜索、有限波束保留和各起点的联合 VarPro-LM 精修整理为可复用算法入口；
- `demo_2d_k3_random_geometry_generalization.m`：生成随机几何和配对观测，并评价三条求解路径以及候选池覆盖诊断；
- `run_2d_k3_random_geometry_generalization_pilot.m`：冻结本章 150 个场景的先导实验配置。

这里的 `AnyBranchSuccess` 只表示保留分支中是否至少存在一个成功解，是依赖真值的候选覆盖率诊断，不是可在线输出的第四种估计器；真值初始化同样只用于检查局部联合精修能力，不是可部署入口。该先导脚本用于复现本章研发起点；当前正式边界入口已经转为第 7 章的五带实验。

### 2.1 无结构随机几何定义

每次试验先为三个目标生成独立的原始二维因子

$$
\mathbf u_k=
\begin{bmatrix}
u_{R,k}\\
u_{v,k}
\end{bmatrix},
\qquad
u_{R,k},u_{v,k}
\overset{\mathrm{i.i.d.}}{\sim}
\mathcal U(-1,1),
\qquad k=1,2,3.
$$

代码随后分别沿距离轴和速度轴平移三点包围盒，使两个轴的包围盒中点都回到零：

$$
\xi_{q,k}
=
u_{q,k}
-
\frac{
\min_\ell u_{q,\ell}
+
\max_\ell u_{q,\ell}
}{2},
\qquad q\in\{R,v\}.
$$

这一步只做平移，不做逐轴缩放。因此随机三角形的方向、形状和三对相对间隔全部保留，并且

$$
\max_k|\xi_{R,k}|\leq1,
\qquad
\max_k|\xi_{v,k}|\leq1.
$$

归一化距离、速度偏移及实际坐标定义为

$$
r_k=\eta_{R,\mathrm{span}}\xi_{R,k},
\qquad
q_k=\eta_{v,\mathrm{span}}\xi_{v,k},
$$

$$
R_k=R_c+r_k\Delta R_{\mathrm{res}},
\qquad
v_k=v_c+q_k\Delta v_{\mathrm{res}}.
$$

先导实验固定 $\eta_{R,\mathrm{span}}=0.90$、$\eta_{v,\mathrm{span}}=0.55$。这两个参数是随机簇允许达到的最大归一化半跨度，不是每次试验都必然达到的实际半跨度，也不是任意目标对的固定间隔。

`CenterSamplingMode='safe_roi_uniform'` 会根据本次随机簇的实际跨度和两个粗网格步长保护带计算

$$
m_R
=
\max_k|r_k|\Delta R_{\mathrm{res}}
+2\delta R_{\mathrm{grid}},
\qquad
m_v
=
\max_k|q_k|\Delta v_{\mathrm{res}}
+2\delta v_{\mathrm{grid}},
$$

再分别从

$$
R_c\sim\mathcal U(R_{\min}+m_R,R_{\max}-m_R),
\qquad
v_c\sim\mathcal U(v_{\min}+m_v,v_{\max}-m_v)
$$

中抽取中心。这样不仅三个目标不会越过 ROI，簇边缘还保留两个粗网格步长的搜索余量。在该模式下，输入的 `CenterRangeM=3000` 和 `CenterVelocityMps=12` 不是固定中心；它们只在 `CenterSamplingMode='fixed'` 时作为请求中心并被裁剪到安全范围。

对每一对目标 $i<j$，定义

$$
\eta_{R,ij}
=
\frac{|R_i-R_j|}{\Delta R_{\mathrm{res}}}
=|r_i-r_j|,
\qquad
\eta_{v,ij}
=
\frac{|v_i-v_j|}{\Delta v_{\mathrm{res}}}
=|q_i-q_j|,
$$

$$
d_{ij}^{(2D)}
=
\sqrt{\eta_{R,ij}^2+\eta_{v,ij}^2},
\qquad
d_{\min,2D}
=
\min_{i<j}d_{ij}^{(2D)}.
$$

这里的 $\eta_{v,ij}$ 是随机几何生成器用于分层的直接速度差。目标簇位于安全 ROI 内且不会跨越无模糊区间首尾，因此它与本批场景的周期最短速度差一致；估计误差匹配仍按 2.2 节所述使用一般性的周期速度差。

为使先导实验覆盖困难、过渡和容易几何，代码按 $d_{\min,2D}$ 进行拒绝采样：

| 分层标签 | $d_{\min,2D}$ 范围 | 场景数 |
| --- | ---: | ---: |
| hard | $[0.10,0.20)$ | 50 |
| transition | $[0.20,0.40)$ | 50 |
| easy | $[0.40,0.80)$ | 50 |

三层等量抽样是机制覆盖设计，得到的是各带条件成功率，不能按 $50{:}50{:}50$ 直接推断自然随机场景中的总体成功率。三层名称也不是预先证明的难度定理；尤其 easy 标签不等于 Wilson 95% 意义下的安全区。正式分箱和保守边界最终由第 7 章的五带确认实验冻结。

### 2.2 逐次保存的几何量与诊断量

逐次 CSV 固定按照 $12$、$13$、$23$ 三对保存完整几何，而不是只保留可能来自不同目标对的 `MinRangeGapRayleigh` 和 `MinVelocityGapRayleigh`：

```text
EtaR12, EtaV12, Eta2D12, Coherence12
EtaR13, EtaV13, Eta2D13, Coherence13
EtaR23, EtaV23, Eta2D23, Coherence23
```

其中

$$
\mathrm{SignedEtaR}_{ij}
=
\frac{R_j-R_i}{\Delta R_{\mathrm{res}}},
\qquad
\mathrm{SignedEtaV}_{ij}
=
\frac{v_j-v_i}{\Delta v_{\mathrm{res}}},
$$

`EtaRij` 和 `EtaVij` 是相应带符号量的绝对值，`Eta2Dij` 是二者的欧氏范数，`Coherenceij` 则是两个单位范数真实原子的相关幅度

$$
\mu_{ij}
=
\left|
\phi(R_i,v_i)^{\mathsf H}\phi(R_j,v_j)
\right|.
$$

CSV 同时保存下列带符号字段：

```text
SignedEtaR12, SignedEtaV12
SignedEtaR13, SignedEtaV13
SignedEtaR23, SignedEtaV23
```

此外还保存三个目标的完整距离、速度、相位、`TrialSeed` 和场景中心。这样既能恢复随机三角形的方向与排列，也不会把来自不同目标对的最小距离差和最小速度差错误拼成一个不存在的“最近目标对”。`MinRangeGapRayleigh` 与 `MinVelocityGapRayleigh` 仍可用于单轴汇总，但它们不保证由同一对目标取得；二维最近对必须以 `Min2DGapRayleigh` 或三个 `Eta2Dij` 字段判断。

每次试验的诊断量分为三类：

- **真实几何条件：** `Min2DGapRayleigh`、`TrueMaxCoherence` 和 `TrueGramConditionNumber`；
- **局部信息条件：** 消去未知复幅度后的 `FIMMinEigenvalue`、`FIMConditionNumber`，以及距离、速度的最大归一化 CRB 标准差；
- **算法结果：** 顺序 NOMP、Top-$L$ 最终输出和真值初始化的成功标志、双轴最大误差、求解失败与错误标识，以及 Top-$L$ 的候选数、RSS 选中起点、相对残差和选中解 Gram 条件数。

`AnyBranchSuccess` 另行记录“所有 Top-$L$ 最终分支中是否至少存在一个成功分支”。它是依赖真值的候选池覆盖上界，不参与实际分支选择，也不能当作可部署估计器的成功率。

三条求解路径和候选池诊断在同一次试验中共享完全相同的真实几何、随机相位、噪声与 `TrialSeed`。评价前遍历全部 $3! = 6$ 种目标排列，并使用周期速度差

$$
d_v(\widehat v,v)
=
\operatorname{mod}\!\left(
\widehat v-v+\frac{P_v}{2},P_v
\right)
-\frac{P_v}{2},
\qquad
P_v=2v_{\mathrm{ua}}.
$$

最佳排列定义为

$$
\pi^\star
=
\arg\min_{\pi}
\sum_{k=1}^{3}
\left[
\left(
\frac{\widehat R_{\pi(k)}-R_k}{\Delta R_{\mathrm{res}}}
\right)^2
+
\left(
\frac{d_v(\widehat v_{\pi(k)},v_k)}{\Delta v_{\mathrm{res}}}
\right)^2
\right].
$$

匹配后的双轴最大误差为

$$
e_R^{\max}
=
\max_k
\frac{|\widehat R_{\pi^\star(k)}-R_k|}
{\Delta R_{\mathrm{res}}},
\qquad
e_v^{\max}
=
\max_k
\frac{|d_v(\widehat v_{\pi^\star(k)},v_k)|}
{\Delta v_{\mathrm{res}}}.
$$

$$
\mathrm{Success}
\iff
e_R^{\max}\leq0.10
\quad\text{且}\quad
e_v^{\max}\leq0.10.
$$

因此成功判据是“同一最优目标对应下，距离和速度两轴同时达标”，不是只要求二维欧氏总误差小于 $0.10$。若某条路径抛出异常，则该路径记为求解失败且定位失败。FIM/CRB 只用于描述真值附近的局部信息条件；它随目标几何、真实复幅度相位和噪声方差变化，但不直接参与成功判定或算法选择。

### 2.3 先导实验配置与启动入口

本章使用历史先导入口 `experiments/range_doppler/run_2d_k3_random_geometry_generalization_pilot.m`。它显式冻结几何分层、SNR、粗网格、Top-$L$ 规模和输出选项：

```matlab
% experiments/range_doppler/run_2d_k3_random_geometry_generalization_pilot.m

script_dir = fileparts(mfilename('fullpath'));
project_root = fileparts(fileparts(script_dir));
addpath(project_root);
setup_project(false);

results_k3_random_geometry_pilot = ...
    demo_2d_k3_random_geometry_generalization( ...
        'NumTrialsPerStratum', 50, ...
        'StratumLabels', ["hard", "transition", "easy"], ...
        'StratumMin2DGapBounds', ...
            [0.10 0.20; 0.20 0.40; 0.40 0.80], ...
        'EtaRangeSpan', 0.90, ...
        'EtaVelocitySpan', 0.55, ...
        'SNRdB', 20, ...
        'Seed', 17031, ...
        'CenterRangeM', 3000, ...
        'CenterVelocityMps', 12, ...
        'CenterSamplingMode', 'safe_roi_uniform', ...
        'RangeRoiHalfWidthM', 180, ...
        'RangeOversample', 4, ...
        'VelocityOversample', 8, ...
        'VelocityLimitsMps', [-30 30], ...
        'MatchToleranceRayleigh', 0.10, ...
        'TopL', 3, ...
        'BeamWidth', 8, ...
        'GenerateFigure', true, ...
        'SaveResults', true, ...
        'WriteCsv', true, ...
        'Verbose', true);
```

启动脚本未逐项传入、但同样参与本轮结果的算法默认值为：

| 配置项 | 冻结值 | 作用 |
| --- | ---: | --- |
| `RandomGeometryMaxAttempts` | $20000$ | 每个分层场景的最大拒绝采样次数 |
| `LocalMaxIterations` | $10$ | 顺序 NOMP 单目标局部精修上限 |
| `GlobalMaxIterations` | $10$ | 顺序 NOMP 和 Top-$L$ 联合精修上限 |
| `TruthInitMaxIterations` | $10$ | 真值初始化联合精修上限 |
| `CoarseDuplicateDistance` | $0.05$ | Top-$L$ 粗候选去重半径，单位为 Rayleigh |
| `PeakSuppressionRadiusRayleigh` | $0.20$ | 粗谱峰抑制半径，单位为 Rayleigh |
| `BeamDuplicateDistance` | $0.10$ | 波束状态去重半径，单位为 Rayleigh |
| `RefinementDuplicateDistance` | $0.05$ | 联合精修后的目标去重半径，单位为 Rayleigh |
| `Ridge` | $10^{-8}$ | 复幅度 Ridge LS 的相对正则输入 |

这些值定义在 `demo_2d_k3_random_geometry_generalization.m` 的参数解析器中。复现实验时既要保留启动脚本，也要保留这些默认值；如果后续修改算法默认值，应在调用处显式传入旧值或把结果标记为新版本实验。

第 $\ell$ 个分层中的第 $n$ 次试验使用

$$
s_{\ell,n}
=
17031+100000\ell+n-1,
\qquad
\ell\in\{1,2,3\},
\quad
n\in\{1,\ldots,50\},
$$

其中 $\ell=1,2,3$ 依次对应 hard、transition 和 easy。该值作为 `twister` 随机种子，因此几何拒绝采样、中心位置、目标相位和噪声都能由 `TrialSeed` 完整重放，而不同分层使用互不重叠的种子段。

三个目标的复幅度模长固定为 $1$，相位独立采样：

$$
\varphi_k\sim\mathcal U(0,2\pi),
\qquad
\beta_k=e^{\mathrm j\varphi_k},
\qquad
\mathbf y_0=\Phi\boldsymbol\beta.
$$

噪声按单个单位模长目标的平均采样功率定标。若观测向量长度为 $M$，且真实原子已经单位范数归一化，则代码采用

$$
P_{\mathrm{ref}}
=
\frac{\|\phi(R_1,v_1)\|_2^2}{M}
=
\frac{1}{M},
\qquad
\sigma_w^2
=
\frac{P_{\mathrm{ref}}}{10^{20/10}}.
$$

所以 `SNRdB=20` 表示“相对于单个单位模长目标”的标称 SNR，而不是每个随机场景都具有完全相同的总场景 SNR。由于三个原子可能相干且相位随机，$\|\Phi\boldsymbol\beta\|_2^2/M$ 会随几何和相位变化。

在 MATLAB 项目根目录运行：

```matlab
setup_project(false);

run(fullfile( ...
    'experiments', 'range_doppler', ...
    'run_2d_k3_random_geometry_generalization_pilot.m'));
```

输出文件为：

```text
data/range_doppler_k3_random_geometry_generalization_pilot.mat
data/range_doppler_k3_random_geometry_generalization_pilot_summary.csv
data/range_doppler_k3_random_geometry_generalization_pilot_trials.csv
images/range_doppler_k3_random_geometry_generalization_pilot.png
```

其中，`summary.csv` 按 hard、transition 和 easy 三层汇总三条求解路径及 `AnyBranchSuccess` 诊断的成功率和 95% Wilson 区间；`trials.csv` 保存逐次几何、相位、种子、误差和条件数；图片用于观察分层成功率以及最小二维间隔与相干度、Gram 条件数、FIM 条件数之间的关系。

开发阶段先以每层 $1$ 次的 smoke test 检查随机几何生成、三对间隔一致性、公共 Top-$L$ 入口、Gram/FIM 字段和结果表构造，随后按上述配置完成每层 $50$ 次、共 $150$ 个场景的先导实验。本章结果只评价固定 $K=3$ 定位机制，不能替代 unknown-$K$ 的选阶与端到端成功率实验。

### 2.4 随机 $K=3$ 几何先导实验结果

先导实验的核心结论是：固定 $K=3$ 时，三条求解路径在三个分层中都取得了较高的条件成功率，但 hard 层已经出现“候选池包含正确解、最低 RSS 却选中错误解”的现象。因此，这批数据能够定位算法损失机制，却不足以单独给出保守分辨边界。

在 $K=3$、单目标标称 SNR 为 $20\,\mathrm{dB}$、`TopL=3`、`BeamWidth=8` 且每层 $50$ 个场景的条件下，三条求解路径和一项候选池覆盖诊断的结果为：

| 分层 | 顺序 NOMP | 95% Wilson 区间 | Top-$L$ 的 RSS 选中输出 | 95% Wilson 区间 | `AnyBranchSuccess` 诊断 | 95% Wilson 区间 | 真值初始化诊断 | 95% Wilson 区间 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| hard | 94% | [83.78%, 97.94%] | 90% | [78.64%, 95.65%] | 98% | [89.51%, 99.65%] | 92% | [81.16%, 96.85%] |
| transition | 96% | [86.54%, 98.90%] | 96% | [86.54%, 98.90%] | 96% | [86.54%, 98.90%] | 96% | [86.54%, 98.90%] |
| easy | 100% | [92.87%, 100%] | 100% | [92.87%, 100%] | 100% | [92.87%, 100%] | 100% | [92.87%, 100%] |

对应的分层描述性均值为：

| 分层 | 平均 $d_{\min,2D}$ | 平均最大相干度 | 平均 Gram 条件数 | 平均 FIM 条件数 | 平均最大距离 CRB 标准差 | 平均最大速度 CRB 标准差 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| hard | 0.16095 | 0.97185 | 251.06 | 5707.1 | 0.03404 | 0.03067 |
| transition | 0.29581 | 0.91100 | 82.864 | 3437.4 | 0.02591 | 0.01518 |
| easy | 0.57861 | 0.75027 | 15.628 | 329.87 | 0.00988 | 0.00469 |

本轮 $150$ 次 Top-$L$ 调用均未抛出求解异常，并且每次都返回 $8$ 个保留起点。这说明在本批配置和数据上没有观察到多起点求解器异常，但不能据此证明它对所有几何都数值稳定。

从 hard 到 easy，平均最大相干度、Gram 条件数、FIM 条件数和两个维度的 CRB 总体下降，说明按 $d_{\min,2D}$ 拒绝采样确实形成了总体难度梯度。不过这些量彼此相关，表中又是各带条件均值，因此只能作描述性比较，不能把均值变化解释为 $d_{\min,2D}$ 对成功率的单变量因果效应。

transition 层有两份场景在三条求解路径及候选池诊断下共同失败。其中 `transition_24`（`TrialSeed=217054`）满足

$$
d_{\min,2D}=0.37695,
\qquad
\kappa(\mathrm{FIM})=69879.52,
\qquad
\max_k\sigma_{R,k}^{\mathrm{CRB}}=0.16784.
$$

它的最小二维间隔已经接近 easy 层下界，但完整三目标局部信息仍然高度病态。这说明三对完整几何

$$
\left\{
(\eta_{R,12},\eta_{v,12}),
(\eta_{R,13},\eta_{v,13}),
(\eta_{R,23},\eta_{v,23})
\right\}
$$

以及目标相位、噪声实现、原子相干度和 Gram/FIM 指标都不能被单个 $d_{\min,2D}$ 替代。

逐次配对计数为：

| 分层 | 顺序 NOMP | Top-$L$ 的 RSS 选中输出 | `AnyBranchSuccess` | 真值初始化诊断 |
| --- | ---: | ---: | ---: | ---: |
| hard | $47/50$ | $45/50$ | $49/50$ | $46/50$ |
| transition | $48/50$ | $48/50$ | $48/50$ | $48/50$ |
| easy | $50/50$ | $50/50$ | $50/50$ | $50/50$ |

hard 层中还可进一步分解出：

- 顺序 NOMP 失败而 Top-$L$ 的 RSS 选中输出成功：$0$ 次；
- 顺序 NOMP 成功而 Top-$L$ 的 RSS 选中输出失败：$2$ 次；
- 候选池中存在成功分支，但最低 RSS 未选中它：$4$ 次；
- 所有 Top-$L$ 分支均失败：$1$ 次。

因此，hard 层候选池覆盖率为 $98\%$，但最低 RSS 选中输出的成功率只有 $90\%$。五次 Top-$L$ 输出失败中，四次属于选择失败，只有一次属于候选覆盖失败；第 3 章将据此审计最低 RSS 选择机制。

最后，每层只有 $50$ 次试验。即使 easy 层观察到 $50/50$，其 95% Wilson 下界也只有 $92.87\%$，所以本章不能宣称已经得到“95% 保守安全区”。若保持零失败，样本量至少达到 $73$ 时 Wilson 下界才刚能达到 $95\%$；若出现失败，则还需要更多样本。项目的正式保守边界以第 7 章五带 unknown-$K$ 确认实验为准，本章只提供其随机几何建模与失败机制依据。

## 3. 最低 RSS 选择的失败机制

第 2 章表明，固定 $K=3$ 时，Top-$L$ 候选池覆盖率高于最低 RSS 最终输出成功率。这意味着“候选池中存在正确分支”和“最低 RSS 选中正确分支”是两个不同问题。本章先精确重放失败场景，再对全部 $150$ 个随机几何场景进行全分支审计，从而区分：

1. **候选覆盖失败：** 当前保留分支中没有满足定位容差的解；
2. **分支选择失败：** 候选池已有正确解，但最低 RSS 选中了错误解；
3. **真值初始化诊断：** 即使从真值附近启动，当前联合精修在给定噪声和 $0.10$ Rayleigh 容差下仍未成功。

前两类互斥，并可完整分解最低 RSS 输出失败；第三项是辅助诊断，可能与前两类重叠。这里评估的是冻结配置下的经验失效机制，不是对最低 RSS 准则的一般性否定，也不包含 unknown-$K$ 选阶。

### 3.1 失败场景的精确重放与分支审计

本节审计第 2 章中 Top-$L$ 最终输出失败的 $7$ 个场景，包括 hard 层 $5$ 个和 transition 层 $2$ 个。对应入口为：

```text
demos/demo_2d_k3_random_geometry_failure_audit.m
experiments/range_doppler/run_2d_k3_random_geometry_failure_audit.m
```

启动脚本使用 `AuditMode='multistart_failures'`、`Strata=["hard","transition"]` 和 `ConsensusRadiusRayleigh=0.25`。在项目根目录运行：

```matlab
setup_project(false);
run(fullfile( ...
    'experiments', 'range_doppler', ...
    'run_2d_k3_random_geometry_failure_audit.m'));
```

审计不是只读取原 CSV 的成功标志，而是从每个 `TrialSeed` 重新执行几何拒绝采样、中心抽样、随机相位和复高斯噪声生成，再重新运行顺序 NOMP、Top-$L$ 联合精修与真值初始化。代码同时检查：

- 距离重放最大误差不超过 $10^{-9}\,\mathrm m$；
- 速度和相位重放最大误差分别不超过 $10^{-12}\,\mathrm{m/s}$ 和 $10^{-12}\,\mathrm{rad}$；
- 顺序 NOMP、最低 RSS 输出、`AnyBranchSuccess` 和真值初始化四项结果标志与先导实验一致；
- 每条分支的 RSS 排名、定位误差、Gram 条件数、幅度动态范围、重复目标警告和共识数。

对第 $b$ 条联合精修分支，代码先用 Ridge LS 得到复幅度 $\widehat{\boldsymbol\alpha}_b$，随后以未附加 Ridge 惩罚项的残差能量排序：

$$
\operatorname{RSS}_b
=
\left\|
\mathbf y
-
\mathbf A(\widehat{\boldsymbol\theta}_b)
\widehat{\boldsymbol\alpha}_b
\right\|_2^2,
\qquad
b_{\min}
=
\arg\min_b \operatorname{RSS}_b.
$$

因此，`Ridge` 参与复幅度估计和联合精修的数值稳定化，但最终分支排名比较的是观测残差平方和本身。若至少存在一条成功分支而 $b_{\min}$ 失败，则记为“选择失败”；若全部分支均失败，则记为“无成功分支”。

$7$ 个场景的真实坐标和相位均通过精确重放检查，四项结果标志全部与先导实验一致，共重新评估 $56$ 条分支。汇总结果为：

| 分层 | 场景数 | 精确重放 | 四项标志一致 | 选择失败 | 无成功分支 | 真值初始化失败 | 平均成功分支数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| hard | $5$ | $5$ | $5$ | $4$ | $1$ | $4$ | $3.4$ |
| transition | $2$ | $2$ | $2$ | $0$ | $2$ | $2$ | $0$ |

为比较正确分支与最低 RSS 分支，定义正确分支的相对 RSS 代价

$$
\delta_b
=
\frac{
\operatorname{RSS}_b-\operatorname{RSS}_{\min}
}{
\operatorname{RSS}_{\min}
}.
$$

hard 层五个失败场景的分支级结果为：

| 场景 | 成功分支数/$8$ | 失败类型 | 最佳成功分支 RSS 排名 | 最佳成功分支 $\delta_b$ | 真值初始化 |
| --- | ---: | --- | ---: | ---: | --- |
| `hard_14` | $0$ | 候选覆盖失败 | — | — | 成功 |
| `hard_22` | $7$ | 选择失败 | $2$ | $0.0289\%$ | 失败 |
| `hard_28` | $2$ | 选择失败 | $6$ | $0.0144\%$ | 失败 |
| `hard_39` | $3$ | 选择失败 | $4$ | $0.2338\%$ | 失败 |
| `hard_50` | $5$ | 选择失败 | $3$ | $0.0388\%$ | 失败 |

`hard_14` 从真值初始化能够成功，但八条搜索分支均未满足双轴定位容差，说明该场景的主要问题是当前粗候选和波束搜索没有覆盖到可成功精修的起点。其余四个 hard 场景都已经包含正确分支，却被最低 RSS 选错；最佳正确分支相对最低 RSS 只增加 $0.0144\%$ 至 $0.2338\%$ 的残差。

这一结果能够证明分支之间存在近 RSS 竞争，但仅凭原始分支表还不能断言它们都对应不同局部极值，因为多个起点可能重复收敛到同一参数解。第 4 章将先对精修解聚类，再区分“同一解的重复副本”和“真正不同的近 RSS 解簇”。

四个选择失败场景的 Gram 条件数与分支共识数为：

| 场景 | RSS 选中分支 Gram 条件数 | 最佳成功分支 Gram 条件数 | 选中分支共识数 | 最佳成功分支共识数 |
| --- | ---: | ---: | ---: | ---: |
| `hard_22` | $112.94$ | $180.77$ | $7$ | $8$ |
| `hard_28` | $25.954$ | $43.390$ | $8$ | $8$ |
| `hard_39` | $161.36$ | $6301.4$ | $5$ | $4$ |
| `hard_50` | $16.645$ | $1258.8$ | $1$ | $6$ |

四个最佳成功分支的 Gram 条件数都高于相应的错误选中分支。因此，直接在 RSS 上增加正的 Gram 条件数惩罚会更偏向这些已经选错的低条件数分支，不能作为修复方案。共识数同样不具备一致方向：`hard_22` 和 `hard_50` 支持成功分支，`hard_28` 两者相同，而 `hard_39` 反而支持错误分支。因此，Gram 条件数或单一共识数都不能独立完成可靠二次选择。

`transition_24` 和 `transition_44` 的八条分支全部失败，顺序 NOMP 和真值初始化也同时失败。现有候选池的任何重新排序都无法修复这两个场景；同时，真值初始化失败表明其损失不能只归因于粗搜索初始化。是否能通过更大的搜索预算、不同精修设置或放宽定位容差改善，需要另行实验，不能由本次重放直接断定。

综上，后续工作被明确分成三类：

- `hard_14`：检查粗谱峰覆盖、Top-$L$、`BeamWidth` 和峰抑制半径；
- `hard_22`、`hard_28`、`hard_39`、`hard_50`：在保留正确候选的前提下研究解聚类和可观测二次选择；
- `transition_24`、`transition_44`：结合 FIM/CRB、噪声实现和成功容差分析局部统计困难。

本节输出为：

```text
data/range_doppler_k3_random_geometry_failure_audit.mat
data/range_doppler_k3_random_geometry_failure_audit_summary.csv
data/range_doppler_k3_random_geometry_failure_audit_trials.csv
data/range_doppler_k3_random_geometry_failure_audit_branches.csv
images/range_doppler_k3_random_geometry_failure_audit.png
```

### 3.2 全部随机几何的最低 RSS 选择基线

定点失败审计只能说明七个已知失败场景的机制，不能给出最低 RSS 在全部随机几何中的条件失效率。因此，本节重新运行第 2 章的全部 $150$ 个场景，并保存每个场景的全部八条精修分支。对应入口为：

```text
demos/demo_2d_k3_min_rss_selection_audit.m
experiments/range_doppler/run_2d_k3_min_rss_selection_audit.m
tests/test_range_doppler_k3_min_rss_selection_audit.m
```

`demo_2d_k3_min_rss_selection_audit.m` 不复制随机几何和分支生成逻辑，而是调用 3.1 节已经验证的重放程序，并固定

```text
AuditMode = all
Strata = [hard, transition, easy]
TrialNumbers = []
ConsensusRadiusRayleigh = 0.25
```

其中，`ConsensusRadiusRayleigh` 只用于统计分支共识数，不参与最低 RSS 排名。程序由此重放三个分层各 $50$ 个场景，总计 $150$ 个场景和 $1200$ 条分支。启动方式为：

```matlab
setup_project(false);
run(fullfile( ...
    'experiments', 'range_doppler', ...
    'run_2d_k3_min_rss_selection_audit.m'));
```

设第 $i$ 个场景中“至少存在一条成功分支”的标志为 $A_i$，最低 RSS 分支成功标志为 $S_i$。条件选择效率定义为

$$
\eta_{\mathrm{sel}}
=
\frac{\sum_i A_iS_i}{\sum_i A_i}
=
P\!\left(
S_i=1\mid A_i=1
\right).
$$

这里：

- `MinimumRSSSuccessRate` 是 $P(S_i=1)$；
- `AnyBranchSuccessRate` 是 $P(A_i=1)$，属于依赖真值的候选池覆盖诊断；
- `SelectionEfficiencyGivenAvailable` 是 $\eta_{\mathrm{sel}}$；
- `SelectionMissRate` 是 $P(A_i=1,S_i=0)$，分母为该层全部场景；
- `NoSuccessfulBranchRate` 是 $P(A_i=0)$。

审计还记录原始 RSS 排名中第二条分支相对第一条分支的间隔

$$
\Delta_{\mathrm{RSS},2}
=
\frac{
\operatorname{RSS}_{(2)}
-
\operatorname{RSS}_{(1)}
}{
\operatorname{RSS}_{(1)}
},
$$

以及最佳成功分支的 RSS 名次和相对代价。需要注意，$\Delta_{\mathrm{RSS},2}$ 比较的是原始分支，不保证两条分支对应不同参数解。

正式重放结果中，$150$ 个场景的几何与相位检查全部通过，顺序 NOMP、最低 RSS 输出、候选池覆盖和真值初始化四项标志也全部与先导实验一致；每个场景均返回八条分支，$1200$ 条分支中没有记录求解异常。最低 RSS 基线的主要结果为：

| 分层 | 场景数 | 最低 RSS 成功 | 95% Wilson 区间 | 任一分支成功 | 95% Wilson 区间 | 条件选择效率 | 选择失败率 | 无成功分支率 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| overall | $150$ | $143/150=95.33\%$ | $[90.68\%,97.72\%]$ | $147/150=98.00\%$ | $[94.29\%,99.32\%]$ | $143/147=97.28\%$ | $2.67\%$ | $2.00\%$ |
| hard | $50$ | $45/50=90.00\%$ | $[78.64\%,95.65\%]$ | $49/50=98.00\%$ | $[89.50\%,99.65\%]$ | $45/49=91.84\%$ | $8.00\%$ | $2.00\%$ |
| transition | $50$ | $48/50=96.00\%$ | $[86.54\%,98.90\%]$ | $48/50=96.00\%$ | $[86.54\%,98.90\%]$ | $48/48=100\%$ | $0$ | $4.00\%$ |
| easy | $50$ | $50/50=100\%$ | $[92.87\%,100\%]$ | $50/50=100\%$ | $[92.87\%,100\%]$ | $50/50=100\%$ | $0$ | $0$ |

同一批重放中，顺序 NOMP、最低 RSS 输出、任一分支诊断和真值初始化分别成功 $145$、$143$、$147$ 和 $144$ 次。逐场景配对后，最低 RSS 输出没有救回顺序 NOMP 的失败场景，却使两个顺序 NOMP 成功场景变为失败。因此，在当前固定 $K=3$ 配置下，增加多起点提高了候选覆盖，但若仍以同一拟合观测上的最低 RSS 硬选择，最终输出不保证优于顺序 NOMP。真值初始化成功率也不是性能上界，它只反映从真值附近启动时当前精修器的行为。

七个最低 RSS 输出失败场景可以完整分解为：

- $4$ 个选择失败：`hard_22`、`hard_28`、`hard_39` 和 `hard_50`；
- $3$ 个无成功分支：`hard_14`、`transition_24` 和 `transition_44`。

因此，所有可通过“重新选择现有分支”挽救的损失都集中在 hard 层；transition 层的两个失败场景没有可供选择器恢复的正确分支，easy 层则没有观察到失败。这是三个等量条件分层中的经验结果，不能直接外推为自然随机几何的总体发生率。

全体场景的原始第二低 RSS 相对间隔中位数仅为

$$
\operatorname{median}
\left(
\Delta_{\mathrm{RSS},2}
\right)
=
4.4342\times10^{-11},
$$

即 $4.4342\times10^{-9}\%$。但 easy 层的中位数反而更小，为 $1.3838\times10^{-11}$，说明“原始第二分支几乎同 RSS”并不等价于“定位存在歧义”；更常见的原因是多个起点重复收敛到同一解。四个选择失败场景中，最佳成功分支相对最低 RSS 的代价中位数为 $0.03385\%$，但在确认这些分支是否属于不同参数解之前，不能据此直接设定近 RSS 阈值。

所以本章冻结的结论是：

1. 当前最低 RSS 硬选择的总体条件选择效率为 $97.28\%$，hard 层降至 $91.84\%$；
2. 四个可挽救选择错误全部位于 hard 层，三份其余失败不能由现有分支重新排序修复；
3. Gram 条件数、单一分支共识数和原始第二分支 RSS 间隔都不足以直接构成可靠选择器；
4. 下一步必须先把重复收敛分支合并为不同参数解簇，再讨论近 RSS 竞争和簇级二次选择，这正是第 4 章的任务。

正式输出为：

```text
data/range_doppler_k3_min_rss_selection_audit.mat
data/range_doppler_k3_min_rss_selection_audit_summary.csv
data/range_doppler_k3_min_rss_selection_audit_trials.csv
data/range_doppler_k3_min_rss_selection_audit_branches.csv
images/range_doppler_k3_min_rss_selection_audit.png
```

`tests/test_range_doppler_k3_min_rss_selection_audit.m` 使用三个分层各一个场景检查重放、分支数、RSS 排名和汇总字段，属于结构性 smoke test；本节的正式数值结论来自上述 $150$ 场景 CSV，而不是由三场景测试推断。
## 4. 解聚类与簇级二次选择

第 3 章保存的八条 Top-$L$ 分支并不等于八个不同参数解：多个初始化可能收敛到同一完整三目标解，而真正不同的解又可能具有非常接近的 RSS。本章因此先做完整解聚类，再在不同解簇之间设计二次选择器，并用新伪随机种子检验开发集收益能否复现。

本章的证据顺序和最终决策为：

| 阶段 | 数据 | 目的 | 证据边界 |
| --- | --- | --- | --- |
| 解聚类阈值审计 | 开发集，基种子 $17031$ | 区分重复收敛与不同参数解 | 只冻结诊断表示 |
| 簇级选择器开发 | 同一开发集 | 比较 RSS、四折 CV 和支持数 | 只能产生候选规则 |
| 冻结规则验证 | 独立伪随机集，基种子 $27031$ | 检验开发集净改善能否复现 | 决定是否替换正式基线 |
| 混合簇边界审计 | 开发集与独立集 | 解释同簇成功/失败混合 | 评估硬成功阈值的敏感性 |

开发集上，maximum support 相对最低 RSS 得到 $2$ 次救回、$0$ 次伤害；独立集上则为 $0/0$，四种选择规则均为 $141/150$。因此本章最终保留最低 RSS 作为正式分支选择基线；$0.05$ Rayleigh 解聚类、支持数和四折 CV 继续作为诊断量，不升级为已经验证的性能改进。

### 4.1 完整三目标解聚类与阈值冻结

对应入口为：

```text
demos/demo_2d_k3_refined_solution_cluster_audit.m
experiments/range_doppler/run_2d_k3_refined_solution_cluster_audit.m
tests/test_range_doppler_k3_refined_solution_cluster_audit.m
```

该审计直接读取第 3.2 节保存的 $150$ 个场景和 $1200$ 条精修分支，不重新生成噪声，也不重新运行 NOMP。第 $b$ 条分支是一套完整的三目标联合解

$$
\Theta_b
=
\left\{
(\widehat R_{b,k},\widehat v_{b,k})
\right\}_{k=1}^{3},
$$

而不是单独的某一个距离–速度点。对两个完整解 $\Theta_a$ 和 $\Theta_b$，定义置换不变、速度周期回绕的 Rayleigh 归一化距离

$$
d(\Theta_a,\Theta_b)
=
\min_{\pi\in S_3}
\left[
\sum_{k=1}^{3}
\left\{
\left(
\frac{\widehat R_{a,k}-\widehat R_{b,\pi(k)}}
{\Delta R_{\mathrm{res}}}
\right)^2
+
\left(
\frac{
\delta_{P_v}(
\widehat v_{a,k}-\widehat v_{b,\pi(k)})
}{
\Delta v_{\mathrm{res}}
}
\right)^2
\right\}
\right]^{1/2},
\qquad
P_v=2v_{\mathrm{ua}},
$$

其中 $\delta_{P_v}(\cdot)$ 表示周期为 $P_v$ 的最短有符号速度差。

当 $d(\Theta_a,\Theta_b)\leq d_{\mathrm{cluster}}$ 时，在两条分支之间连边，再取无向图的连通分量作为解簇。这是单链接式连通分量聚类：阈值约束的是相邻分支之间的边，不保证整个簇的直径也不超过 $d_{\mathrm{cluster}}$。每个簇 $c$ 的代表和支持数定义为

$$
b_c^\star
=
\arg\min_{b\in c}\operatorname{RSS}_b,
\qquad
\Theta_c=\Theta_{b_c^\star},
\qquad
n_c=|c|.
$$

因此，簇代表是簇内最低 RSS 的完整三目标解，不是成员参数的平均值；$n_c$ 表示有多少条初始化分支落入该簇。`RepresentativeSuccess`、`ContainsSuccessfulBranch` 和 `MixedSuccessCluster` 都依赖真值，只能用于离线审计，不能进入在线选择规则。

代码扫描

```matlab
ClusterDistanceThresholds = [0.01 0.025 0.05 0.10 0.25];
PrimaryClusterDistance = 0.05;
```

并在扫描前将 $0.05$ Rayleigh 指定为主分析阈值。运行方式为：

```matlab
setup_project(false);
run(fullfile( ...
    'experiments', 'range_doppler', ...
    'run_2d_k3_refined_solution_cluster_audit.m'));
```

在 $d_{\mathrm{cluster}}=0.05$ 下，$1200$ 条分支形成 $261$ 个解簇。主要结果为：

| 分层 | 平均解簇数 | 中位解簇数 | 单簇场景率 | 最低 RSS 簇平均支持数 | 最低 RSS 成功率 | 任一含成功成员簇率 | 条件选择效率 | 第二不同簇 RSS 间隔中位数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| overall | $1.74$ | $1$ | $69.33\%$ | $6.91$ | $95.33\%$ | $98.00\%$ | $97.28\%$ | $1.8196\%$ |
| hard | $2.30$ | $2$ | $46.00\%$ | $5.92$ | $90.00\%$ | $98.00\%$ | $91.84\%$ | $0.2714\%$ |
| transition | $1.66$ | $1$ | $76.00\%$ | $7.14$ | $96.00\%$ | $96.00\%$ | $100\%$ | $32.209\%$ |
| easy | $1.26$ | $1$ | $86.00\%$ | $7.66$ | $100\%$ | $100\%$ | $100\%$ | $40.250\%$ |

第二不同簇间隔只对至少含两个解簇的场景计算。全体场景原始第二分支的 RSS 相对间隔中位数为 $4.4342\times10^{-11}$，即 $4.4342\times10^{-9}\%$；去除同簇重复收敛后，第二个不同解簇的中位间隔变为 $1.8196\%$。因此，原始分支级近并列 RSS 主要反映重复收敛，不能把分支级 Top-$L$ 直接解释为 $L$ 个不同候选解。

阈值敏感性结果为：

| 聚类阈值/Rayleigh | 平均解簇数 | 单簇场景率 | 最低 RSS 簇混合率 | 全部混合簇数 |
| ---: | ---: | ---: | ---: | ---: |
| $0.01$ | $2.120$ | $66.67\%$ | $0$ | $0$ |
| $0.025$ | $1.927$ | $68.67\%$ | $0$ | $0$ |
| $0.05$ | $1.740$ | $69.33\%$ | $0$ | $2$ |
| $0.10$ | $1.540$ | $72.67\%$ | $2.00\%$ | $5$ |
| $0.25$ | $1.287$ | $78.00\%$ | $10.67\%$ | $17$ |

较小阈值会保留更多细微数值差异；增大到 $0.10$ 或 $0.25$ 后，最低 RSS 簇开始同时包含成功和失败成员。$0.05$ Rayleigh 在开发集上合并了大部分重复分支，同时没有出现“最低 RSS 簇成功/失败混合”，因此被固定用于后续审计。这里并不是说 $0.05$ 能消除所有混合簇：该阈值下仍有两个未被最低 RSS 选中的混合簇，第 4.4 节会单独分析。

七个最低 RSS 失败场景中：

- `hard_14`、`transition_24` 和 `transition_44` 没有任何含成功成员的解簇；
- `hard_22`、`hard_28`、`hard_39` 和 `hard_50` 存在成功解簇，其最佳成功簇 RSS 名次分别为 $2$、$2$、$2$ 和 $3$。

后四个场景的第二不同簇 RSS 间隔分别为 $0.02888\%$、$0.01439\%$、$0.23377\%$ 和 $0.00653\%$。其中 `hard_50` 的第二簇仍是失败簇，最佳成功簇位于第三名；因此“第二不同簇很近”不等于“第二簇就是正确解”。

本节输出为：

```text
data/range_doppler_k3_refined_solution_cluster_audit.mat
data/range_doppler_k3_refined_solution_cluster_audit_summary.csv
data/range_doppler_k3_refined_solution_cluster_audit_trials.csv
data/range_doppler_k3_refined_solution_cluster_audit_clusters.csv
images/range_doppler_k3_refined_solution_cluster_audit.png
```

对应测试检查 $150$ 个主阈值场景及敏感性切片的解簇数、代表分支和汇总字段；正式统计仍以上述 CSV 为准。

### 4.2 簇级候选、四折 CV 与开发集配对审计

对应入口为：

```text
demos/demo_2d_k3_cluster_level_selector_audit.m
experiments/range_doppler/run_2d_k3_cluster_level_selector_audit.m
tests/test_range_doppler_k3_cluster_level_selector_audit.m
```

程序读取 $0.05$ Rayleigh 解簇结果，并按原 `TrialSeed` 重建同一批观测，不重新运行 NOMP 或联合精修。对簇代表构造近 RSS 候选集

$$
\mathcal C_\tau
=
\left\{
c:
\frac{
\operatorname{RSS}_c-\operatorname{RSS}_{\min}
}{
\operatorname{RSS}_{\min}
}
\leq \tau_{\mathrm{RSS}}
\right\}.
$$

开发阶段扫描

```matlab
NearRSSRelativeTolerances = ...
    [0 0.0001 0.00025 0.0005 0.001 0.0025 0.005];
PrimaryNearRSSRelativeTolerance = 0.0025;
```

即 $0$ 至 $0.5\%$，并把 $\tau_{\mathrm{RSS}}=0.0025=0.25\%$ 作为主开发阈值。该值接近 hard 层第二不同簇间隔中位数 $0.2714\%$，因此属于基于开发集选定的候选门限，必须经过独立数据验证，不能直接视为普适常数。

候选集内比较四种规则：

1. **minimum RSS：** 始终选择簇代表 RSS 最低者；
2. **four-fold CV：** 选择四折验证 MSE 最低者，若并列则按 RSS 和簇排名打破平局；
3. **maximum support：** 优先选择支持数 $n_c$ 最大者，再按 RSS 和簇排名打破平局；
4. **CV one-SE + support：** 先保留 CV 分数处于“最佳 CV 加一个标准误差”范围内的簇，再按支持数、RSS 和簇排名选择。

四折 CV 与簇内有几条分支无关。代码将同一二维距离–脉冲观测按采样编号奇偶性分成四个交错棋盘折：

| 折 | 距离采样编号 | 脉冲编号 |
| --- | --- | --- |
| Fold 1 | 奇数 | 奇数 |
| Fold 2 | 偶数 | 奇数 |
| Fold 3 | 奇数 | 偶数 |
| Fold 4 | 偶数 | 偶数 |

其折编号可写为

$$
f(r,p)
=
1+\operatorname{mod}(r-1,2)
+2\operatorname{mod}(p-1,2).
$$

对候选代表 $\Theta_c$，距离和速度固定，只在三个训练折上重新估计复幅度。第 $f$ 折的训练解为

$$
\widehat{\boldsymbol\alpha}_{c,-f}
=
\arg\min_{\boldsymbol\alpha}
\left[
\left\|
\mathbf y_{\mathcal T_f}
-
\mathbf A_{\mathcal T_f}(\Theta_c)
\boldsymbol\alpha
\right\|_2^2
+
\lambda_{\mathrm{eff}}
\|\boldsymbol\alpha\|_2^2
\right],
$$

其中 `ValidationRidge=1e-8` 是相对正则输入，`solve_ridge_least_squares` 实际使用

$$
\lambda_{\mathrm{eff}}
=
10^{-8}
\max\!\left(
\frac{
\operatorname{Re}\operatorname{tr}
\left[
\mathbf A_{\mathcal T_f}^{\mathsf H}
\mathbf A_{\mathcal T_f}
\right]
}{3},
1
\right),
$$

并通过增广 QR/反斜杠系统求解。验证折 MSE 和总 CV 分数为

$$
E_{c,f}
=
\frac{
\left\|
\mathbf y_{\mathcal V_f}
-
\mathbf A_{\mathcal V_f}(\Theta_c)
\widehat{\boldsymbol\alpha}_{c,-f}
\right\|_2^2
}{
|\mathcal V_f|
},
\qquad
\operatorname{CV}(\Theta_c)
=
\frac14\sum_{f=1}^{4}E_{c,f}.
$$

若 $c_{\mathrm{CV}}$ 是 CV 最低簇，则 one-SE 候选还需满足

$$
\operatorname{CV}(\Theta_c)
\leq
\operatorname{CV}(\Theta_{c_{\mathrm{CV}}})
+
\frac{
\operatorname{sd}
\left(
E_{c_{\mathrm{CV}},1},\ldots,E_{c_{\mathrm{CV}},4}
\right)
}{\sqrt4}.
$$

四个折来自同一份观测，不是四份独立噪声试验，因此这里的 one-SE 只是内部稳定性启发式规则，不能解释成独立重复实验的置信区间。`RepresentativeSuccess` 和“合格代表 oracle”依赖真值，只用于选择后的离线计分；实际规则只读取观测、候选参数、RSS、CV 分数和支持数。

运行方式为：

```matlab
setup_project(false);
run(fullfile( ...
    'experiments', 'range_doppler', ...
    'run_2d_k3_cluster_level_selector_audit.m'));
```

在主容差 $0.25\%$ 下，每个场景平均有 $1.1267$ 个合格代表簇。开发集配对结果为：

| 方法 | overall | 95% Wilson 区间 | hard | transition | easy | 相对最低 RSS 救回/伤害 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| minimum RSS | $143/150=95.33\%$ | $[90.68\%,97.72\%]$ | $45/50$ | $48/50$ | $50/50$ | — |
| four-fold CV | $144/150=96.00\%$ | $[91.55\%,98.15\%]$ | $46/50$ | $48/50$ | $50/50$ | $1/0$ |
| maximum support | $145/150=96.67\%$ | $[92.43\%,98.57\%]$ | $47/50$ | $48/50$ | $50/50$ | $2/0$ |
| CV one-SE + support | $145/150=96.67\%$ | $[92.43\%,98.57\%]$ | $47/50$ | $48/50$ | $50/50$ | $2/0$ |
| 合格代表 oracle | $147/150=98.00\%$ | — | $49/50$ | $48/50$ | $50/50$ | — |

容差敏感性为：

| 近 RSS 容差 | 平均合格簇数 | 合格代表 oracle | minimum RSS | four-fold CV | maximum support | CV one-SE + support |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1.0000$ | $95.33\%$ | $95.33\%$ | $95.33\%$ | $95.33\%$ | $95.33\%$ |
| $0.01\%$ | $1.0067$ | $95.33\%$ | $95.33\%$ | $95.33\%$ | $95.33\%$ | $95.33\%$ |
| $0.025\%$ | $1.0200$ | $96.00\%$ | $95.33\%$ | $96.00\%$ | $95.33\%$ | $95.33\%$ |
| $0.05\%$ | $1.0533$ | $97.33\%$ | $95.33\%$ | $96.00\%$ | $96.67\%$ | $96.67\%$ |
| $0.10\%$ | $1.0867$ | $97.33\%$ | $95.33\%$ | $96.00\%$ | $96.67\%$ | $96.67\%$ |
| $0.25\%$ | $1.1267$ | $98.00\%$ | $95.33\%$ | $96.00\%$ | $96.67\%$ | $96.67\%$ |
| $0.50\%$ | $1.1733$ | $98.00\%$ | $95.33\%$ | $96.00\%$ | $96.67\%$ | $96.67\%$ |

从 $0.05\%$ 到 $0.5\%$，支持数规则在该开发集上都保持 $145/150$，纯 CV 保持 $144/150$。四个原最低 RSS 选择失败场景的主容差结果为：

| 场景 | 合格簇数 | 开发集上被救回的规则 | 其余规则失败原因 |
| --- | ---: | --- | --- |
| `hard_22` | $3$ | maximum support、one-SE + support | 最低 RSS 簇支持数为 $1$，成功簇支持数为 $3$；纯 CV 仍选最低 RSS 簇 |
| `hard_28` | $2$ | four-fold CV | 错误簇支持数为 $6$，成功簇支持数为 $2$ |
| `hard_39` | $2$ | 无 | 成功簇 RSS 高 $0.2338\%$、支持数为 $1$，CV 也更差 |
| `hard_50` | $5$ | maximum support、one-SE + support | 规则选择支持数为 $4$ 的成功簇；最低 RSS 簇支持数为 $1$ |

这些案例表明 CV 和支持数利用了互补证据，但当前简单组合没有同时救回三者。更重要的是，maximum support 只有 $2$ 个改善、$0$ 个退化，McNemar 精确双侧检验为 $p=0.5$，总体 Wilson 区间也高度重叠。因此，$145/150$ 只是开发集上的探索结果，不能据此宣称显著优于最低 RSS。

开发阶段据此冻结以下设置用于独立验证：

```text
ClusterDistanceThreshold = 0.05 Rayleigh
NearRSSRelativeTolerance = 0.0025 = 0.25%
NumFolds = 4
ValidationRidge = 1e-8
PrimarySelector = maximum support
BaselineSelector = minimum RSS
```

本节输出为：

```text
data/range_doppler_k3_cluster_level_selector_audit.mat
data/range_doppler_k3_cluster_level_selector_audit_summary.csv
data/range_doppler_k3_cluster_level_selector_audit_trials.csv
data/range_doppler_k3_cluster_level_selector_audit_candidates.csv
images/range_doppler_k3_cluster_level_selector_audit.png
```

### 4.3 冻结选择器的独立伪随机验证

独立验证保持雷达模型、SNR、几何分层、每层样本数和所有选择器设置不变，只把基种子从开发集的 $17031$ 改为 $27031$。第 $\ell$ 层第 $t$ 次试验仍使用

$$
s_{\ell,t}
=
s_0+100000\ell+t-1,
$$

因此两批数据不复用相同的伪随机种子流。该“独立集”是同一生成机制下的新伪随机样本，不是实测外部数据。

端到端入口为：

```text
experiments/range_doppler/run_2d_k3_cluster_selector_holdout_validation.m
demos/demo_2d_k3_cluster_selector_validation_report.m
tests/test_range_doppler_k3_cluster_selector_validation_report.m
```

启动脚本依次执行：

```text
新种子固定 K=3 随机几何 pilot
→ 全部 1200 条分支最低 RSS 审计
→ 固定 0.05 Rayleigh 解聚类
→ 固定 0.25% 近 RSS 簇级选择
→ 开发集/独立集配对汇总
```

运行方式为：

```matlab
setup_project(false);
run(fullfile( ...
    'experiments', 'range_doppler', ...
    'run_2d_k3_cluster_selector_holdout_validation.m'));
```

独立集仍含 hard、transition 和 easy 各 $50$ 个场景。其基础求解结果为：

| 层级 | 顺序 NOMP | 最低 RSS | 任一分支诊断 | 真值初始化诊断 |
| --- | ---: | ---: | ---: | ---: |
| overall | $140/150=93.33\%$ | $141/150=94.00\%$ | $145/150=96.67\%$ | $146/150=97.33\%$ |
| hard | $43/50$ | $43/50$ | $46/50$ | $48/50$ |
| transition | $47/50$ | $48/50$ | $49/50$ | $48/50$ |
| easy | $50/50$ | $50/50$ | $50/50$ | $50/50$ |

在 $0.05$ Rayleigh 下，$1200$ 条分支形成 $256$ 个解簇，平均每场景 $1.7067$ 个。固定 $0.25\%$ 门限后，平均仅有 $1.0867$ 个合格代表簇；合格代表 oracle 为 $143/150$，而全部分支 oracle 为 $145/150$。两者相差的两个场景含有成功分支，但没有门限内成功的簇代表。

开发集与独立集的选择结果为：

| 数据集 | minimum RSS | four-fold CV | maximum support | CV one-SE + support | 合格代表 oracle |
| --- | ---: | ---: | ---: | ---: | ---: |
| 开发集 | $143/150=95.33\%$ | $144/150=96.00\%$ | $145/150=96.67\%$ | $145/150=96.67\%$ | $147/150=98.00\%$ |
| 独立集 | $141/150=94.00\%$ | $141/150=94.00\%$ | $141/150=94.00\%$ | $141/150=94.00\%$ | $143/150=95.33\%$ |

独立集分层结果为：

| 方法 | hard | transition | easy |
| --- | ---: | ---: | ---: |
| minimum RSS | $43/50$ | $48/50$ | $50/50$ |
| four-fold CV | $43/50$ | $48/50$ | $50/50$ |
| maximum support | $43/50$ | $48/50$ | $50/50$ |
| CV one-SE + support | $43/50$ | $48/50$ | $50/50$ |
| 合格代表 oracle | $45/50$ | $48/50$ | $50/50$ |

冻结的 maximum support 与最低 RSS 在独立集上的逐场景成功标志完全相同：救回 $0$ 次、伤害 $0$ 次，均为 $141/150$，95% Wilson 区间为 $[88.99\%,96.81\%]$，McNemar 精确双侧 $p=1$。因此，开发集中的两次净救回没有在新种子数据中复现，maximum support 不能替换最低 RSS 成为正式规则；纯 CV 和 one-SE 组合也没有独立集净改善。

独立集共有四个“存在成功分支但最低 RSS 失败”的场景：

| 场景 | 可观测候选结构 | 冻结规则未救回的原因 |
| --- | --- | --- |
| `hard_15` | 门限内成功代表 RSS 高 $0.1389\%$、支持数 $3$；最低 RSS 混合簇支持数 $5$ | 最低 RSS 簇同时具有更低 CV 和更高支持数 |
| `hard_42` | 成功代表 RSS 高 $0.2411\%$、支持数 $2$ | 另一错误簇支持数 $4$ 且 CV 更低；maximum support 改选后仍然失败 |
| `hard_45` | 最低 RSS 簇内部含成功成员，但代表失败；下一个独立成功代表 RSS 高 $2.5798\%$ | $0.25\%$ 门限内没有成功代表 |
| `transition_27` | RSS 高 $0.0883\%$ 的混合簇代表失败；独立成功代表 RSS 高 $0.2597\%$ | 成功代表刚超过冻结门限，门限内混合簇代表仍失败 |

另有 `hard_17`、`hard_25`、`hard_30`、`hard_36` 和 `transition_17` 的八条分支全部失败，任何现有簇级选择器都无法修复。

这些结果给出三点限制：

1. 正确簇不一定具有更高支持数，`hard_42` 中支持数最高的候选仍然错误；
2. “簇内含成功成员”不等于簇代表成功，且该信息依赖真值，不能用于在线选择；
3. 不能因为 `transition_27` 的成功代表位于 $0.2597\%$ 就在独立集上把门限从 $0.25\%$ 向上调整，否则相当于再次用验证集调参。

因此，本阶段冻结最低 RSS 为正式基线，解聚类、支持数和 CV 只保留为诊断。若未来仍要检验约 $1\%$ 量级的选择收益，需要预先固定新规则并扩大新的独立样本，而不能继续复用这两批数据调参。

主要输出为：

```text
data/range_doppler_k3_cluster_selector_holdout_seed27031_pilot.mat
data/range_doppler_k3_cluster_selector_holdout_seed27031_minimum_rss.mat
data/range_doppler_k3_cluster_selector_holdout_seed27031_solution_clusters.mat
data/range_doppler_k3_cluster_selector_holdout_seed27031_selector.mat
data/range_doppler_k3_cluster_selector_holdout_validation_report.mat
data/range_doppler_k3_cluster_selector_holdout_validation_summary.csv
data/range_doppler_k3_cluster_selector_holdout_validation_primary_comparison.csv
images/range_doppler_k3_cluster_selector_holdout_validation.png
```

### 4.4 混合解簇的连续定位误差审计

独立验证出现了同一 $0.05$ Rayleigh 解簇同时包含成功和失败分支的现象。为区分“不同错误解被连在一起”和“同一近似解跨过硬成功阈值”，新增：

```text
demos/demo_2d_k3_mixed_solution_cluster_boundary_audit.m
experiments/range_doppler/run_2d_k3_mixed_solution_cluster_boundary_audit.m
tests/test_range_doppler_k3_mixed_solution_cluster_boundary_audit.m
```

该审计直接读取开发集与独立集各 $1200$ 条已保存分支，沿用第 4.1 节的 $0.05$ Rayleigh 连通分量聚类，不重新生成观测，也不重新运行 NOMP。

沿用第 2.2 节的最优目标匹配，定义分支 $b$ 的双轴最大归一化误差

$$
e_{R,b}
=
\max_k
\frac{
|\widehat R_{b,k}-R_{\pi^\star(k)}|
}{
\Delta R_{\mathrm{res}}
},
\qquad
e_{v,b}
=
\max_k
\frac{
|\delta_{P_v}(
\widehat v_{b,k}-v_{\pi^\star(k)})|
}{
\Delta v_{\mathrm{res}}
},
$$

以及联合误差和有符号成功裕量

$$
e_{\infty,b}
=
\max(e_{R,b},e_{v,b}),
\qquad
m_b
=
\tau_{\mathrm{loc}}-e_{\infty,b},
\qquad
\tau_{\mathrm{loc}}=0.10.
$$

因此

$$
\operatorname{Success}_b
\iff
e_{\infty,b}\leq0.10.
$$

$m_b>0$ 表示成功且尚有正裕量，$m_b<0$ 表示越界，$|m_b|$ 是分支到硬成功边界的距离。混合簇定义为同一解簇中同时含成功与失败分支；审计还计算其最近成功–失败完整解距离

$$
d_{\pm,c}
=
\min_{\substack{
b_+\in c,\ \operatorname{Success}_{b_+}=1\\
b_-\in c,\ \operatorname{Success}_{b_-}=0
}}
d(\Theta_{b_+},\Theta_{b_-}).
$$

运行方式为：

```matlab
setup_project(false);
run(fullfile( ...
    'experiments', 'range_doppler', ...
    'run_2d_k3_mixed_solution_cluster_boundary_audit.m'));
```

总体结果为：

| 数据集 | 场景数 | 解簇数 | 混合簇数 | 混合簇率 | 混合成员数 | 成功/失败成员 | 最低 RSS 选中混合簇 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| development | $150$ | $261$ | $2$ | $0.766\%$ | $4$ | $2/2$ | $0$ |
| holdout | $150$ | $256$ | $3$ | $1.172\%$ | $10$ | $4/6$ | $2$ |
| 合计 | $300$ | $517$ | $5$ | $0.967\%$ | $14$ | $6/8$ | $2$ |

混合簇只出现在 $5/300=1.67\%$ 的场景中。$14$ 个混合成员到成功边界的距离为：

| 边界距离 | 成员数 |
| --- | ---: |
| $\leq0.001$ Rayleigh | $3/14=21.4\%$ |
| $\leq0.005$ Rayleigh | $7/14=50.0\%$ |
| $\leq0.010$ Rayleigh | $10/14=71.4\%$ |
| $\leq0.025$ Rayleigh | $12/14=85.7\%$ |

其中位边界距离为 $0.004780$ Rayleigh，最小值为 $0.000212$ Rayleigh；$12/14$ 个成员由距离误差决定是否越过阈值，$2/14$ 个由速度误差决定。

五个混合簇的明细为：

| 数据集 | 场景 | 簇 RSS 排名 | 支持数 | 成功/失败 | 最低 RSS 簇 | 代表裕量 | 最近边界距离 | $d_{\pm,c}$ |
| --- | --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| development | `hard_39` | $3$ | $2$ | $1/1$ | 否 | $+0.003854$ | $0.003854$ | $0.049884$ |
| development | `hard_48` | $2$ | $2$ | $1/1$ | 否 | $+0.006074$ | $0.000512$ | $0.042882$ |
| holdout | `hard_15` | $1$ | $5$ | $2/3$ | 是 | $-0.005107$ | $0.000405$ | $0.004866$ |
| holdout | `hard_45` | $1$ | $3$ | $1/2$ | 是 | $-0.001774$ | $0.001774$ | $0.044301$ |
| holdout | `transition_27` | $2$ | $2$ | $1/1$ | 否 | $-0.000212$ | $0.000212$ | $0.025394$ |

`hard_15` 的最低 RSS 代表满足 $e_\infty=0.105107$，只越界 $0.005107$；同簇成功成员满足 $e_\infty=0.099595$，到边界仅 $0.000405$，且 $d_{\pm,c}=0.004866$。在当前距离聚类口径下，这更像同一近似解内部的标签跨界，而不是两个明显分离的候选解；这是一项由连续距离支持的解释，不是对目标函数收敛盆地的严格证明。

但不能把全部混合现象归为舍入误差。五个混合簇的 $d_{\pm,c}$ 中位数为 $0.042882$ Rayleigh，`hard_39` 达到 $0.049884$；`hard_45` 的簇直径为 $0.069539$，超过连边阈值 $0.05$，正是单链接连通分量可能通过中间成员形成链式合并的例子。由此可见，部分混合簇具有可见的连续误差跨度。

本节支持的结论是：

1. 混合簇只占全部解簇的 $0.967\%$，不否定 $0.05$ Rayleigh 聚类作为总体诊断表示；
2. 混合成员高度集中在 $0.10$ Rayleigh 硬成功边界附近，单一 0/1 成功率对千分之几 Rayleigh 的变化较敏感；
3. 独立集中两个被最低 RSS 选中的混合簇，至少部分损失来自代表解跨过硬阈值，不能全部解释为选择了完全不同的错误解；
4. `ContainsSuccessfulBranch`、混合标签和边界裕量都依赖真值，不能直接用来选择簇代表；
5. 不应为消除混合簇而事后修改 $0.10$ 定位容差或 $0.05$ 聚类阈值；
6. 后续报告应同时给出严格成功率和连续 $e_\infty$ 分布。

正式输出为：

```text
data/range_doppler_k3_mixed_solution_cluster_boundary_audit.mat
data/range_doppler_k3_mixed_solution_cluster_boundary_audit_summary.csv
data/range_doppler_k3_mixed_solution_cluster_boundary_audit_clusters.csv
data/range_doppler_k3_mixed_solution_cluster_boundary_audit_branches.csv
images/range_doppler_k3_mixed_solution_cluster_boundary_audit.png
```

综上，第 4 章完成了“分支去重—开发集选器—独立验证—边界效应解释”的闭环。解聚类把原始第二分支 RSS 间隔中位数从 $4.4342\times10^{-11}$ 转化为第二不同解簇的 $1.8196\%$，从而排除了大量重复收敛对候选解释的干扰；但开发集上的支持数收益没有通过独立验证。因此项目继续使用最低 RSS 作为正式输出，并把簇级信息限制在诊断和后续预先冻结的新实验中。
## 5. 追加 LM、优化预算与迭代上限门控

第 4 章说明，改变簇级代表或二次选择器并未在独立数据上稳定优于最低 RSS。剩余问题因而转向第一次联合 VarPro-LM 是否已经充分优化。本章依次回答四个问题：

| 问题 | 对照 | 统计身份 | 本章结论 |
| --- | --- | --- | --- |
| 簇内共识能否改善结果 | 最低 RSS 成员、medoid、对齐中位中心 | 已查看两种子的探索性审计 | 未见共识初始化的独立收益 |
| 追加 10 步能否在新种子复现 | 原最低 RSS 与全簇追加 LM | 第三种子的一次性冻结验证 | 点估计同向，但配对证据不足 |
| 收益来自总预算还是 LM 重启 | 单次 20 步与 10+10 重启 | 三个已查看种子的机制审计 | 两者成功标签相同，未见重启优势 |
| 如何形成可复用实现 | 全量 10+10 与迭代上限门控 10+10 | 回顾性实现冻结 | 门控少调用 45.95%，保持当前端点 |

本章只改变第一次联合精修之后的预算分配，不改变 Top-$L$ 粗分支、原 10 步结果、解聚类阈值、unknown-$K$ 判决或成功标准。主要代码和结果入口集中列于下表，后文不再粘贴完整启动脚本。

| 阶段 | 运行入口 | 主结果表 |
| --- | --- | --- |
| 共识与同预算审计 | `experiments/range_doppler/run_2d_k3_cluster_consensus_refinement_audit.m` | `data/range_doppler_k3_cluster_consensus_refinement_audit_selection_summary.csv` |
| 第三种子验证 | `experiments/range_doppler/run_2d_k3_all_cluster_extra_lm_seed37031_validation.m` | `data/range_doppler_k3_all_cluster_extra_lm_seed37031_validation_summary.csv` |
| 20 步与 10+10 机制审计 | `experiments/range_doppler/run_2d_k3_lm_budget_restart_mechanism_audit.m` | `data/range_doppler_k3_lm_budget_restart_mechanism_audit_selection_summary.csv` |
| 门控实现冻结 | `experiments/range_doppler/run_2d_k3_iteration_cap_gated_extra_lm_freeze_audit.m` | `data/range_doppler_k3_iteration_cap_gated_extra_lm_freeze_audit_summary.csv` |

### 5.1 簇内共识代表与追加联合精修的同预算审计

本节的目的不是再次调簇级选择器，而是拆分两个可能混在一起的因素：

1. 簇内多个解形成的“共识初值”是否优于最低 RSS 成员；
2. 结果改善是否只是因为又增加了 10 步联合 VarPro-LM。

审计精确重放 `17031` 和 `27031` 两批数据。真值只在所有代表、追加精修和跨簇最低 RSS 选择完成后用于评分，不参与代表构造或候选接受。

**代表与同预算对照**

设簇 $\mathcal C$ 中的完整三目标解为 $\widehat\Theta_i$，解间距离沿用第 4.1 节的置换不变、速度周期化、Rayleigh 归一化距离 $d(\cdot,\cdot)$。medoid 定义为

$$
i_{\mathrm{medoid}}
=
\arg\min_{i\in\mathcal C}
\sum_{j\in\mathcal C}
d(\widehat\Theta_i,\widehat\Theta_j).
$$

若总距离并列，则用较小的完整数据 RSS 打破平局。对齐中位中心则先以最低 RSS 解为参考，对簇成员进行目标排列对齐和周期速度解缠，再逐坐标取距离、速度中位数。由于该中心通常不是已保存分支，还需重新估计复幅度并计算完整数据 RSS。

对每个多成员簇比较六种代表或路径：

| 方法 | 是否为原成员 | 是否追加 10 步 LM | 作用 |
| --- | ---: | ---: | --- |
| 最低 RSS 代表 | 是 | 否 | 原基线 |
| medoid | 是 | 否 | 检查真实成员共识 |
| 对齐中位中心 | 否 | 否 | 检查坐标共识 |
| 最低 RSS 代表续跑 | 是 | 是 | 追加预算对照 |
| 对齐中位中心续跑 | 否 | 是 | 共识初值对照 |
| 两条续跑的 RSS 保护版本 | 视接受结果而定 | 是 | 排除 RSS 变差候选 |

其中 RSS 保护为

$$
\widehat\Theta_{\mathrm{safe}}
=
\begin{cases}
\widehat\Theta_{\mathrm{LM}},
&
\operatorname{RSS}(\widehat\Theta_{\mathrm{LM}})
<
\operatorname{RSS}(\widehat\Theta_{\min}),\\
\widehat\Theta_{\min},
&
\text{其他情况}.
\end{cases}
$$

该条件完全不使用真值，但只保证数据残差不增加，不能保证物理参数误差不增加。

两批数据共有 300 份观测、2400 条分支和 517 个解簇。其中 166 个单成员簇原样透传；351 个多成员簇包含 2234 条分支，并分别从最低 RSS 代表和对齐中位中心追加 10 步，因此新增 $351\times2=702$ 次联合精修。

| 数据集 | 观测数 | 解簇数 | 单成员簇 | 多成员簇 | 两条追加路径求解器失败 |
| --- | ---: | ---: | ---: | ---: | ---: |
| development | 150 | 261 | 85 | 176 | 0 / 0 |
| holdout | 150 | 256 | 81 | 175 | 0 / 0 |
| 合计 | 300 | 517 | 166 | 351 | 0 / 0 |

两条追加路径在 10 步内满足内部收敛标志的比例分别为 90.03% 和 88.60%。其余调用均返回有限解，属于“达到迭代上限”等正常停止状态，而非求解器异常。

**原检查点结果与机制判断**

原始审计表记录为：

| 方法 | development | holdout | 相对最低 RSS 的救回/伤害 |
| --- | ---: | ---: | ---: |
| 最低 RSS | 143/150 | 141/150 | 0/0；0/0 |
| medoid | 143/150 | 141/150 | 0/0；0/0 |
| 对齐中位中心，不追加 LM | 143/150 | 141/150 | 0/0；0/0 |
| RSS 保护的最低 RSS 代表续跑 | 145/150 | 142/150 | 2/0；2/1 |
| RSS 保护的中位中心续跑 | 145/150 | 142/150 | 2/0；2/1 |

按该检查点，两批合并由 $284/300=94.67\%$ 变为 $287/300=95.67\%$，共有 4 次救回、1 次伤害，描述性精确 McNemar 检验为 $p=0.375$。但这不是本章的最终性能计数：第 5.3 节用全精度数值状态复放后发现，development 的 `transition_24` 救回依赖 CSV 中 `%.10g` 参数字符串恢复的量化起点。该样本在全精度复放下仍然失败，因此最终三种子统计以第 5.3 节为准。

尽管原计数需要修正，这一阶段仍给出三个稳定的机制信息：

- medoid 在 280/351 个多成员簇中不同于最低 RSS 成员，但没有增加成功观测；
- 对齐中位中心到最低 RSS 代表的中位距离只有 $4.7005\times10^{-6}$ Rayleigh；两种初值追加相同预算后，300 份观测的成功标签完全一致；
- 最低 RSS 代表续跑在 278/351 个多成员簇中取得更低的最终 RSS，中位中心路径仅为 61/351，另有 12 个近似相同。

因此，没有证据支持引入更复杂的共识代表。早期改善更合理的解释是“增加了优化预算”，而不是“共识初始化更好”。

RSS 保护的局限由 holdout `hard_43` 直接说明：RSS 相对下降约 0.07055%，但 $e_\infty$ 从 0.024737 增至 0.100004 Rayleigh，成功样本反而越过 0.10 边界成为失败。故

$$
\operatorname{RSS}_{\mathrm{new}}<\operatorname{RSS}_{\mathrm{old}}
\quad\not\Rightarrow\quad
e_{\infty,\mathrm{new}}<e_{\infty,\mathrm{old}}.
$$

本节的结论是：保留“最低 RSS 簇代表追加联合 LM”作为待验证候选，放弃 medoid 和对齐中位中心作为默认设计；同时把 RSS 保护理解为数值拟合保护，而不是定位精度保证。

### 5.2 第三伪随机种子的全簇追加 LM 验证

由于 `17031` 和 `27031` 已参与候选形成，本节在查看结果前冻结第三个种子 `37031`，对每个簇——包括单成员簇——执行相同的追加 10 步联合 VarPro-LM。该实验用于检验第 5.1 节观察到的方向能否在一次未查看批次中复现，而不是追求逐个种子累积显著性。

**冻结协议**

| 项目 | 固定值 |
| --- | --- |
| 样本量 | hard、transition、easy 各 50，共 150 |
| Top-$L$ 分支 | 每份观测 8 条 |
| 解聚类阈值 | $d_{\mathrm{cluster}}=0.05$ Rayleigh |
| 簇代表 | 簇内完整数据 RSS 最低成员 |
| 追加精修 | 每个簇最多 10 步联合 VarPro-LM |
| Ridge / 重复距离 | $10^{-8}$ / $0.05$ Rayleigh |
| RSS 相对保护容差 | $\tau_{\mathrm{RSS}}=10^{-12}$ |
| 成功条件 | 距离、速度最大误差均不超过 0.10 Rayleigh |
| 主比较 | RSS 保护方法与原最低 RSS 的配对成功标签 |
| 主检验 | 双侧精确 McNemar，$\alpha=0.05$ |

对第 $j$ 个簇，先固定最低 RSS 代表

$$
\widehat\Theta_{j,0}
=
\arg\min_{\widehat\Theta_i\in\mathcal C_j}
\operatorname{RSS}(\widehat\Theta_i),
$$

再得到追加精修候选 $\widehat\Theta_{j,\mathrm{LM}}$。仅在

$$
\operatorname{RSS}(\widehat\Theta_{j,\mathrm{LM}})
<
\operatorname{RSS}(\widehat\Theta_{j,0})
-
\tau_{\mathrm{RSS}}
\max\!\left\{
\operatorname{RSS}(\widehat\Theta_{j,0}),\epsilon
\right\}
$$

成立时接受新候选，否则回退 $\widehat\Theta_{j,0}$。所有簇处理完毕后，才跨簇选择完整数据 RSS 最低者并计算真值误差。

**结果与证据边界**

第三批产生 260 个簇，其中 82 个单成员簇和 178 个多成员簇；260 个簇全部实际调用 LM，求解器失败为 0。

| 方法 | overall | hard | transition | easy | 救回/伤害 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 原最低 RSS | 142/150 | 43/50 | 50/50 | 49/50 | 0/0 |
| 全簇追加 LM，不保护 | 143/150 | 44/50 | 50/50 | 49/50 | 1/0 |
| 全簇追加 LM，RSS 保护 | 143/150 | 44/50 | 50/50 | 49/50 | 1/0 |

点估计由 94.67% 增至 95.33%，配对风险差为 $+0.667$ 个百分点，配对 Wald 95% 区间约为 $[-0.64,1.97]$ 个百分点；只有 1 个不一致配对，因此双侧精确 McNemar 检验为 $p=1$。这说明观察方向与前两批一致，但信息量不足以区分小幅真实增益与随机波动。

| 簇类型 | 数量 | 10 步内收敛标志 | 严格 RSS 下降 | 中位 RSS 降幅 | 中位移动 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 单成员簇 | 82 | 37/82 = 45.12% | 80/82 = 97.56% | 0.5055% | 0.16223 Rayleigh |
| 多成员簇 | 178 | 155/178 = 87.08% | 90/178 = 50.56% | 约 $1.12\times10^{-10}\%$ | 0 |
| 合计 | 260 | 192/260 = 73.85% | 170/260 = 65.38% | 约 $6.03\times10^{-9}\%$ | $6.35\times10^{-6}$ Rayleigh |

这里“未在 10 步内满足收敛判据”不等于失败；全部 260 次调用都返回有限结果。单成员簇的收敛标志率低于多成员簇，而绝大多数单成员簇仍可降低 RSS，说明“簇中只有一个保存成员”并不意味着该成员已经充分优化。

唯一的端到端救回是 `hard_11`。它来自一个四成员簇，最终簇没有改变：RSS 由 0.00998985 降至 0.00978793，$e_\infty$ 由 0.108460 降至 0.054437 Rayleigh。这支持“正确局部盆地已经存在，但第一次联合精修预算不足”的解释。

连续误差则提醒不能只看成功数：

| 方法 | 平均 $e_\infty$ | 中位 $e_\infty$ | P90 $e_\infty$ |
| --- | ---: | ---: | ---: |
| 原最低 RSS | 0.044768 | 0.012829 | 0.075220 |
| 不保护追加 LM | 0.048122 | 0.012829 | 0.073815 |
| RSS 保护追加 LM | 0.048146 | 0.012829 | 0.073815 |

P90 略降，但平均误差上升。主要反例之一是 `hard_23`：RSS 下降约 0.02998%，跨簇选择后 $e_\infty$ 从 1.58297 增至 2.16019 Rayleigh；它原本和最终都失败，因此没有出现在二值伤害计数中。

本节只能得到有限结论：全簇追加 LM 的工程路径稳定，点估计方向同前，但 $p=1$ 且区间跨 0，尚不足以替换默认算法。下一步需要区分“总迭代预算增加”与“第 10 步后重置 LM 状态”这两个机制。

### 5.3 单次 20 步与 10+10 重启的 LM 预算审计

本节复用三个已经查看的种子 `17031/27031/37031`，共 450 份观测、777 个固定候选簇，不再生成新数据。因此它是探索性机制审计，所列 $p$ 值和区间均为描述统计，不具有新的未查看验证含义。

**公平对照**

每个簇固定同一个原始粗分支起点，并比较：

| 路径 | 起点 | 迭代预算 | 第 10 步后是否重置 LM 状态 |
| --- | --- | ---: | ---: |
| 原基线 | 原 Top-$L$ 粗分支 | 最多 10 步 | 不适用 |
| 连续 20 步 | 同一粗分支 | 单次最多 20 步 | 否 |
| 10+10 重启 | 原 10 步全精度输出 | 再运行最多 10 步 | 是 |

候选簇仍由原 10 步分支和 $d_{\mathrm{cluster}}=0.05$ Rayleigh 固定；簇代表仍是原 10 步成员中完整数据 RSS 最低者。两条追加预算路径都相对原 10 步结果使用 $\tau_{\mathrm{RSS}}=10^{-12}$ 的严格 RSS 保护，且只在全部路径和跨簇选择固定后计算真值误差。

“连续 20 步”必须从相同粗起点重新运行一次最大 20 步 LM。当前求解器没有导出可恢复的内部阻尼状态；把 10 步结果重新传给求解器本身就是重启，不能代表连续路径。

**三种子全精度结果**

| 方法 | 成功数 | 成功率 | 救回/伤害 | 平均 $e_\infty$ | P90 $e_\infty$ | 相对基线改选簇 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 原 10 步 | 426/450 | 94.667% | 0/0 | 0.039878 | 0.061765 | 0 |
| RSS 保护连续 20 步 | 429/450 | 95.333% | 4/1 | 0.039098 | 0.059078 | 42 |
| RSS 保护 10+10 重启 | 429/450 | 95.333% | 4/1 | 0.039100 | 0.059074 | 43 |

两条追加预算路径相对原 10 步的配对风险差均为 $+0.667$ 个百分点，描述性 95% 区间为 $[-0.306,1.640]$ 个百分点，双侧精确 McNemar $p=0.375$。分种子成功数为：

| 数据集 | 原 10 步 | 连续 20 步 | 10+10 重启 |
| --- | ---: | ---: | ---: |
| development | 143/150 | 144/150 | 144/150 |
| holdout | 141/150 | 142/150 | 142/150 |
| third seed | 142/150 | 143/150 | 143/150 |
| 合计 | 426/450 | 429/450 | 429/450 |

连续 20 步与 10+10 重启在 450 份观测上的成功标签完全相同，但数值轨迹并不等价：

- 37/450 份观测选择了不同的最终簇；
- 按审计容差分类，重启路径在 76 份观测上选中更低 RSS，连续路径在 42 份上更低，332 份近似相同；
- 上述差异均未改变 0.10 Rayleigh 成功标签。

所以，当前数据没有显示“重置阻尼和停止状态”比“从同一粗起点直接给足 20 步预算”具有独立终点优势。

**停止原因与门控线索**

为解释追加预算作用，`src/algorithms/top_l_beam_range_doppler_refinement.m` 增加了三个只读诊断量：`stop_reason`、`accepted_step_count` 和 `final_damping`。它们只暴露现有求解状态，不改变算法路径。

原 10 步的 777 个簇代表中：

| 首轮停止原因 | 簇数 | 比例 |
| --- | ---: | ---: |
| 达到最大迭代数 | 420 | 54.05% |
| 距离—速度步长容差 | 203 | 26.13% |
| 静止 LM 步 | 151 | 19.43% |
| 投影梯度容差 | 3 | 0.39% |

达到上限的 420 个簇中，两条追加预算路径都经常继续移动；连续路径获得更低 RSS 的簇数为 199，重启路径为 178，另有 15 个在非等价输出下 RSS 近似并列，未呈现重启优势。

提前停止的 357 个簇中，连续 20 步会再次在相同停止条件处结束；重新启动 LM 则有 43 个簇继续移动，35 个取得严格 RSS 下降，但这些额外变化没有造成簇级或最终观测级成功标签翻转。由此形成一个事后线索：只对首轮达到迭代上限的簇代表追加 10 步，可能删除无实质端点收益的重启调用。

**全精度状态修正**

本节从原始粗分支全精度复放所有首轮结果，并发现 `transition_24` 的旧“救回”依赖以 `%.10g` 写入 CSV 后再恢复的参数。全精度结果为：

| 路径 | $e_\infty$ | 成功状态 |
| --- | ---: | --- |
| 原 10 步 | 0.21815 | 失败 |
| 连续 20 步 | 0.27214 | 失败 |
| 10+10 重启 | 0.25637 | 失败 |

尽管两条追加路径仍把 RSS 约由 0.010128 降至 0.010103，它们并未稳定救回该样本。因此，MAT 中的全精度数值字段必须作为连续优化的权威状态；CSV 只用于查看和汇总，不能作为 LM 检查点。

本节最终说明：

1. 54.05% 的簇代表在首轮 10 步时仍触及迭代上限，追加预算具有明确的数值动机；
2. 在当前 450 份观测上，两种 20 步预算都取得小幅点估计改善，但区间跨 0，不能据此断言稳定提升；
3. 连续 20 步和 10+10 重启成功标签相同，未观察到重启机制的独立优势；
4. “首轮达到迭代上限”是比“所有簇无条件续跑”更有依据的触发条件；
5. 下一步应把该触发条件写成正式函数，并在相同三种子上验证实现等价性，而不是继续查看新种子。

### 5.4 迭代上限门控追加 LM 的正式函数与冻结回归

本节把第 5.3 节的事后线索实现为可复用函数：

`src/algorithms/iteration_cap_gated_range_doppler_refinement.m`

这里仍不生成新目标或噪声，而是用 `17031/27031/37031` 完成回顾性实现冻结。因此，本节验证的是“代码是否准确实现既定规则、是否保持当前数据端点”，不是对规则泛化能力的新确认。

**正式接口与规则**

核心接口为：

```matlab
[ranges, velocities, amplitudes, diagnostics] = ...
    iteration_cap_gated_range_doppler_refinement( ...
        Y, baseline_ranges, baseline_velocities, ...
        baseline_stop_reason, model, ...
        'ExtraMaxIterations', 10, ...
        'Ridge', ridge, ...
        'DuplicateDistance', duplicate_distance, ...
        'RSSComparisonRelativeTolerance', 1e-12, ...
        'BaselineAmplitudes', baseline_amplitudes, ...
        'BaselineDataRSS', baseline_rss);
```

设首轮全精度状态为 $\widehat\Theta_0$，追加候选为 $\widehat\Theta_1$，$\tau_{\mathrm{RSS}}=10^{-12}$；当首轮停止原因为 `maximum iterations reached` 时令 $g_0=1$，否则令 $g_0=0$。正式选择规则是

$$
\widehat\Theta_{\mathrm{sel}}
=
\begin{cases}
\widehat\Theta_1,
&
g_0=1
\ \text{且}\
\operatorname{RSS}(\widehat\Theta_1)
<
\operatorname{RSS}(\widehat\Theta_0)
-
\tau_{\mathrm{RSS}}
\max\{\operatorname{RSS}(\widehat\Theta_0),\epsilon\},
\\
\widehat\Theta_0,
&
\text{其他情况}.
\end{cases}
$$

对应代码行为为：

- 首轮不是迭代上限停止：不调用第二次 LM，距离、速度、复幅度和 RSS 原样透传；
- 首轮达到迭代上限：从首轮全精度参数重新启动最多 10 步联合 VarPro-LM；
- 候选没有严格降低完整数据 RSS、包含非有限值或求解器异常：回退首轮结果；
- 所有决策只使用停止原因和数据 RSS，不使用真值。

函数还会由输入的距离、速度和复幅度重算基线 RSS，并检查

$$
\frac{
\left|
\operatorname{RSS}_{\mathrm{input}}
-
\operatorname{RSS}_{\mathrm{recomputed}}
\right|
}{
\max(\operatorname{RSS}_{\mathrm{input}},\epsilon)
}
\le 10^{-10}.
$$

若不满足，则抛出 `IterationCapGate:InconsistentBaselineState`。这可阻止量化后的 CSV 参数被误当作全精度优化状态。`diagnostics` 同时返回门控资格、是否调用、是否接受、失败标志、基线/候选/选中 RSS、追加迭代数、停止原因、接受步数、最终阻尼和最终来源。

门控位于“原 10 步分支已成簇并选出簇内最低 RSS 代表”之后，不改变：

- Top-$L$ 残差峰和粗分支；
- 原 10 步分支结果；
- $d_{\mathrm{cluster}}=0.05$ Rayleigh 的解聚类；
- 每簇最低 RSS 代表和跨簇最低 RSS 选择；
- 0.10 Rayleigh 成功阈值。

因此，它是联合精修末端的预算控制器，不是新检测器、选阶器或真值选择器。

**冻结回归结果**

| 方法 | 成功数 | 成功率 | 救回/伤害 | LM 调用 | 最终选中追加结果 | 相对基线改选簇 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 原 10 步 | 426/450 | 94.667% | 0/0 | 0 | 0 | 0 |
| 迭代上限门控 10+10 | 429/450 | 95.333% | 4/1 | 420 | 83 | 43 |
| 全簇代表 10+10 | 429/450 | 95.333% | 4/1 | 777 | 114 | 43 |

按数据集拆分：

| 数据集 | 簇数 | 达到首轮上限 | 门控避免调用 | 门控/全量成功数 | 最终簇差异 | 成功标签差异 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| development | 261 | 143 | 118 | 144/144 | 0 | 0 |
| holdout | 256 | 135 | 121 | 142/142 | 0 | 0 |
| third seed | 260 | 142 | 118 | 143/143 | 0 | 0 |
| 合计 | 777 | 420 | 357 | 429/429 | 0 | 0 |

门控把追加求解器调用从 777 次降至 420 次，减少 357 次，即 45.95%。全量重启共有 422 个簇通过严格 RSS 保护，门控保留其中 387 个；被删除的 35 个接受结果均来自提前停止后重启。最终层面的关系为：

| 核对项 | 结果 |
| --- | ---: |
| 全量 / 门控最终选中追加结果 | 114 / 83 |
| 数值输出存在细微差异的观测 | 31/450 |
| 最终选择不同簇 | 0/450 |
| 成功标签不同 | 0/450 |
| 最大选中 RSS 相对差 | $1.4432\times10^{-10}$ |
| 最大 $e_\infty$ 绝对差 | $3.5405\times10^{-6}$ Rayleigh |

这 31 份差异来自全量方法对已提前停止的代表重新启动 LM 后取得极小 RSS 下降。门控并非与全量重启逐位等价，而是在当前数据上保持了最终簇和成功标签，同时删除了对端点无实质影响的计算。

| 方法 | 平均 $e_\infty$ | 中位 $e_\infty$ | P90 $e_\infty$ |
| --- | ---: | ---: | ---: |
| 原 10 步 | 0.03987798 | 0.01279478 | 0.06176479 |
| 迭代上限门控 10+10 | 0.03909974 | 0.01277205 | 0.05907751 |
| 全簇代表 10+10 | 0.03909974 | 0.01277205 | 0.05907414 |

已保存回归记录还验证了：提前停止时零求解器调用并逐值透传；达到上限时实际调用追加 LM 并执行严格 RSS 保护；不一致的基线状态被明确拒绝；三个种子的门控端点与全量 10+10 对照一致。对应测试为 `tests/test_range_doppler_iteration_cap_gate.m`，冻结等价性详见 `data/range_doppler_k3_iteration_cap_gated_extra_lm_freeze_audit_equivalence_summary.csv`。

本节冻结的唯一候选实现为：

> 保持原 10 步成簇与最低 RSS 代表不变；仅对首轮因达到最大迭代数而停止的代表追加一次最多 10 步联合 VarPro-LM；使用全精度数值状态；仅在完整数据 RSS 严格下降时接受；任何异常均回退原结果。

该实现尚未替换 unknown-$K$ 默认主线。它只是下一章同观测配对确认的唯一候选；是否接入主线，应由预先固定的 unknown-$K$ 端到端实验决定。

## 6. unknown-$K$ 主线接入与门控确认

第 5 章在固定 $K=3$ 条件下冻结了“首轮达到迭代上限才追加 LM”的候选实现。本章进一步回答两个层次不同的问题：

| 层次 | 实验 | 能支持的结论 | 不能支持的结论 |
| --- | --- | --- | --- |
| 最终算法边界 | 门控开启的 42 点对称二维切片 | 当前 unknown-$K$ 完整链的距离—速度补偿边界 | 门控相对关闭状态的因果增益 |
| 门控效应确认 | 无结构随机几何的同观测开/关配对 | 门控对端到端成功率的配对影响 | 超出冻结模型与几何范围的普遍增益 |

主要代码和结果入口为：

| 内容 | 入口 | 主结果 |
| --- | --- | --- |
| unknown-$K$ 门控接入 | `src/algorithms/nomp_range_doppler_unknown_k.m` | `experiments/range_doppler/eval_2d_nomp_unknown_k_bic.m` |
| 42 点二维切片 | `experiments/range_doppler/run_2d_nomp_unknown_k_k3_iteration_cap_gate_boundary_mc500.m` | `data/range_doppler_unknown_k_k3_iteration_cap_gate_boundary_mc500_boundary.csv` |
| 同观测随机几何配对 | `experiments/range_doppler/run_2d_nomp_unknown_k_random_geometry_cap_gate_k0to3_kmax5_mc500.m` | `data/range_doppler_unknown_k_random_geometry_cap_gate_k0to3_kmax5_mc500_gate_paired.csv` |

### 6.1 门控接入与 42 点二维切片边界

**接入位置与不变量**

门控以默认关闭的可选项接入 unknown-$K$ 主函数，以保持旧接口兼容：

| 选项 | 默认值 | 含义 |
| --- | ---: | --- |
| `UseIterationCapGate` | `false` | 是否启用迭代上限门控 |
| `IterationCapGateExtraMaxIterations` | 10 | 追加 LM 的最大迭代数 |
| `IterationCapGateRSSComparisonRelativeTolerance` | $10^{-12}$ | 严格 RSS 下降容差 |
| `IterationCapGateBaselineRSSConsistencyRelativeTolerance` | $10^{-10}$ | 全精度基线一致性容差 |

启用后，处理顺序固定为：

1. 原 unknown-$K$ 序贯 NOMP 先完整生成所有非零 $K$ 前缀；
2. 对每个已生成前缀读取全精度全局联合精修状态；
3. 仅当该前缀的首轮全局 LM 停止原因为 `maximum iterations reached` 时，追加最多 10 步联合 VarPro-LM；
4. 追加结果先通过第 5.4 节的精修 RSS 保护，再按 `BICRidge` 重新估计复幅度并通过最终 BIC-RSS 保护；
5. 只有两道保护都通过，才替换该前缀供 BIC 评分的距离、速度、复幅度和诊断。

第二道保护要求

$$
\operatorname{RSS}_{\mathrm{BIC,gate}}
<
\operatorname{RSS}_{\mathrm{BIC,base}}
-
\tau_{\mathrm{RSS}}
\max\!\left\{
\operatorname{RSS}_{\mathrm{BIC,base}},\epsilon
\right\},
\qquad
\tau_{\mathrm{RSS}}=10^{-12}.
$$

门控不回写序贯残差，因此不会改变后续峰检测、后续 $K$ 前缀或 CFAR 停止位置。它只改变已经生成前缀的末端连续精修状态以及随后对这些固定前缀进行的 BIC 排序。

**42 点冻结协议**

该实验使用真实 $K=3$ 的对称等间隔目标：相邻目标在距离和速度维分别相差 $\eta_R$ 与 $\eta_v$ 个 Rayleigh 单位，目标中心、相位和噪声伪随机。完整前缀 BIC 为主模式，CFAR+BIC 为预先保留的次要模式。

| 项目 | 冻结值 |
| --- | --- |
| 协议 ID | `unknown_k_k3_cap_gate_boundary_mc500_confirmatory_v1` |
| 真实目标数 / 求解器上限 | $K_{\mathrm{true}}=3,\ K_{\max}=5$ |
| SNR / 幅度规则 | 20 dB / `fixed_per_target` |
| 每个二维点 | 500 份观测 |
| 网格规模 | 6 个速度切片、每片 7 点，共 42 点 |
| 总模式—观测数 | $42\times2\times500=42000$ |
| 第一阶段 / 追加预算 | 10 步 / 仅封顶前缀追加最多 10 步 |
| 成功条件 | 选阶正确，且距离、速度最大误差均不超过 0.10 Rayleigh |
| 点判据 | 双侧 Wilson 95% 区间下限不低于 0.95 |
| 切片边界 | 当前 $\eta_R$ 及所有更大已测 $\eta_R$ 均通过时的最小点 |
| 中途查看 / 提前停止 | 禁止 |

冻结网格为：

| $\eta_v$ | $\eta_R$ |
| ---: | --- |
| 0.55 | 0.10:0.025:0.25 |
| 0.45 | 0.30:0.025:0.45 |
| 0.35 | 0.40:0.025:0.55 |
| 0.25 | 0.50:0.025:0.65 |
| 0.15 | 0.65:0.025:0.80 |
| 0.10 | 0.65:0.025:0.80 |

六个切片基种子为 `[310003,320009,330017,340019,350023,360037]`，每片七个条件使用连续且不重复的种子。正式运行前未用这些种子调试或查看端点。

**完整前缀 BIC 主结果**

当前模型的距离和速度 Rayleigh 分辨率分别为 15 m 和 2.34375 m/s，因此 $\Delta R=15\eta_R$ m，$\Delta v=2.34375\eta_v$ m/s。

| $\eta_v$ | 保守 $\eta_R$ | $\Delta v$ (m/s) | $\Delta R$ (m) | 边界点成功数 | Wilson 95% 区间 | 选阶准确率 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0.10 | 0.725 | 0.234375 | 10.875 | 488/500 | [95.852%, 98.622%] | 99.4% |
| 0.15 | 0.700 | 0.351563 | 10.500 | 490/500 | [96.358%, 98.910%] | 99.6% |
| 0.25 | 0.550 | 0.585938 | 8.250 | 485/500 | [95.110%, 98.174%] | 98.6% |
| 0.35 | 0.450 | 0.820313 | 6.750 | 488/500 | [95.852%, 98.622%] | 99.6% |
| 0.45 | $\le0.300$ | 1.054688 | $\le4.500$ | 487/500 | [95.603%, 98.474%] | 98.2% |
| 0.55 | $\le0.100$ | 1.289063 | $\le1.500$ | 488/500 | [95.852%, 98.622%] | 99.0% |

$\eta_v=0.45$ 和 0.55 两条切片在本轮最小 $\eta_R$ 处已经通过，因而属于左侧截尾：数据只说明真实转折分别不大于 0.30 和 0.10，不能把这两个值写成已经精确夹定的边界。

其余四条切片由相邻网格点夹住：

| $\eta_v$ | 前一未通过点 | 首个通过点 |
| ---: | --- | --- |
| 0.10 | $\eta_R=0.700$，Wilson 下限 94.866% | $\eta_R=0.725$，Wilson 下限 95.852% |
| 0.15 | $\eta_R=0.675$，Wilson 下限 94.866% | $\eta_R=0.700$，Wilson 下限 96.358% |
| 0.25 | $\eta_R=0.525$，Wilson 下限 94.382% | $\eta_R=0.550$，Wilson 下限 95.110% |
| 0.35 | $\eta_R=0.425$，Wilson 下限 94.382% | $\eta_R=0.450$，Wilson 下限 95.852% |

CFAR+BIC 得到的保守边界为 $[0.700,0.675,0.550,0.450,\le0.300,\le0.100]$，对应 $\eta_v=[0.10,0.15,0.25,0.35,0.45,0.55]$。它只在前两个切片比完整前缀 BIC 向困难区移动一个网格点，主边界仍按完整前缀 BIC 报告。

42 点等权汇总及门控使用情况为：

| 指标 | 完整前缀 BIC | CFAR+BIC |
| --- | ---: | ---: |
| 选阶正确率 | 99.257% | 99.400% |
| 端到端成功率 | 20589/21000 = 98.043% | 20621/21000 = 98.195% |
| 生成的非零前缀 | 105000 | 63768 |
| 门控调用 | 16129 | 9461 |
| 严格 RSS 接受 | 14573 | 8430 |
| 接受/调用 | 90.353% | 89.103% |
| 最终所选前缀采用门控 | 13.676% | 13.662% |
| 相对全前缀重启避免的调用 | 84.639% | 85.163% |
| 外层求解失败 | 0 | 0 |

完整性检查确认 42000 行结果、42 个条件种子、两模式逐观测配对、最终错误日志为空，冻结源码指纹在运行前后保持一致。该早期结果表尚未单列门控函数内部已捕获并回退的异常数；这一审计字段已在第 6.2 节正式补齐。

本节只能得出“门控开启后的最终算法在对称二维切片上的边界”。因为没有在同一批 42000 行观测上同时运行门控关闭分支，不能把边界相对旧实验的变化全部归因于门控；而且对称等间隔切片也不能替代无结构随机几何。

### 6.2 无结构随机几何的同观测门控确认

本节使用新的冻结批次，在每个随机场景上同时运行完整 BIC/CFAR+BIC 与门控关/开四个分支。目标、相位、噪声、观测能量和复校验和逐值相同，因此门控开关的差异具有同观测配对解释。

**随机几何与冻结协议**

对 $K\ge2$，生成器在以随机安全中心为基准的归一化二维盒内抽取无结构局部坐标，不强制对角、等间隔或任一坐标轴的最小跨度。距离、速度半跨度分别为 0.90 和 0.55 Rayleigh；唯一排重条件是任意目标对的最小二维归一化距离不小于 0.10 Rayleigh。

这里的“随机目标”指安全 ROI 内随机中心附近的无结构局部簇，而不是三个目标彼此独立地遍布整个 ROI。对应最大局部轴向全跨度为 27 m 和 2.578125 m/s。

| 项目 | 冻结值 |
| --- | --- |
| 协议 ID | `random_geometry_cap_gate_k0to3_kmax5_mc500_confirmatory_v1` |
| 真实目标数 / 求解器上限 | $K_{\mathrm{true}}=0{:}3,\ K_{\max}=5$ |
| 每个真实 $K$ | 500 个独立场景 |
| 总场景 / 总求解 | 2000 / 8000 |
| 每场景分支 | 完整 BIC/CFAR+BIC $\times$ 门控关/开 |
| SNR / 幅度规则 | 20 dB / `fixed_per_target` |
| 中心 / 局部半跨度 | `safe_roi_uniform` / 0.90、0.55 Rayleigh |
| 最小二维间隔 / 最小轴跨度 | 0.10 / 0 Rayleigh |
| 第一阶段 / 追加预算 | 10 步 / 仅封顶前缀追加最多 10 步 |
| RSS 保护容差 | $10^{-12}$ |
| 定位成功阈值 | 距离、速度最大误差均不超过 0.10 Rayleigh |
| 正式基种子 | 2718281 |
| 中途查看、换种子、调阈值、提前停止 | 禁止 |

场景种子固定为 `case_seed = 2718281 + 10000*K_true + trial_index - 1`。主终点是门控开启、完整前缀 BIC、$K_{\mathrm{true}}=3$ 的端到端成功率及 Wilson 95% 区间；主方法比较是同一批 $K=3$ 完整 BIC 观测上的门控开减门控关、救回/伤害数和双侧精确 McNemar 检验。$K=0{:}2$、CFAR+BIC、几何分层和运行时间均为次要分析。

完整性核验通过：2000 个独立场景形成 8000 行结果，每场景恰有四个配对分支；外层求解失败和门控内部追加 LM 失败均为 0；正式种子与冻结源码指纹符合协议。整批运行约 23 分 14 秒。

**主结果与配对效应**

门控开启、完整前缀 BIC、$K_{\mathrm{true}}=3$ 的主终点为

$$
\frac{480}{500}=96.0\%,
\qquad
\mathrm{Wilson\ 95\%\ CI}
=
[93.903\%,97.396\%].
$$

门控开关的同观测配对结果为：

| 模式 | 门控关闭 | 门控开启 | 配对差及近似 95% 区间 | 救回/伤害 | 双侧精确 McNemar |
| --- | ---: | ---: | ---: | ---: | ---: |
| 完整前缀 BIC（主） | 473/500 = 94.6% | 480/500 = 96.0% | $+1.4$ 个百分点，$[0.369,2.431]$ | 7/0 | $p=0.015625$ |
| CFAR+BIC（次） | 475/500 = 95.0% | 482/500 = 96.4% | $+1.4$ 个百分点，$[0.369,2.431]$ | 7/0 | $p=0.015625$ |

两个模式救回的是同一组 7 个场景。完整 BIC 中，5 个场景由 $\widehat K=4$ 修正为 $\widehat K=3$，另外 2 个在选阶已经正确时把连续定位误差压回 0.10 Rayleigh 以内；按预设几何层分布为 hard 1 个、transition 5 个、easy 1 个。

$p=0.015625$ 表示：若门控对配对成败标签没有系统影响，观察到至少当前这样极端的不一致方向的概率为 1.5625%；它不是“门控无效的概率”。

其余真实目标数的端到端成功率为：

| $K_{\mathrm{true}}$ | 完整 BIC 关 / 开 | CFAR+BIC 关 / 开 |
| ---: | ---: | ---: |
| 0 | 99.8% / 99.8% | 100% / 100% |
| 1 | 99.8% / 99.8% | 100% / 100% |
| 2 | 99.6% / 99.6% | 99.6% / 99.6% |
| 3 | 94.6% / 96.0% | 95.0% / 96.4% |

因此，该冻结批次支持门控在当前 $K=3$ 随机几何设置下带来正向端到端配对效应，同时没有观察到 $K=0{:}2$ 的成功率伤害。

**几何分层与尚未封口的边界**

完整 BIC 门控开启后的预设分层为：

| 最小二维间隔 | 样本数 | 门控关闭 | 门控开启 | 门控后 Wilson 95% 区间 | 下限是否达到 95% |
| --- | ---: | ---: | ---: | ---: | ---: |
| hard：实际 $[0.10,0.20)$ | 58 | 48/58 = 82.76% | 49/58 = 84.48% | [73.07%, 91.62%] | 否 |
| transition：$[0.20,0.40)$ | 193 | 179/193 = 92.75% | 184/193 = 95.34% | [91.38%, 97.53%] | 否 |
| easy：$[0.40,+\infty)$ | 249 | 246/249 = 98.80% | 247/249 = 99.20% | [97.12%, 99.78%] | 是 |

整体 96.0% 的点估计超过 95%，但 Wilson 下限只有 93.903%，所以 `RandomMinimum2DGap=0.10` 的整个生成范围不能按“Wilson 下限也不低于 95%”的保守标准宣告通过。只有探索性 easy 层满足该标准，这正是第 7 章重新冻结五个窄带的依据。

完整 BIC 门控开启后剩余 20 个失败可拆为 11 个选阶错误（6 个低估、5 个高估）和 9 个选阶正确但连续定位超限。门控在 500 个 $K=3$ 完整 BIC 场景中共调用追加 LM 509 次、严格接受 465 次，62 个最终所选前缀采用追加结果，其中 7 个改变端到端成败；内部追加 LM 失败为 0。

现有汇总图标题中的“近零速度”是展示层遗留标签；正式数据的随机中心速度约覆盖 $[-28.076,28.125]$ m/s。几何汇总表把 hard 元数据写成 $[0,0.2)$，但生成器强制最小间隔 0.10，实际最小值为 0.101831，因此有效范围仍是 $[0.10,0.20)$。这两个展示问题不改变主统计量。

本章结论是：unknown-$K$ 门控接入保持了前缀生成链，并在新的同观测随机几何批次中获得 $+1.4$ 个百分点、7 次救回和 0 次伤害的配对结果；但随机几何的保守最小间隔尚未由该宽范围实验确定，需要第 7 章的五带确认。

## 7. 五带随机二维最小间隔的最终边界

第 6.2 节已经用同观测配对确认门控的正向作用，但 `RandomMinimum2DGap=0.10` 覆盖的整个随机几何范围没有达到“Wilson 95% 下限不低于 95%”的保守标准。本章固定门控开启，以五个预先冻结的最小二维间隔带测量最终工作边界。

主要代码和证据入口为：

| 内容 | 文件 |
| --- | --- |
| 五带生成与汇总 | `demos/demo_2d_unknown_k_k3_random_geometry_gap_boundary.m` |
| 正式启动脚本 | `experiments/range_doppler/run_2d_nomp_unknown_k_k3_random_geometry_gap_boundary_mc500.m` |
| 主汇总表 | `data/range_doppler_unknown_k_k3_random_geometry_gap_boundary_mc500_summary.csv` |
| 边界判定表 | `data/range_doppler_unknown_k_k3_random_geometry_gap_boundary_mc500_boundary.csv` |
| 逐试验表 | `data/range_doppler_unknown_k_k3_random_geometry_gap_boundary_mc500_trials.csv` |

### 7.1 冻结协议与边界判据

对目标 $i,j$，定义速度周期差为 $\Delta v_{ij,\mathrm{per}}$，当前模型的二维归一化距离为

$$
d_{ij}
=
\sqrt{
\left(
\frac{R_i-R_j}{15\ \mathrm{m}}
\right)^2
+
\left(
\frac{\Delta v_{ij,\mathrm{per}}}
{2.34375\ \mathrm{m/s}}
\right)^2
},
\qquad
d_{\min,\mathrm{2D}}
=
\min_{i<j}d_{ij}.
$$

本章要回答的是：

> 在相同随机几何生成器、20 dB、真实 $K=3$ 和门控开启条件下，$d_{\min,\mathrm{2D}}$ 至少落入哪个区间，才能使完整前缀 BIC 端到端成功率的双侧 Wilson 95% 下限达到 95%？

第 6.2 节已查看数据中的窄带结果只用于选择五带网格，不参与本章正式证据。正式协议为：

| 项目 | 冻结值 |
| --- | --- |
| 协议 ID | `unknown_k_k3_random_geometry_gap_boundary_mc500_confirmatory_v1` |
| 真实目标数 / 求解器上限 | $K_{\mathrm{true}}=3,\ K_{\max}=5$ |
| SNR / 幅度规则 | 20 dB / `fixed_per_target` |
| 中心 | `safe_roi_uniform` |
| 距离 / 速度半跨度 | 0.90 / 0.55 Rayleigh |
| 最小轴跨度 | 0 |
| 门控 | 始终开启；首轮 10 步，封顶时追加最多 10 步 |
| 追加结果 | 完整数据 RSS 严格下降才接受 |
| 定位成功阈值 | 距离、速度最大误差均不超过 0.10 Rayleigh |
| 间隔带 / 每带场景 | 5 / 500 |
| 独立场景 / 总求解 | 2500 / 5000 |
| 主 / 次模式 | 完整前缀 BIC / CFAR+BIC |
| 点判据 | 双侧 Wilson 95% 下限不低于 0.95 |
| 中途查看、换种子、改带宽、提前停止 | 禁止 |

五个带均为左闭右开区间：

| 带序号 | $d_{\min,\mathrm{2D}}$ |
| ---: | --- |
| 1 | $[0.20,0.25)$ |
| 2 | $[0.25,0.30)$ |
| 3 | $[0.30,0.35)$ |
| 4 | $[0.35,0.40)$ |
| 5 | $[0.40,+\infty)$ |

在每带 $n=500$ 时，至少需要 485 次成功，双侧 Wilson 95% 下限才会超过 95%。边界从最高间隔带向下判定：只有当前带及全部更高带连续通过，才能把当前带下边界报告为保守边界；不能越过中间失败带，用更低间隔带的偶然通过降低边界。

正式首种子为 3141593，带间步长为 100000，每带使用 500 个连续且不重复的场景种子。正式种子没有用于测试或调参。

### 7.2 主边界结果与失败机制

完整性检查确认：2500 个独立场景形成 5000 行逐试验结果；每个场景的 BIC 与 CFAR+BIC 分支共享相同真值、噪声和观测校验和；五带实际间隔均落在冻结区间；外层 unknown-$K$ 失败和门控内部追加 LM 失败均为 0；Wilson 区间独立重算最大差为 $4.44\times10^{-16}$，五个冻结源码指纹在运行前后保持一致。

五带实际观测范围为：

| 冻结带 | 实际最小值—最大值 |
| --- | ---: |
| $[0.20,0.25)$ | 0.200151—0.249967 |
| $[0.25,0.30)$ | 0.250257—0.299978 |
| $[0.30,0.35)$ | 0.300141—0.349974 |
| $[0.35,0.40)$ | 0.350033—0.399847 |
| $[0.40,+\infty)$ | 0.401022—1.147939 |

完整前缀 BIC 是预先冻结的主分析。端到端成功要求 $\widehat K=3$，并且最优匹配后的最大距离误差与最大速度误差都不超过 0.10 Rayleigh。

| 最小二维间隔带 | 端到端成功 | Wilson 95% 区间 | 下限 $\ge95\%$ | 选阶正确率 | 选阶正确后的定位成功率 |
| --- | ---: | ---: | ---: | ---: | ---: |
| $[0.20,0.25)$ | 466/500 = 93.2% | [90.648%, 95.093%] | 否 | 97.4% | 95.688% |
| $[0.25,0.30)$ | 474/500 = 94.8% | [92.490%, 96.427%] | 否 | 97.8% | 96.933% |
| $[0.30,0.35)$ | 481/500 = 96.2% | [94.142%, 97.554%] | 否 | 98.4% | 97.764% |
| $[0.35,0.40)$ | 486/500 = 97.2% | [95.355%, 98.325%] | 是 | 99.4% | 97.787% |
| $[0.40,+\infty)$ | 498/500 = 99.6% | [98.553%, 99.890%] | 是 | 99.6% | 100% |

$[0.30,0.35)$ 的点估计已经达到 96.2%，但 Wilson 下限只有 94.142%，仍未通过冻结判据。最高两带连续通过，而下一带失败，因此

$$
\boxed{
d_{\min,\mathrm{2D}}^{\mathrm{conservative}}
=
0.35\ \text{Rayleigh}
}.
$$

边界状态为 `established_on_frozen_grid`。它表示：在冻结的模型、生成器、算法和五带网格下，$d_{\min,\mathrm{2D}}\ge0.35$ 覆盖的两个已检验带均满足保守判据；实验只把转折夹在失败带 $[0.30,0.35)$ 与通过带 $[0.35,0.40)$ 之间，不能据此宣称存在精确等于 0.350000 的理论相变点。

主模式失败可拆为：

| 最小二维间隔带 | 总失败 | 选阶错误（低估 / 高估） | 选阶正确但定位失败 |
| --- | ---: | ---: | ---: |
| $[0.20,0.25)$ | 34 | 13（6 / 7） | 21 |
| $[0.25,0.30)$ | 26 | 11（2 / 9） | 15 |
| $[0.30,0.35)$ | 19 | 8（1 / 7） | 11 |
| $[0.35,0.40)$ | 14 | 3（1 / 2） | 11 |
| $[0.40,+\infty)$ | 2 | 2（0 / 2） | 0 |
| 合计 | 95 | 37（10 / 27） | 58 |

随着二维间隔增大，选阶错误明显减少；但在 $[0.35,0.40)$ 内仍有 11 个场景选阶正确而连续定位超限。到最高带后，该类失败在本批数据中降为 0，只剩 2 次高估。因此 0.35 是“检测、选阶与联合连续精修”完整链路的边界，不是单独 BIC 或单独 LM 的边界。

### 7.3 CFAR+BIC 对照与门控稳定性

CFAR+BIC 是预先冻结的次要对照：

| 最小二维间隔带 | 端到端成功 | Wilson 95% 区间 | 下限 $\ge95\%$ | 选阶正确率 |
| --- | ---: | ---: | ---: | ---: |
| $[0.20,0.25)$ | 461/500 = 92.2% | [89.515%, 94.242%] | 否 | 95.2% |
| $[0.25,0.30)$ | 470/500 = 94.0% | [91.564%, 95.765%] | 否 | 96.6% |
| $[0.30,0.35)$ | 484/500 = 96.8% | [94.866%, 98.021%] | 否 | 99.0% |
| $[0.35,0.40)$ | 485/500 = 97.0% | [95.110%, 98.174%] | 是 | 99.2% |
| $[0.40,+\infty)$ | 500/500 = 100% | [99.238%, 100%] | 是 | 100% |

CFAR+BIC 同样得到 0.35 Rayleigh 的保守边界。逐带配对关系为：

| 间隔带 | 仅 BIC 成功 | 仅 CFAR+BIC 成功 | CFAR+BIC 减 BIC |
| --- | ---: | ---: | ---: |
| $[0.20,0.25)$ | 7 | 2 | $-1.0$ 个百分点 |
| $[0.25,0.30)$ | 5 | 1 | $-0.8$ 个百分点 |
| $[0.30,0.35)$ | 0 | 3 | $+0.6$ 个百分点 |
| $[0.35,0.40)$ | 2 | 1 | $-0.2$ 个百分点 |
| $[0.40,+\infty)$ | 0 | 2 | $+0.4$ 个百分点 |
| 五带等权合计 | 14 | 9 | $-0.2$ 个百分点 |

五带等权汇总为 BIC 2405/2500、CFAR+BIC 2400/2500；23 个不一致场景的双侧精确 McNemar 检验为 $p=0.404873$，没有显示一个模式在该配对数据上整体占优。该等权汇总是人为实验配比，不代表自然场景中五带的真实混合比例。

两种模式的选阶偏差方向不同：完整 BIC 累计低估 10 次、高估 27 次；CFAR+BIC 累计低估 37 次、高估 13 次。CFAR 前置门槛减少了部分高估，却在最难的 $[0.20,0.25)$ 带增加低估，因此没有证据用它替换完整前缀 BIC 作为主模式。

门控稳定性为：

| 模式 | 追加 LM 调用 | 严格 RSS 接受 | 接受/调用 | 最终所选前缀采用门控 | 内部 / 外层失败 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 完整前缀 BIC | 2542 | 2332 | 91.74% | 424/2500 | 0 / 0 |
| CFAR+BIC | 1705 | 1566 | 91.85% | 400/2500 | 0 / 0 |

结果读取后，与本协议直接相关的 21 项 MATLAB 回归全部通过；当前五个冻结源码文件的 SHA-256 仍与正式启动前一致。

### 7.4 工程解释、适用范围与文档衔接

在相同生成器中，正式测得的几何筛选条件可写成

$$
\left(
\frac{\Delta R}{15\ \mathrm{m}}
\right)^2
+
\left(
\frac{\Delta v_{\mathrm{per}}}
{2.34375\ \mathrm{m/s}}
\right)^2
\ge
0.35^2.
$$

若速度差为 0，轴截距为 5.25 m；若距离差为 0，轴截距为 0.8203125 m/s。两者只是同一二维边界的轴截距，不是分别经过单轴确认的两个独立下限；距离差与周期速度差可以相互补偿。

科研报告中的正式测量值应写为 0.35 Rayleigh。该值贴近判据：主 BIC 在 $[0.35,0.40)$ 的 Wilson 下限仅比要求高 0.355 个百分点，CFAR+BIC 更是以 485/500 刚好通过。若后续生成实验希望保留额外工程裕量，可以采用 $d_{\min,\mathrm{2D}}\ge0.40$；但这是基于已测结果增加的保护带，不能改写成“实验测得边界为 0.40”。

本结论仅适用于本节冻结的 $K=3$、$K_{\max}=5$、20 dB、`fixed_per_target`、随机相位、可分离原子、安全 ROI 均匀中心、0.90/0.55 Rayleigh 局部半跨度、门控开启和 0.10 Rayleigh 匹配门限。它不是二维 NOMP 的普适理论分辨率，也不能直接外推到不同 SNR、强弱目标、其他 $K$、非可分离观测、明显距离走动或更大几何范围。

本篇至此完成了“固定 $K$ 随机几何审计—候选聚类—追加 LM—unknown-$K$ 门控确认—五带边界测量”的完整证据链。算法演进与实验入口继续参见[《19.2D_NOMP未知K联合精修》](./19.2D_NOMP未知K联合精修.md)；统一观测模型、VarPro-LM、BIC/CFAR 公式、评价指标和最终引用结论以[《20.2D_NOMP联合精修》](./20.2D_NOMP联合精修.md)为准。
